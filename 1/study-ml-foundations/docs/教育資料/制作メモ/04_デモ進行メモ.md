> ※ メトリクスは `ml/registry/artifacts/{run_id}/metrics.json` で管理。

# コードデモ・シナリオ（Phase 1 版）

**章:** Ch 8（12:30〜15:30、約 3 分）  
**画面:** ターミナル中心  
**前提:** `make build` は事前完了

---

## 全体フロー

```text
1. make seed    -> PostgreSQL 投入確認
2. make train   -> 学習ログを読み解く
3. artifacts    -> Run ID / latest / metrics を確認
4. まとめ       -> 推論 API は Phase 2 で扱うことを明示
```

---

## Step 1: make seed（30 秒）

### 操作

```bash
make seed
```

### ナレーション

> まずデータ投入です。`make seed` で California Housing を PostgreSQL に投入します。

---

## Step 2: make train（90 秒）

### 操作

```bash
make train
```

### 注目ログ

- `Loading data...`
- `train: 16512 rows, test: 4128 rows`
- `Preprocessing...`
- `Feature engineering...`
- `[50] valid_0's rmse: ...`
- `[300] valid_0's rmse: ...`
- `Run ID: ...`
- `Metrics: RMSE=..., R²=...`
- `Model saved to ...`

### ナレーション

> `make train` で前処理、特徴量生成、LightGBM 学習、評価、保存まで一気に実行します。

---

## Step 3: artifacts 確認（40 秒）

### 操作

```bash
ls -la ml/registry/artifacts
ls -la ml/registry/artifacts/latest
cat ml/registry/artifacts/latest/metrics.json
```

### ナレーション

> モデルは Run ID ごとに保存され、`latest` が常に最新を指します。`metrics.json` が今回の学習成績です。

---

## Step 4: スコープ明示（20 秒）

### ナレーション

> Phase 1 は学習基礎に集中するため、推論 API は扱いません。推論とアプリ統合は Phase 2 で実施します。

---

## 撮影チェックリスト

- [ ] ターミナル履歴クリア
- [ ] `make build` 実施済み
- [ ] `make seed` / `make train` 成功確認済み
- [ ] `ml/registry/artifacts/latest/metrics.json` 存在確認
