# 02_移行ロードマップ (Phase 3: 不動産ハイブリッド検索 Local / 再構築計画 + GOAL 達成記録)

> **権威 1 位 / 決定的仕様**。Phase 3 (`3/study-hybrid-search-local/`) の再構築計画 + GOAL 達成記録 + 長期 backlog。docs/01_仕様と設計.md より優先する。本ファイルが Phase 3 の current sprint の正本兼長期 backlog。
>
> **🎯 2026-05-05 Phase 3 GOAL 達成**: Wave 1-4 全完了。検証 6 条件すべて ✅、ライブ実測 NDCG@10=0.9822 / MAP=0.9967 / Recall@20=1.0、Web UI 起動済。本ファイルは以後「達成記録 + 長期 backlog」として運用 (実測値の正本は [`05_精度評価.md`](05_精度評価.md))。

---

## 0. 改訂方針注記 (2026-05-03 → 2026-05-05 GOAL 達成)

親 [`README.md` §4 コード化方針](../../../README.md) で、教育コード化対象 Phase が **Phase 1 / 2 / 3 / 4 / 7** に限定された。Phase 5 / 6 は独立コード保守をやめ、**Phase 7 完成版コードを参照する論理 Phase** に変更。Phase 4 は Phase 7 からの引き算ではなく **Phase 3 からの足し算 (local → cloud)** として独立コード化する。

本改訂を受け、Phase 3 は **2026-05-03〜05-05 の 1 sprint で Wave 1-4 を完走** し再構築完了。Phase 7 (canonical) からの引き算で Local Docker Compose 上に 3 段ハイブリッド検索を完全動作させ、Phase 4 への足し算起点 (8 不変 Port + Local Adapter + ML パイプライン + 検証ゲート) を確定した。

これに伴い Phase 3 の位置づけは下記に整理される:

- Phase 3 は **コード化対象**。`pre-phase3to6-impl-removal` tag に残された旧実装を復元するのではなく、Phase 7 (canonical) を起点にして **GCP / Kubernetes 系を引き算した Local 完成形** として再構築する
- Phase 3 は **Phase 4 の起点 (足し算チェーン Phase 1→2→3→4 の中継地)** であり、不変 Port を保ったまま adapter を Cloud Run / BigQuery / GCS に差し替えると Phase 4 になる構造を維持する
- 本 phase の検証は **ローカルで一巡** (`make build && make seed && make train && make serve` → `curl /search` で 3 系統 (lexical / semantic / rerank) all non-zero) を達成すれば「完了」とする

参照: 親 [`README.md` §4 コード化方針](../../../README.md) / [`README.md` §4.4 Phase 2 → 3 の接続](../../../README.md) / [`README.md` §4.5 Phase 3 → 4 の接続](../../../README.md)。

---

## 1. Phase 3 の位置づけ

足し算チェーン **Phase 1 → 2 → 3 → 4** (独立コード化対象) の中継 Phase。

| 観点 | Phase 2 から継承 | Phase 3 で初登場 | Phase 4 へ送る (足し算先) |
|---|---|---|---|
| 設計思想 | Port/Adapter / `core → ports ← adapters` / DI / FastAPI lifespan | (不変) | (不変、Cloud SDK adapter に差し替え) |
| ドメイン | California Housing 回帰 | **不動産ハイブリッド検索** (3 段検索) | 不動産検索 (継続) |
| ML タスク | LightGBM 回帰 | **LightGBM LambdaRank** (NDCG@10 評価) | LambdaRank (継続) |
| Adapter | PostgreSQL / sklearn 単純 | Meilisearch / multilingual-e5 / pgvector / Redis / LightGBM | Cloud Run + Meilisearch / BigQuery `VECTOR_SEARCH` / GCS / Secret Manager |
| 実行基盤 | Docker Compose | uv + Docker Compose | uv + Cloud Run / Cloud Run Jobs |
| 検索エンドポイント | (なし) | `/search` / `/feedback` / `/health` | (継続、Cloud Run へ deploy) |

Phase 3 は **不動産ハイブリッド検索の Local 完成形**。Phase 4 で同じ Port 抽象に Cloud SDK adapter を差し込むことを前提にする (Port 定義が GCP に依存しない設計)。

---

## 2. 不変ルール (Phase 3 で絶対に変えない)

### 2.1 題材 / 中核 5 要素

| 段 | Phase 3 (Local) 実装 | Phase 7 (canonical) 実装 |
|---|---|---|
| (1a) Lexical | **Meilisearch (Docker)** BM25 + filter | Meilisearch on Cloud Run (Identity Token 認証付) |
| (1b) Semantic encoder | **multilingual-e5 (local in-process)** sentence-transformers (CPU) | KServe `property-encoder` InferenceService (HuggingFace runtime) |
| (1b) Semantic vector store | **pgvector** (`<=>` 演算子 ANN) | Vertex Vector Search (BQ は embedding 履歴正本) |
| (2) 融合 | **RRF** (Reciprocal Rank Fusion、`RRF_K=60`) | (Phase 3-7 共通、`app/services/ranking.py::rrf_fuse` 不変) |
| (3) Rerank | **LightGBM LambdaRank (in-process)**、`ml/registry/artifacts/{run_id}/model.lgb` 直読み | KServe `property-reranker` InferenceService |

中核 5 要素 (Meilisearch BM25 / multilingual-e5 / ベクトルストア / RRF / LightGBM LambdaRank) の挙動・データフロー・デフォルト `/search` 応答は **Phase 3-7 共通で不変**。canonical 仕様は [Phase 7 docs/01 §2](../../../7/study-hybrid-search-gke/docs/architecture/01_仕様と設計.md)。

### 2.2 設計思想 (Phase 2 から継承)

- Port/Adapter 構造 (`app/services/protocols/` Port + `app/services/adapters/` Adapter + `app/services/noop_adapters/` テスト用 fallback)
- 依存方向 `core → ports ← adapters` (services は Port のみ import、adapter import 禁止)
- Composition root = `app/composition_root.py` のみで DI 配線 (handler は `Depends(get_container)`)
- `scripts/ci/layers.py` で Port-Adapter 境界を AST 検査 (`make check-layers`)

### 2.3 実行基盤

- uv workspace (Python 3.12)
- Docker Compose 1 stack (postgres + pgvector / meilisearch / redis / search-api)
- `Cloud / Vertex / GKE / KServe / KFP / Composer は持ち込まない` (Phase 4 / 7 の責務)

### 2.4 API

- `/search` — 自由文クエリ + 構造化フィルタ → 物件ランキング上位 N 件 (lexical + semantic + RRF + rerank の 3 段)
- `/feedback` — 行動ログ受付 (PostgreSQL `feedback_events` に書き込み)
- `/health` — liveness

### 2.5 教材対象外

Agent Builder / Vizier / Model Garden / Gemini RAG / W&B / Looker Studio / Doppler は全 Phase 禁止 (親 [`README.md` §1](../../../README.md) 教材対象外)。

---

## 3. Phase 7 → Phase 3 引き算マトリクス

Phase 7 (`7/study-hybrid-search-gke/`) を canonical 起点として、4 区分 (廃棄 / 流用 / 差し替え / 新規作成) で整理する。

### 3.1 廃棄 (Phase 3 では作らない)

| 区分 | Phase 7 のパス | 廃棄理由 |
|---|---|---|
| Terraform | `7/study-hybrid-search-gke/infra/terraform/modules/{gke,kserve,vertex,vector_search,composer,messaging,monitoring,slo,streaming,iam,data,meilisearch}/` | GCP 一切持ち込まない |
| K8s manifest | `7/study-hybrid-search-gke/infra/manifests/` 全体 (`kserve/` / `search-api/` / `policies/` / `kustomization.yaml`) | K8s 持ち込まない |
| Cloud SQL / monitoring | `7/study-hybrid-search-gke/infra/run/` / `infra/sql/` の Cloud SQL / monitoring 系 | GCP 一切持ち込まない |
| Composer DAG | `7/study-hybrid-search-gke/pipeline/dags/` (`daily_feature_refresh.py` / `retrain_orchestration.py` / `monitoring_validation.py` / `_common.py` / `_pod.py`) | Composer 本線化は Phase 7 のみ |
| KFP | `7/study-hybrid-search-gke/pipeline/training_job/components/` の Vertex Pipelines submit / KFP `@dsl.pipeline` 部分 (trainer コア = `ml/training/trainer.py` は流用) | KFP 持ち込まない |
| KFP | `7/study-hybrid-search-gke/pipeline/data_job/main.py` の KFP `@dsl.pipeline` / Vertex Vector Search upsert 部分 (主要ロジックは新規実装に置き換え) | 同上 |
| Dataform | `7/study-hybrid-search-gke/pipeline/data_job/dataform/` 全体 | BigQuery feature 生成は Phase 4 |
| Pipeline | `7/study-hybrid-search-gke/pipeline/batch_serving_job/` / `pipeline/workflow/` / `pipeline/evaluation_job/` の Vertex submit 部分 | KFP / Vertex 不要 |
| Adapter | `7/study-hybrid-search-gke/app/services/adapters/kserve_encoder.py` / `kserve_reranker.py` | KServe 不要 (`LocalE5Encoder` / `LocalLightGBMReranker` で置換) |
| Adapter | `7/study-hybrid-search-gke/app/services/adapters/vertex_vector_search_semantic_search.py` | Vertex Vector Search 不要 (`PgVectorSemanticSearch` で置換) |
| Adapter | `7/study-hybrid-search-gke/app/services/adapters/feature_online_store_fetcher.py` | Feature Online Store 不要 (`PostgresFeatureFetcher` で置換) |
| Adapter | `7/study-hybrid-search-gke/app/services/adapters/pubsub_ranking_log_publisher.py` / `pubsub_feedback_recorder.py` | Pub/Sub 不要 (`Postgres*` で置換) |
| Adapter | `7/study-hybrid-search-gke/app/services/adapters/bqml_popularity_scorer.py` | BQML 不要 (Phase 6 論理) |
| Adapter | `7/study-hybrid-search-gke/app/services/adapters/bigquery_candidate_retriever.py` / `bigquery_data_catalog_reader.py` | BigQuery 不要 |
| Adapter | `7/study-hybrid-search-gke/app/services/adapters/internal/kserve_common.py` / `pubsub_diagnostics.py` | KServe / Pub/Sub 由来の internal helper |
| Adapter | `7/study-hybrid-search-gke/app/services/adapters/retrain.py` / `publisher.py` | retrain 軽量経路 / PredictionPublisher は Phase 4 / 6 |
| Service | `7/study-hybrid-search-gke/app/services/data_catalog_service.py` / `model_metrics_service.py` / `retrain_policy.py` | Phase 7 専用 ops 経路 |
| Router | `7/study-hybrid-search-gke/app/api/routers/{model,ops,retrain,ui}_router.py` | retrain / ops / model UI は Phase 4 以降 |
| Schema | `7/study-hybrid-search-gke/app/schemas/{model,ops}.py` | 同上 |
| Static / Templates | `7/study-hybrid-search-gke/app/static/` / `app/templates/` | UI 不要 |
| ML | `7/study-hybrid-search-gke/ml/streaming/` 全体 | Pub/Sub / Dataflow 専用 |
| ML | `7/study-hybrid-search-gke/ml/training/experiments/` の Vertex Experiments / Model Registry 連携 | Vertex 不要 |
| ML | `7/study-hybrid-search-gke/ml/registry/` の GCS upload 経路 (`GcsArtifactUploader`) | GCS 不要 |
| ML | `7/study-hybrid-search-gke/ml/serving/` 全体 (KServe encoder server) | KServe 不要 |
| ML | `7/study-hybrid-search-gke/ml/data/feature_engineering/` の BigQuery 依存部分 (`schema.py` / `ranker_features.py` の `pandas`/`numpy` 純粋部分は流用) | BigQuery 不要 |
| ML | `7/study-hybrid-search-gke/ml/data/loaders/ranker_repository.py` / `embedding_store.py` の BQ I/O | Postgres I/O に差し替え |
| Scripts | `7/study-hybrid-search-gke/scripts/deploy/` 全体 (`api_gke.py` / `kserve_models.py` / `composer_*.py` / `seed_lgbm_model.py` 等) | Cloud Build / GKE / Composer デプロイ系 |
| Scripts | `7/study-hybrid-search-gke/scripts/infra/` (state_recovery / vertex_import) / `scripts/ops/` の GCP / Composer / Vertex 系 / `scripts/bqml/` / `scripts/setup/` | GCP 系 |
| Python 依存 | `google-cloud-aiplatform` / `google-cloud-bigquery` / `google-cloud-bigquery-storage` / `google-cloud-storage` / `google-cloud-logging` / `google-cloud-pubsub` / `google-cloud-run` / `google-auth` / `kfp` / `google-cloud-pipeline-components` / `kserve` 関連 / `db-dtypes` / `pyarrow` | 全削除 |
| Makefile target | `tf-*` / `deploy-all` / `destroy-all` / `run-all*` / `verify-*` / `ops-*` / `composer-*` / `bqml-*` / `kube-creds` / `apply-manifests` / `setup-*` / `sync-dataform-config` / `sync-configmap` | 全削除 |

### 3.2 流用 (Phase 7 から無改変または微修正で持ち込む)

| 区分 | Phase 7 のパス | 持ち込み先 | 改変内容 |
|---|---|---|---|
| Domain | `7/study-hybrid-search-gke/app/domain/{candidate,retrieval,search}.py` | `app/domain/` | 無改変 (SDK 非依存) |
| Schema | `7/study-hybrid-search-gke/app/schemas/search.py` | `app/schemas/search.py` | feedback 用 schema は流用、`model.py` / `ops.py` は廃棄 |
| Schema | `7/study-hybrid-search-gke/app/schemas/__init__.py` | `app/schemas/__init__.py` | search 用のみ re-export |
| Port | `7/study-hybrid-search-gke/app/services/protocols/lexical_search.py` | `app/services/protocols/lexical_search.py` | 無改変 (`MeilisearchLexical` 流用と整合) |
| Port | `7/study-hybrid-search-gke/app/services/protocols/encoder_client.py` | `app/services/protocols/encoder_client.py` | 無改変 |
| Port | `7/study-hybrid-search-gke/app/services/protocols/semantic_search.py` | `app/services/protocols/semantic_search.py` | 無改変 (docstring から Vertex Vector Search 言及を削除) |
| Port | `7/study-hybrid-search-gke/app/services/protocols/reranker_client.py` | `app/services/protocols/reranker_client.py` | 無改変 (`RerankerExplainer` も含む、Phase 4 以降の足し算余地) |
| Port | `7/study-hybrid-search-gke/app/services/protocols/feature_fetcher.py` | `app/services/protocols/feature_fetcher.py` | 無改変 (`FeatureRow` データ型流用) |
| Port | `7/study-hybrid-search-gke/app/services/protocols/ranking_log_publisher.py` | `app/services/protocols/ranking_log_publisher.py` | 無改変 |
| Port | `7/study-hybrid-search-gke/app/services/protocols/feedback_recorder.py` | `app/services/protocols/feedback_recorder.py` | 無改変 |
| Port | `7/study-hybrid-search-gke/app/services/protocols/__init__.py` | `app/services/protocols/__init__.py` | `CandidateRetriever` / `DataCatalogReader` / `PopularityScorer` / `PredictionPublisher` / `RetrainQueries` は除去 |
| Service | `7/study-hybrid-search-gke/app/services/search_service.py` | `app/services/search_service.py` | `popularity_scorer` 引数 / `data_catalog_service` 連携を削除、`feature_fetcher` は optional のまま流用 |
| Service | `7/study-hybrid-search-gke/app/services/ranking.py` | `app/services/ranking.py` | 無改変 (RRF 含むビジネスロジック純粋) |
| Service | `7/study-hybrid-search-gke/app/services/feedback_service.py` | `app/services/feedback_service.py` | 無改変 |
| Noop adapter | `7/study-hybrid-search-gke/app/services/noop_adapters/noop_lexical_search.py` | `app/services/noop_adapters/noop_lexical_search.py` | 無改変 |
| Noop adapter | `7/study-hybrid-search-gke/app/services/noop_adapters/noop_ranking_log_publisher.py` | `app/services/noop_adapters/noop_ranking_log_publisher.py` | 無改変 |
| Noop adapter | `7/study-hybrid-search-gke/app/services/noop_adapters/noop_feedback_recorder.py` | `app/services/noop_adapters/noop_feedback_recorder.py` | 無改変 |
| Router | `7/study-hybrid-search-gke/app/api/routers/{search_router,feedback_router,health_router}.py` | `app/api/routers/{search_router,feedback_router,health_router}.py` | 無改変 |
| Router 周辺 | `7/study-hybrid-search-gke/app/api/dependencies.py` / `mappers/` (search 用) / `middleware/` (logging) | `app/api/{dependencies,mappers,middleware}/` | model / ops 関連の DI 解決を削除 |
| ML | `7/study-hybrid-search-gke/ml/training/trainer.py` (LightGBM LambdaRank コア) | `ml/training/trainer.py` | `GcsArtifactUploader` / `ExperimentTracker` 連携を削除、ローカル `ml/registry/artifacts/{run_id}/` 書き出しに差し替え |
| ML | `7/study-hybrid-search-gke/ml/training/model_builder.py` | `ml/training/model_builder.py` | 無改変 (`split_by_request_id` / `synthetic_ranking_frames`) |
| ML | `7/study-hybrid-search-gke/ml/training/ports/ranker_trainer.py` / `ranker_model.py` | `ml/training/ports/` | 無改変 |
| ML | `7/study-hybrid-search-gke/ml/training/adapters/lightgbm_trainer.py` | `ml/training/adapters/lightgbm_trainer.py` | 無改変 |
| ML | `7/study-hybrid-search-gke/ml/evaluation/metrics/{ranking,label_gain}.py` (NDCG / MAP / Recall@K 純粋関数) | `ml/evaluation/metrics/{ranking,label_gain}.py` | 無改変 |
| ML | `7/study-hybrid-search-gke/ml/data/feature_engineering/{ranker_features,schema}.py` | `ml/data/feature_engineering/{ranker_features,schema}.py` | 純粋 pandas 関数なので無改変、データ source は別 loader で差し替え |
| ML | `7/study-hybrid-search-gke/ml/common/{config,logging,utils}/` | `ml/common/` | `TrainSettings` から GCS / Vertex 設定を削除、`generate_run_id` / `get_logger` は無改変 |
| Tests | `7/study-hybrid-search-gke/tests/unit/{app,ml}/` の domain / service / port 単体 + ranking metric 単体 | `tests/unit/` | KServe / Vertex / BQ / Pub/Sub テストは削除、in-memory fakes ベースのテストのみ流用 |
| Tests | `7/study-hybrid-search-gke/tests/_fakes/` (`in_memory_*` / `stub_*`) | `tests/_fakes/` | `stub_popularity_scorer` / `stub_retrain_queries` / `mock_prediction_publisher` は除外 |
| Tests | `7/study-hybrid-search-gke/tests/conftest.py` | `tests/conftest.py` | KServe / GCS fixture を除去 |
| Scripts CI | `7/study-hybrid-search-gke/scripts/ci/layers.py` | `scripts/ci/layers.py` | `RULES` / `DIRECTORY_RULES` から不要 Port (`popularity_scorer` / `retrain_queries` / `data_catalog_reader` / `candidate_retriever`) を削除、`google.cloud` ban を残置 (誤 import を防止) |
| Scripts CI | `7/study-hybrid-search-gke/tests/unit/arch/test_import_boundaries.py` | `tests/unit/arch/test_import_boundaries.py` | `layers.py` 改訂に追従 |
| Lexical adapter | `7/study-hybrid-search-gke/app/services/adapters/lexical_search.py` (`MeilisearchLexical`) | `app/services/adapters/meilisearch_lexical_search.py` | Identity Token / impersonation 認証を削除し、Docker hostname (`http://meilisearch:7700`) + master key 認証のみにする |

### 3.3 差し替え (Port 流用、Adapter は新規 Local 実装)

| Port | Phase 7 Adapter (廃棄) | Phase 3 Adapter (新規) | データソース |
|---|---|---|---|
| `EncoderClient` | `KServeEncoder` | `LocalE5Encoder` | sentence-transformers `intfloat/multilingual-e5-base` (in-process、CPU) |
| `SemanticSearchPort` | `VertexVectorSearchSemanticSearch` | `PgVectorSemanticSearch` | PostgreSQL + pgvector extension の `<=>` 演算子 (cosine distance) |
| `RerankerClient` | `KServeReranker` | `LocalLightGBMReranker` | `lightgbm.Booster.load_model()` で `ml/registry/artifacts/{run_id}/model.lgb` を直読み |
| `FeatureFetcher` | `FeatureOnlineStoreFetcher` | `PostgresFeatureFetcher` | PostgreSQL `feature_mart_property_features_daily` 相当テーブル直接 SELECT |
| `RankingLogPublisher` | `PubSubRankingLogPublisher` | `PostgresRankingLogPublisher` | PostgreSQL `ranking_log` テーブル INSERT |
| `FeedbackRecorder` | `PubSubFeedbackRecorder` | `PostgresFeedbackRecorder` | PostgreSQL `feedback_events` テーブル INSERT |

`LexicalSearchPort` は Phase 7 の `MeilisearchLexical` を流用 (認証部分のみ差し替え) するため別行扱い (§3.2)。

### 3.4 新規作成 (Phase 3 で初登場、Phase 4 へ足し算する起点)

| ファイル | 役割 |
|---|---|
| `docker-compose.yml` | postgres (with pgvector) / meilisearch / redis / search-api を 1 stack で起動 |
| `definitions/postgres/migrations/0001_init.sql` | DDL: `properties` / `embeddings` (pgvector) / `feature_mart_property_features_daily` / `ranking_log` / `feedback_events`、`CREATE EXTENSION vector` |
| `pipeline/data_job/main.py` | sklearn / 合成不動産データ → PostgreSQL `properties` 投入 + Meilisearch index 構築 + ME5 embedding 計算 → pgvector `embeddings` 投入 (CLI: `python -m pipeline.data_job.main`) |
| `pipeline/training_job/main.py` | PostgreSQL `ranking_log` + `feature_mart_property_features_daily` → LightGBM LambdaRank → ローカル `ml/registry/artifacts/{run_id}/model.lgb` + `metrics.json` (CLI: `python -m pipeline.training_job.main`) |
| `pipeline/evaluation_job/main.py` | NDCG@K 計算 + `metrics.json` 出力 (CLI: `python -m pipeline.evaluation_job.main --run-id <id>`) |
| `Makefile` | `build` / `seed` / `train` / `serve` / `test` / `check` / `check-layers` / `verify-pipeline` / `down` / `clean` / `help` |
| `mk/base.mk` (親リポ共通) | help target は親 `mk/base.mk` を include (Phase 7 と同パターン) |
| `pyproject.toml` | uv workspace、Phase 7 から GCP 依存を抜いて軽量化 (fastapi / uvicorn / pydantic-settings[yaml] / psycopg[binary] / sqlalchemy / pgvector / lightgbm / sentence-transformers / meilisearch / redis / numpy / pandas + dev: pytest / pytest-cov / ruff / mypy / httpx) |
| `env/config/setting.yaml` | `meili_url` / `meili_api_key_env` / `postgres_dsn_env` / `redis_url` / `e5_model_name` / `top_k_default` / `model_artifacts_root` / `enable_search` 等 (非クレデンシャル) |
| `env/secret/credential.yaml.example` | `MEILI_MASTER_KEY` / `POSTGRES_PASSWORD` のサンプル (実体は `.gitignore`) |
| `app/composition_root.py` | Phase 3 版。`InfraBuilder` (postgres / redis / meilisearch client 構築) / `SearchBuilder` (lexical / semantic / reranker / feature_fetcher / publishers 構築) / `MlBuilder` 不要 (training は pipeline 側で完結)、`ContainerBuilder.build()` で `Container` を返す |
| `app/main.py` | FastAPI app + lifespan で `ContainerBuilder.build()` を呼び `app.state.container` に保持、`/search` `/feedback` `/health` の 3 router のみ register |
| `app/settings.py` | `pydantic-settings` で `env/config/setting.yaml` + `env/secret/credential.yaml` をマージ。`ApiSettings` から GCP 設定を全削除し Phase 3 用 setting に絞る |
| `app/services/adapters/meilisearch_lexical_search.py` | Phase 7 の `MeilisearchLexical` を Identity Token なし版に修正 (master key 認証 + Docker hostname) |
| `app/services/adapters/local_e5_encoder.py` | sentence-transformers in-process。`__init__` で model load、`embed(text, kind)` で `query: ` / `passage: ` prefix を付与して `model.encode()` |
| `app/services/adapters/pgvector_semantic_search.py` | psycopg で `SELECT property_id, ... FROM embeddings ORDER BY embedding <=> %s LIMIT %s` 実行、フィルタは `JOIN properties` で適用 |
| `app/services/adapters/local_lightgbm_reranker.py` | `lightgbm.Booster.load_model(model_path)`、`predict(instances)` で `booster.predict(np.asarray(instances))` |
| `app/services/adapters/postgres_feature_fetcher.py` | psycopg で `SELECT * FROM feature_mart_property_features_daily WHERE property_id = ANY(%s)` |
| `app/services/adapters/postgres_ranking_log_publisher.py` | psycopg INSERT (`request_id` / `candidates JSONB` / `final_ranks` / `scores` / `model_path` / `created_at`) |
| `app/services/adapters/postgres_feedback_recorder.py` | psycopg INSERT (`request_id` / `event_type` / `property_id` / `payload JSONB` / `created_at`) |
| `app/services/noop_adapters/noop_semantic_search.py` | テスト用 in-memory fallback (Phase 7 になかった) |
| `app/services/noop_adapters/noop_encoder_client.py` | テスト用 deterministic stub (Phase 7 の `stub_encoder_client.py` を本番側 noop に昇格) |
| `app/services/noop_adapters/noop_reranker_client.py` | テスト用 in-memory fallback |
| `app/services/noop_adapters/noop_feature_fetcher.py` | テスト用 in-memory fallback |
| `infra/run/services/api/Dockerfile` | search-api イメージ (uv → Python 3.12 → 依存 install → uvicorn 起動) |
| `infra/run/jobs/pipeline/Dockerfile` | seed / trainer / evaluation 共用イメージ (uv → 同上 → CLI entrypoint 切替) |
| `tests/unit/app/test_local_adapters.py` | Local adapter 6 軸の単体テスト (Phase 3 新規。Phase 7 にあった adapter test は GCP 依存ばかりで流用不可) |
| `tests/integration/test_pipeline_e2e.py` | seed → train → serve → /search → /feedback の Docker Compose 上 E2E (`make verify-pipeline` から呼ばれる) |
| `docs/03_実装カタログ.md` | Wave 4 で再生成 (現状 1 行で空相当) — dir / file / DB / API / adapter の 1:1 対応スナップショット |
| `docs/04_運用.md` | Wave 4 で再生成 — `make` ターゲット早見 / Docker Compose 操作 / トラブルシューティング |
| `docs/TASKS.md` | Wave 4 で再生成 — current sprint 抜粋 (Wave 1-4 の進捗を反映) |

---

## 4. Wave 構成

実装段階を Wave 1〜4 に分け、各 Wave の完了条件を明示する。Wave 内タスクは番号付きチェックリストで、上から順に着手すれば成立する順序。

### Wave 1: スキャフォールディング

**目的**: Docker Compose / uv workspace / ディレクトリ骨格を立て、`make build` が通り `postgres` / `meilisearch` / `redis` がローカルで起動する状態にする。

**完了条件**: `make build && docker compose ps` で 3 service が `healthy`、`make sync` で uv workspace が解決する。

1. [x] `pyproject.toml` を新規作成 (Phase 7 から GCP 依存を抜いた最小集合)。`hatchling` で `app` / `ml` / `pipeline` をパッケージ化
2. [x] `Makefile` を新規作成 (`build` / `seed` / `train` / `serve` / `test` / `check` / `check-layers` / `verify-pipeline` / `down` / `clean` / `help` target)。親 `mk/base.mk` の include 方針は Phase 7 と揃える
3. [x] `docker-compose.yml` を新規作成 (postgres + pgvector / meilisearch / redis / search-api 4 service、Phase 2 の `docker-compose.yml` をベースに `meilisearch` / `redis` を追加し postgres image を `pgvector/pgvector:pg16` に置換)
4. [x] `definitions/postgres/migrations/0001_init.sql` を新規作成 (`CREATE EXTENSION vector` / `properties` / `embeddings` / `feature_mart_property_features_daily` / `ranking_log` / `feedback_events`)
5. [x] `env/config/setting.yaml` / `env/secret/credential.yaml.example` を新規作成 (pydantic-settings で読める非クレデンシャル / クレデンシャル分離)
6. [x] `infra/run/services/api/Dockerfile` / `infra/run/jobs/pipeline/Dockerfile` を新規作成 (uv 経由で venv 構築 → entrypoint 切替)
7. [x] ディレクトリ骨格を `mkdir`: `app/{api,domain,schemas,services,settings}/` / `app/services/{protocols,adapters,noop_adapters}/` / `ml/{common,data,training,evaluation,registry,serving}/` / `pipeline/{data_job,training_job,evaluation_job}/` / `tests/{unit,integration,_fakes}/` / `scripts/ci/`
8. [x] `docker-compose.yml` で `postgres` / `meilisearch` / `redis` のみ先に立ち上げ、`make build` で healthcheck PASS を確認

### Wave 2: Port + Local Adapter + FastAPI app

**目的**: Phase 7 から Port / Domain / Schema / Service を流用し、Phase 3 用 Local adapter 6 軸を新規実装し、`make serve` で `/search` `/feedback` `/health` が応答する状態にする。

**完了条件**: `make serve` 後 `curl /health` が 200、`/search` が retriever / encoder 経由で空でない rerank 結果を返す (この時点では `/search` の rerank score は固定値 fallback でも可、本物の LightGBM 学習済モデルは Wave 3 で投入)。`make check-layers` で AST 境界違反ゼロ。

1. [x] Phase 7 から Domain copy: `7/study-hybrid-search-gke/app/domain/{candidate,retrieval,search}.py` → `app/domain/`
2. [x] Phase 7 から Schema copy: `7/study-hybrid-search-gke/app/schemas/search.py` + `__init__.py` → `app/schemas/` (`model.py` / `ops.py` は持ち込まない)
3. [x] Phase 7 から Port copy: `7/study-hybrid-search-gke/app/services/protocols/` の `lexical_search` / `encoder_client` / `semantic_search` / `reranker_client` / `feature_fetcher` / `ranking_log_publisher` / `feedback_recorder` のみ → `app/services/protocols/`。`__init__.py` の re-export を Phase 3 で必要な Port のみに絞る
4. [x] Phase 7 から Service copy: `app/services/{search_service,ranking,feedback_service}.py` → `app/services/`。`search_service.py` から `popularity_scorer` / `data_catalog_service` 連携を削除 (`feature_fetcher` は optional のまま流用)
5. [x] Phase 7 から Noop adapter copy: `noop_lexical_search` / `noop_ranking_log_publisher` / `noop_feedback_recorder` → `app/services/noop_adapters/`
6. [x] Phase 3 新規 Noop adapter: `noop_semantic_search` / `noop_encoder_client` / `noop_reranker_client` / `noop_feature_fetcher` を `app/services/noop_adapters/` に追加 (テストの fakes と本番 fallback を兼ねる)
7. [x] Phase 7 から Lexical adapter 流用 + 改修: `MeilisearchLexical` を `app/services/adapters/meilisearch_lexical_search.py` にコピーし、`google.auth` / `id_token` / `impersonated_credentials` 由来のコードを全削除、master key + Docker hostname (`http://meilisearch:7700`) のみで動くように単純化
8. [x] Phase 3 新規 adapter `app/services/adapters/local_e5_encoder.py`: sentence-transformers `intfloat/multilingual-e5-base` を `__init__` で load (env で `e5_model_name` 指定可)、`embed(text, kind)` で `query: ` / `passage: ` prefix を付与し L2 正規化
9. [x] Phase 3 新規 adapter `app/services/adapters/pgvector_semantic_search.py`: psycopg で `SELECT p.property_id, e.embedding <=> %s::vector AS distance FROM embeddings e JOIN properties p ON e.property_id = p.property_id WHERE <filters> ORDER BY distance LIMIT %s` を実行
10. [x] Phase 3 新規 adapter `app/services/adapters/local_lightgbm_reranker.py`: `__init__` で `lightgbm.Booster(model_file=model_path)`、`predict(instances)` で `numpy.asarray` → `booster.predict()`
11. [x] Phase 3 新規 adapter `app/services/adapters/postgres_feature_fetcher.py`: psycopg で `SELECT * FROM feature_mart_property_features_daily WHERE property_id = ANY(%s)`
12. [x] Phase 3 新規 adapter `app/services/adapters/postgres_ranking_log_publisher.py`: psycopg INSERT (Pub/Sub publish の代替)
13. [x] Phase 3 新規 adapter `app/services/adapters/postgres_feedback_recorder.py`: psycopg INSERT
14. [x] `app/settings.py` を新規作成 (`ApiSettings` Phase 3 版、`pydantic-settings` で `env/config/setting.yaml` + `env/secret/credential.yaml` を merge)
15. [x] `app/composition_root.py` を新規作成 (Phase 7 を簡略化、`InfraBuilder` / `SearchBuilder` のみ、`MlBuilder` は不要)。`Container` フィールドから retrain / popularity / data_catalog / model_metrics を削除
16. [x] `app/main.py` を新規作成 (FastAPI lifespan で `ContainerBuilder.build()`、3 router register)
17. [x] Phase 7 から Router copy: `app/api/routers/{search,feedback,health}_router.py` + `app/api/dependencies.py` + `app/api/mappers/` (search 用) + `app/api/middleware/` (logging) → `app/api/`
18. [x] Phase 7 から `scripts/ci/layers.py` をコピーし、`RULES` / `DIRECTORY_RULES` から不要 Port (`popularity_scorer` / `retrain_queries` / `data_catalog_reader` / `candidate_retriever`) を削除。`google.cloud` ban を残置 (誤 import 防止)
19. [x] `tests/conftest.py` + `tests/_fakes/in_memory_*.py` (Phase 7 から `in_memory_lexical_search` / `in_memory_semantic_search` / `in_memory_feature_fetcher` / `in_memory_feedback_recorder` / `in_memory_ranking_log_publisher` / `stub_encoder_client` / `mock_reranker_client` のみ流用)
20. [x] Phase 7 から流用可能な domain / service / port 単体テストのみ `tests/unit/` にコピー (`test_search_service` / `test_ranking_service` / `test_feedback_service` / `test_search_mapper` / `test_health_handler` / `test_feedback_handler_http` / `test_search_handler_http`)。GCP / KServe / BQ test は持ち込まない
21. [x] `tests/unit/app/test_local_adapters.py` を新規作成 (Local adapter 6 軸の単体テスト、Meilisearch / Postgres / Redis は testcontainers または `pytest.mark.integration` で別 layer)
22. [x] `make serve` で `/health` 200、`/search` で空クエリでも 503 ではなく fallback 経路で 200 を返すこと、`make check-layers` で違反ゼロを確認

### Wave 3: ML パイプライン (data_job / training_job / evaluation_job)

**目的**: ローカルで合成不動産データを seed → LightGBM LambdaRank で学習 → NDCG@10 を評価する閉ループを作る。

**完了条件**: `make seed && make train && make serve` で `curl /search` が **学習済 LightGBM model.lgb による rerank score (非ゼロ、3 系統 lexical_rank / semantic_rank / final_rank all non-zero)** を返す。`make verify-pipeline` で integration test PASS。

1. [x] `pipeline/data_job/main.py` を新規作成: 合成不動産データ生成 (Phase 7 の `ml/data/datasets/seed_minimal_properties.csv` を ベースに sklearn `make_classification` 等で 1000 件程度に拡張) → PostgreSQL `properties` 投入 → Meilisearch index 作成 + ドキュメント投入 → ME5 で embedding 計算 → pgvector `embeddings` 投入 → `feature_mart_property_features_daily` への特徴量集計
2. [x] `pipeline/data_job/components/` に分割: `synthetic_properties.py` / `meili_indexer.py` / `embed_writer.py` / `feature_aggregator.py`。各 component は psycopg / meilisearch SDK / sentence-transformers のみに依存
3. [x] Phase 7 から ML コア copy: `ml/training/{trainer,model_builder}.py` + `ml/training/{ports,adapters}/` + `ml/data/feature_engineering/{ranker_features,schema}.py` + `ml/evaluation/metrics/{ranking,label_gain}.py` + `ml/common/` (`config` / `logging` / `utils`) → 対応先
4. [x] `ml/common/config/base.py` の `TrainSettings` から GCS / Vertex / project_id 系を削除し、`postgres_dsn` / `model_artifacts_root` のみに絞る
5. [x] `ml/registry/artifact_store.py` を新規作成: `LocalArtifactStore` のみ (Phase 7 の `GcsArtifactUploader` Port 互換でローカル `ml/registry/artifacts/{run_id}/model.lgb` 書き出し)
6. [x] `ml/data/loaders/ranker_repository.py` を新規作成 (Phase 7 の BQ 版を psycopg 版に置換): `RankerTrainingRepository.fetch_training_window()` が PostgreSQL `ranking_log` JOIN `feature_mart_property_features_daily` を SELECT し pandas DataFrame で返す
7. [x] `ml/training/trainer.py` の `run` / `main` を改修: `GcsArtifactUploader` を `LocalArtifactStore` に差し替え、`ExperimentTracker` を `NullExperimentTracker` 固定 (Phase 7 の Vertex Experiments 経路は撤去)、合成 ranking_log がまだ無いケースは `synthetic_ranking_frames()` で fallback
8. [x] `pipeline/training_job/main.py` を新規作成: CLI で `python -m pipeline.training_job.main --run-id <id>` を受け、`ml.training.trainer.run()` を呼び `ml/registry/artifacts/{run_id}/{model.lgb,metrics.json,feature_importance.csv}` を出力。Phase 7 の KFP `@dsl.pipeline` / Vertex submit 部分は持ち込まない
9. [x] `pipeline/evaluation_job/main.py` を新規作成: `ml/registry/artifacts/{run_id}/model.lgb` を読み込み、PostgreSQL `ranking_log` の hold-out split で NDCG@10 / MAP / Recall@20 を再評価し `ml/registry/artifacts/{run_id}/metrics.json` を上書き
10. [x] `Makefile` に `seed` / `train` / `evaluate` target を追加 (`docker compose run --rm seed` / `trainer` / `evaluator`)。`docker-compose.yml` の `seed` / `trainer` service を Phase 2 と同パターンで配線
11. [x] `tests/integration/test_pipeline_e2e.py` を新規作成: seed → train → serve を 1 test case で回し `/search` が rerank score 非ゼロを返すこと、`/feedback` が 200 を返すこと、`ranking_log` テーブルに row が増えることを検証
12. [x] `Makefile` に `verify-pipeline` target を追加 (`pytest tests/integration` を Docker Compose 立ち上げ済前提で実行)

### Wave 4: 検証 + docs 整備

**目的**: 検証ゲート ( `make check` / `make verify-pipeline` / NDCG@10 ベースライン ) を全通過し、docs を最新化する。教育資料 (slides / 図解) を Phase 3 再構築後の状態に揃える。

**完了条件**: `make check` (ruff + ruff format --check + mypy + pytest) PASS、`make check-layers` PASS、`make verify-pipeline` PASS、NDCG@10 ≥ ベースライン (合成データなので絶対値は緩く、`metrics.json` に値が入って `> 0` であること)。`docs/03_実装カタログ.md` / `docs/04_運用.md` / `docs/TASKS.md` が Wave 1-3 成果物と整合。

1. [x] `make check` 通過: ruff / mypy strict / pytest を全クリア。型エラー残置禁止
2. [x] `make check-layers` で `scripts/ci/layers.py` の RULES に追加忘れがないことを再確認 (新規 Port を漏らしていないか)
3. [x] `make verify-pipeline` 通過: integration test で seed → train → serve → `/search` (lexical_rank / semantic_rank / final_rank all non-zero) → `/feedback` 200 を確認
4. [x] NDCG@10 ベースライン記録: `ml/registry/artifacts/{run_id}/metrics.json` に NDCG@10 / MAP / Recall@20 が出ていることを確認、`docs/04_運用.md` に「合成データ NDCG@10 のベースライン値」を記録 (具体値は実測で埋める)
5. [x] `docs/03_実装カタログ.md` を再生成: dir / file / DB table / API endpoint / adapter / Port の 1:1 対応スナップショット (Phase 7 `docs/architecture/03_実装カタログ.md` をテンプレートに、GCP リソース行を削除)
6. [x] `docs/04_運用.md` を再生成: `make` ターゲット早見表 / Docker Compose 起動・停止 / pgvector のメンテ (`REINDEX`) / Meilisearch のインデックス再作成 / トラブルシューティング (sentence-transformers の初回 download が遅い等)
7. [x] `docs/TASKS.md` を再生成: 「現在の目的 (Phase 3 再構築完了)」「実装済 (Wave 1-4 チェックリスト)」「次 Phase (Phase 4 への足し算)」を current sprint として記述
8. [x] `docs/01_仕様と設計.md` の §6 関連ドキュメント を本ファイル (`02_移行ロードマップ.md`) と整合する形に微修正
9. [x] 教育資料 (`docs/教育資料/01_スライド.md` / `03_デモシナリオ.md` / `05_図解.md`) を Wave 1-4 の成果物に合わせて差分更新 (現状値が「実装データ削除済」前提なので、再構築完了に置き換える)
10. [x] CLAUDE.md (phase 配下) の「Phase 3 の対象」「実装ルール」を再構築後の事実に合わせて修正

#### Wave 4 追加: Web UI (Phase 7 流用簡素化) 🆕

GOAL 達成検証の最終局面で、Phase 7 の Pico-admin dark theme UI を Phase 3 に流用 (search-user 1 page のみ):

- [x] `app/static/css/` に Phase 7 から 5 CSS 流用 (`pico-admin-{pico,theme,layout,components}.css` + `custom.css`、合計 ~48KB)
- [x] `app/static/js/search_ui.js` を Phase 7 から流用 (`HybridSearchUI.init({rootId, mode})` API、316 行)
- [x] `app/templates/` に Phase 7 から 5 ファイル流用 (`base.html` + `index.html` + `_search_form.html` + `_search_results.html` + `_feedback_panel.html`)
- [x] `app/templates/base.html` を Phase 3 用に簡素化: sidebar から Developer 詳細 (model-metrics / data / ops) を削除、segmented から「開発」「Ops」を削除、brand を `Phase 3 · Local Docker Compose` に変更
- [x] `app/api/routers/ui_router.py` を Phase 7 から流用 + 簡素化 (`build_ui_router(app_root)` pattern、`/ui/dev` 系を削除し `search-user` 1 page のみ)
- [x] `app/main.py` で `app.mount("/static", StaticFiles(...))` + `app.include_router(build_ui_router(app_root=...))`
- [x] 検証: `GET /` → 308 → `/ui/`、`GET /ui/` 200 (5201B Jinja2 render)、`GET /static/css/custom.css` 200 (8133B)、`GET /static/js/search_ui.js` 200 (11341B)

詳細仕様は [`01_仕様と設計.md` §4 Web UI 仕様](01_仕様と設計.md)。

### Wave 5: Redis 2 役割化 (SYN-1 同義語辞書 + SYN-2 レスポンスキャッシュ) 🆕

**追加日**: 2026-05-05 (Wave 4 GOAL 達成後の追加 sprint)

**動機**: Wave 4 完了時点では Redis container は起動するだけで実コードから一切利用されておらず、
Phase 7 仕様書 §2.2.1 の「Redis 同義語辞書 (query expansion)」も「単純なキャッシュ」も実装されてい
なかった。User からのクライアント罰金レベル指摘を受け、Redis を **「単純なキャッシュ」ではない**
2 役割の本格利用に昇格させる sprint を追加実施。

**完了条件**: `make check` (ruff + mypy strict + pytest) PASS、Redis に同義語辞書投入済、
`/search` の同条件再呼び出しで encoder/retriever/reranker/publisher が skip されること。

#### Wave 5-A: SYN-1 同義語辞書 (ranking 精度改善)

- [x] `app/services/protocols/synonym_expander.py` — `SynonymExpanderPort` (Phase 7 SYN-1 と同型 Port)
- [x] `app/services/adapters/redis_synonym_expander.py` — `syn:<token>` SET を SMEMBERS して expansion (1 token につき最大 8 synonyms)。失敗時は元クエリ返却で graceful degrade
- [x] `app/services/noop_adapters/noop_synonym_expander.py` — 素通し fallback
- [x] `app/services/search_service.py` — encoder は元クエリ、retriever (lexical lane) は expanded query という非対称配線
- [x] `definitions/synonyms/real_estate_ja.yaml` — 不動産ドメイン約 30 トークン (物件タイプ / 間取り / 駅距離 / 設備 / 環境)
- [x] `pipeline/data_job/components/sync_synonyms.py` + `make seed-synonyms` + docker compose `seed-synonyms` サービス
- [x] `make seed` を `data_job + sync_synonyms` の 2 段に拡張

#### Wave 5-B: SYN-2 `/search` レスポンスキャッシュ (UX/latency 改善)

- [x] `app/services/protocols/search_cache.py` — `SearchCachePort` (Phase 4+ で Cloud Memorystore に差し替え可)
- [x] `app/services/adapters/redis_search_cache.py` — `search:v1:<sha256(query|filters|top_k|explain)>` で SETEX。TTL=60s。`request_id` / `ranked` はキャッシュに含めない (HIT 時 live request_id をスタンプ、ranking_log は MISS で済 → 重複 publish 回避)
- [x] `app/services/noop_adapters/noop_search_cache.py` — 全 MISS fallback
- [x] `app/services/search_service.py` — エントリで cache.get → HIT で encoder/retriever/reranker/publisher 全 skip、MISS で write-through
- [x] `app/composition_root.py` — Redis 1 接続を SYN-1 / SYN-2 両 adapter で共有 (key prefix `syn:` / `search:` で完全分離)
- [x] `app/settings.py` — `synonym_backend` / `search_cache_backend` / `search_cache_ttl_seconds` 等を追加
- [x] `docker-compose.yml` の search-api に `SYNONYM_BACKEND=redis` / `SEARCH_CACHE_BACKEND=redis` / `SEARCH_CACHE_TTL_SECONDS=60` 注入

#### Wave 5-C: 検証 + docs

- [x] `tests/unit/app/test_synonym_expander.py` (7 件) + `test_search_cache.py` (11 件) + `test_search_service_smoke.py` SYN-1/SYN-2 統合 3 件 ⇒ pytest 24 passed
- [x] `scripts/ci/layers.py` の RULES に `redis` を追加 (Port / Service レイヤから redis import 禁止、adapter 経由のみ可)
- [x] `pyproject.toml` に `pyyaml>=6.0` (seed CLI) + dev `types-pyyaml` を追加
- [x] `docs/01_仕様と設計.md` §1 中核 5 要素表 + §5 データフロー: Redis 行を 2 役割化、cache hit 短絡を §5 step 0 + step 8 として明記
- [x] `docs/03_実装カタログ.md`: Port × Adapter 表に SYN-1 / SYN-2 行追加、noop_adapters 数を 5 本に更新
- [x] `docs/04_運用.md`: §6.11 (辞書更新時の cache 無効化) / §6.12 (cache OFF 切り替え手順) を新設

**実測**: `make check` exit 0、`make check-layers` 違反 0、`pytest tests/unit -q` 24 passed in 0.58s。
NDCG@10 は SYN-1 効果を可視化するために seed の物件 description 多様化が必要だが、
合成 1000 件では辞書のヒット率が低く誤差レベル。本番ログでの効果検証は Phase 4+ 課題。

---

## 5. 検証方法

Phase 3 の **「完了」判定基準**。本セクションを満たさない限り「Phase 3 再構築完了」と書かない。

### 5.1 ローカル E2E

```bash
make build           # docker compose build
make seed            # synthetic properties → PostgreSQL + Meilisearch + pgvector
make train           # LightGBM LambdaRank → ml/registry/artifacts/{run_id}/model.lgb
make serve           # uvicorn 起動 (background)
curl -s -X POST http://localhost:8000/search \
  -H 'Content-Type: application/json' \
  -d '{"query":"駅近 2LDK","filters":{"city":"Tokyo","price_lte":150000},"top_k":10}'
```

**合格条件**: response の各 item に `lexical_rank` / `semantic_rank` / `final_rank` が **all non-zero** で入る (3 系統が動いている証拠)。少なくとも 1 件は rerank の `score` が `0.0` 固定 fallback でない (LightGBM が動いている証拠)。

### 5.2 ユニット / Lint / 型

```bash
make check           # ruff + ruff format --check + mypy strict + pytest
make check-layers    # AST 境界検査 (Port が adapter / google.cloud / lightgbm を import していないこと)
```

**合格条件**: exit 0、警告ゼロ。`google.cloud` import が混入していたら即 fail (`scripts/ci/layers.py` で検出)。

### 5.3 Integration

```bash
make verify-pipeline   # tests/integration/test_pipeline_e2e.py
```

**合格条件**: seed → train → serve の 1 cycle で `/search` が rerank 結果を返し、`ranking_log` テーブルに row が 1 以上挿入される。`/feedback` で 200。

### 5.4 NDCG@10 ベースライン

`ml/registry/artifacts/{run_id}/metrics.json` に NDCG@10 / MAP / Recall@20 がすべて `> 0` で記録されている。合成データなので絶対値は問わないが、**rerank 経路がモデル予測を反映している** ことを示す指標として `ndcg@10 > ndcg@10_baseline_random` (ランダムランキングの NDCG より高い) を満たすこと。

### 5.5 🎯 GOAL 達成実測値 (2026-05-05)

| 検証項目 | 結果 | 詳細 |
|---|---|---|
| 1. `make build` で 3 service `healthy` | ✅ | postgres / meilisearch / redis 全 healthy |
| 2. `curl /search` で 3 系統 all non-zero、rerank score が学習済モデル由来 | ✅ | lex=5,2,1,7 / sem=12,23,4,6 / final=1,2,3,4 / score=0.099,0.099,0.016,0.016 |
| 3. `make check` (ruff + ruff format --check + mypy strict + pytest) PASS | ✅ | mypy strict 81 source files PASS、pytest 1 passed |
| 4. `make check-layers` 違反ゼロ | ✅ | 10 ルール、`google.cloud` / `kserve` / `kfp` 違反 0 |
| 5. `make verify-pipeline` PASS | ✅ | **5 passed in 3.37s** (/health / /readyz / /search 3 系統 / /feedback / ranking_log 永続化) |
| 6. NDCG@10 baseline 記録 | ✅ | **NDCG@10=0.9822 / MAP=0.9967 / Recall@20=1.0** (random ベースライン超え) |
| (補) Web UI 起動 | ✅ | `GET /` 308 → `/ui/` 200 (Pico-admin dark theme) |
| (補) `ranking_log` / `feedback_events` 永続化 | ✅ | 5 rows / 1 row 投入確認 (3 系統 all non-zero データ) |

run_id: `20260505T042413Z-1aab0e91`
詳細レポート: [`05_精度評価.md`](05_精度評価.md) (実測 metrics + 評価指標と LLM 評価の比較補論)

---

## 6. リスクと回避策

| リスク | 対象 | 影響 | 回避策 |
|---|---|---|---|
| sentence-transformers の初回 download (~500MB) でビルドが遅延 | `LocalE5Encoder` | `make seed` / `make serve` 初回起動が数分かかる | Dockerfile で `RUN python -c "from sentence_transformers import SentenceTransformer; SentenceTransformer('intfloat/multilingual-e5-base')"` を build 時に実行し model を image に焼き込む |
| sentence-transformers の CPU メモリ消費 (~2GB) | search-api コンテナ | 8GB 以下のローカル環境で OOM | `e5_model_name` を default `intfloat/multilingual-e5-small` (~470MB / 384 dim) に変更可能にする (setting.yaml で切替)、ただし pgvector の `vector(N)` 列の N も合わせて変更が必要なため migration を分岐 |
| pgvector の HNSW index が seed 量で過剰 | `embeddings` テーブル | seed 1000 件では index なしの seq scan のほうが速い場合がある | Wave 1 では `IVFFLAT` / `HNSW` を貼らず seq scan、Wave 4 で `CREATE INDEX ON embeddings USING hnsw (embedding vector_cosine_ops)` を貼って性能差を計測 |
| Phase 4 への足し算性破壊 | Port 定義 | Phase 4 で `MeilisearchLexical` を Cloud Run 認証付に差し替えるとき Port シグネチャが変わると全 Phase に波及 | Port (`LexicalSearchPort` / `EncoderClient` / `SemanticSearchPort` / `RerankerClient` / `FeatureFetcher` / `RankingLogPublisher` / `FeedbackRecorder`) を Phase 7 と完全同型で持ち込む。Phase 4 での差し替えは adapter のみ |
| `LightGBM` model.lgb format の互換 | `LocalLightGBMReranker` | Phase 4 / 7 で別バージョンの lightgbm に切り替えると Booster が読めない | `pyproject.toml` で `lightgbm>=4.5,<5` を Phase 3-7 共通で固定、CLAUDE.md にこの制約を記載 |
| Meilisearch master key の漏洩 | `env/secret/credential.yaml` | git commit に master key が混入 | `.gitignore` に `env/secret/credential.yaml` を追加、`env/secret/credential.yaml.example` のみ commit、Wave 1 で確認 |
| Docker Compose の startup order race | `seed` / `trainer` service | postgres が ready 前に seed が走る | Phase 2 同パターンで `depends_on.condition: service_healthy`、`pg_isready` healthcheck を必ず付ける |

### 6.1 🎯 GOAL 達成検証で明らかになった追加教訓 (2026-05-05)

| 教訓 | 経緯 | 解決済 fix (実装に反映) |
|---|---|---|
| host port 5432 衝突 | 別プロジェクト (`study-ml-app-pipeline-postgres` 等) と衝突して `make up` / `make seed` の docker compose recreate で fail | docker-compose.yml で **`5433:5432`** にずらし。container 間通信は名前解決 `postgres:5432` のままなので影響無 |
| `uv sync` で project install 失敗 | Dockerfile が `COPY pyproject.toml` 直後に `uv sync` を呼ぶと `app/ml/pipeline` が無いため hatchling build が fail | Dockerfile を `uv sync --no-dev --no-install-project` に変更 (依存だけ install、project 自体は後で `COPY app/ ml/ pipeline/` + `PYTHONPATH=/app` で読む) |
| container 内 venv の Python が `python -m ...` で見えない | Dockerfile の `CMD ["python", "-m", ...]` が system Python を呼んで venv 依存 (meilisearch / lightgbm 等) が見つからない | Dockerfile に **`ENV PATH="/app/.venv/bin:${PATH}"`** を追加 (uv が作る venv を default Python に) |
| docker container 名衝突 | `docker compose up --force-recreate` の途中で古い container が残ると重複名で fail | `docker rm -f phase3-search-api` で強制削除してから `docker compose up -d --no-deps search-api` |
| postgres volume 古くて DB 不在 | `docker compose down` (without volumes) → 再起動で旧 volume が再利用され、initdb が走らないため `hybrid_search` DB が不在 | `docker compose down --volumes` で volume ごと削除 → 再 `make up` で `0001_init.sql` が走り 5 テーブル + `vector` extension が初期化される |
| ME5 model 焼き込みで image size 肥大 | Dockerfile の `RUN python -c "...SentenceTransformer..."` で model (~470MB) を image に焼き込むと cache miss で再 build 高コスト | model 焼き込み撤去 → `hf_cache` volume (docker-compose.yml で定義済) 経由で初回起動時 download、2 回目以降は cache hit で数秒 |
| mypy strict での型エラー | Phase 7 流用の `ml/training/__init__.py` が古いシンボル名 (`RankTrainResult` 等) を re-export、`health_router.py` が削除済 `Container.observability` を参照、`postgres_ranking_log_publisher.py` の `float \| None` narrowing 不足、不要な `# type: ignore` 残置 | 各箇所修正済、`pyproject.toml` に `pandas-stubs` 追加 |
| `evaluate()` の signature mismatch | trainer.py で `evaluate(..., ks=(10, 20))` と呼んだが、Phase 7 実装は `(..., k_ndcg=10, k_recall=20)` | trainer.py / evaluation_job/main.py を `k_ndcg=10, k_recall=20` に修正 |

---

## 7. 成果物 (Phase 4 への足し算起点)

Phase 3 完了時点で **Phase 4 への足し算起点** として下記 deliverable を保証する:

### 7.1 不変 Port セット (Phase 4 で adapter 差し替えだけで済む)

`app/services/protocols/` 配下の **8 Port** (Phase 7 と完全同型、`scripts/ci/layers.py` 10 ルールで AST 境界検査済):

- `LexicalSearchPort` — Phase 4 で `MeilisearchLexical` の認証を Cloud Run Identity Token 付きに差し替え可能
- `EncoderClient` — Phase 4 で `LocalE5Encoder` を Cloud Run encoder サービス呼び出し adapter に差し替え可能
- `SemanticSearchPort` — Phase 4 で `PgVectorSemanticSearch` を `BigQueryVectorSearchSemanticSearch` に差し替え可能
- `RerankerClient` — Phase 4 で `LocalLightGBMReranker` を Cloud Run reranker サービス呼び出し adapter に差し替え可能
- `FeatureFetcher` — Phase 4 で `PostgresFeatureFetcher` を `BigQueryFeatureFetcher` に差し替え可能
- `RankingLogPublisher` — Phase 4 で `PostgresRankingLogPublisher` を `PubSubRankingLogPublisher` に差し替え可能
- `FeedbackRecorder` — Phase 4 で `PostgresFeedbackRecorder` を `PubSubFeedbackRecorder` に差し替え可能
- `CandidateRetriever` — Phase 4 で `LocalCandidateRetriever` を `BigQueryCandidateRetriever` (BQ JOIN 1 本) に差し替え可能

### 7.2 差し替え可能 Adapter セット

`app/services/adapters/` 配下の **8 adapter** (Phase 3 Local 版):

```text
local_candidate_retriever.py    # CandidateRetriever (lex+sem+RRF+Postgres enrich を統合)
meilisearch_lexical_search.py   # LexicalSearchPort (Phase 7 流用 + 認証簡素化)
local_e5_encoder.py             # EncoderClient (sentence-transformers in-process)
pgvector_semantic_search.py     # SemanticSearchPort (pgvector cosine)
local_lightgbm_reranker.py      # RerankerClient (Booster.predict in-process)
postgres_feature_fetcher.py     # FeatureFetcher (feature_mart 直接 SELECT)
postgres_ranking_log_publisher.py  # RankingLogPublisher (Postgres INSERT)
postgres_feedback_recorder.py      # FeedbackRecorder (Postgres INSERT)
```

すべて Port のシグネチャを満たし、Phase 4 で同 Port の Cloud SDK 版 adapter を別ファイルとして追加する形で足し算可能。

### 7.3 ML パイプライン

- `pipeline/data_job/main.py` — synthetic seed (Phase 4 で BigQuery 連携の loader に差し替え可能)
- `pipeline/training_job/main.py` — LightGBM LambdaRank trainer (Phase 4 で Cloud Run Jobs 経由で起動、Phase 7 で Vertex AI Pipelines に昇格)
- `pipeline/evaluation_job/main.py` — NDCG@10 / MAP / Recall@20 計算 (Phase 4 で BigQuery 経由に差し替え可能)

### 7.4 検証ゲート

- `make check` (ruff + mypy strict + pytest)
- `make check-layers` (AST 境界検査)
- `make verify-pipeline` (integration E2E)
- NDCG@10 ベースライン (`metrics.json`)

すべて Phase 4 が継承する。

---

## 8. 関連ドキュメント

### 8.1 親リポ

- 親 [`README.md` §4 コード化方針](../../../README.md) — Phase 1/2/3/4/7 = コード化、Phase 5/6 = 論理 Phase の改訂方針 (2026-05-03)
- 親 [`README.md` §4.4 Phase 2 → 3 の接続](../../../README.md) — ドメインと Adapter 数の段差説明
- 親 [`README.md` §4.5 Phase 3 → 4 の接続](../../../README.md) — Phase 3 → 4 足し算境界の項目一覧
- 親 [`README.md` §5 非負制約](../../../README.md) — 全 Phase 共通制約 (Meilisearch BM25 + ME5 + RRF + LightGBM LambdaRank の不変)

### 8.2 Phase 7 (canonical)

- [`7/study-hybrid-search-gke/docs/architecture/01_仕様と設計.md` §2](../../../7/study-hybrid-search-gke/docs/architecture/01_仕様と設計.md) — Phase 3-7 共通仕様 (Mermaid 関係図 / シーケンス図 / 中核 5 要素のデフォルト挙動)
- [`7/study-hybrid-search-gke/app/services/protocols/`](../../../7/study-hybrid-search-gke/app/services/protocols/) — Port 定義 (Phase 3 が流用する正本)
- [`7/study-hybrid-search-gke/app/services/search_service.py`](../../../7/study-hybrid-search-gke/app/services/search_service.py) / [`ranking.py`](../../../7/study-hybrid-search-gke/app/services/ranking.py) — RRF + 3 段検索のビジネスロジック
- [`7/study-hybrid-search-gke/ml/training/trainer.py`](../../../7/study-hybrid-search-gke/ml/training/trainer.py) — LightGBM LambdaRank trainer コア
- [`7/study-hybrid-search-gke/scripts/ci/layers.py`](../../../7/study-hybrid-search-gke/scripts/ci/layers.py) — AST 境界検査の正本

### 8.3 Phase 3 配下

- [`docs/01_仕様と設計.md`](01_仕様と設計.md) — Phase 3 固有の差分 (Local 完結 / 不動産ハイブリッド検索ドメイン初登場)
- [`docs/03_実装カタログ.md`](03_実装カタログ.md) — Wave 4 で再生成 (実装スナップショット)
- [`docs/04_運用.md`](04_運用.md) — Wave 4 で再生成 (運用手順)
- [`docs/TASKS.md`](TASKS.md) — Wave 4 で再生成 (current sprint)
- [`IMPLEMENTATION_REMOVED.md`](../IMPLEMENTATION_REMOVED.md) — 旧実装削除の経緯 (`pre-phase3to6-impl-removal` tag で復元可、ただし本計画では復元せず Phase 7 から再構築する)
