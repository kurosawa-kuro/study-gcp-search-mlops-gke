---
marp: true
theme: default
paginate: true
header: "ハイブリッド検索入門"
footer: "study-hybrid-search-local"
style: |
  section {
    font-size: 25px;
    padding: 44px 56px;
    font-family: "Noto Sans JP", "Hiragino Sans", "Yu Gothic", "BIZ UDPGothic", sans-serif;
  }
  h1 { font-size: 42px; }
  h2 { font-size: 34px; }
  code { font-size: 20px; }
  table { font-size: 20px; }
---

# ハイブリッド検索入門

不動産検索を題材に  
**lexical + semantic + rerank** を学ぶ

---

## この Phase の主眼

- 検索を 1 つのモデルではなく、複数段の処理で考える
- `Meilisearch` / `multilingual-e5` / `LightGBM LambdaRank` の役割分担を理解する
- `Redis` の **2 役割** をつかむ — 同義語辞書 (ranking 精度) と `/search` レスポンスキャッシュ (UX/latency)

---

## なぜハイブリッド検索か

| 手法 | 強み | 弱み |
|---|---|---|
| lexical search | 速い、厳密条件に強い | 言い換えに弱い |
| semantic search | 意味の近さに強い | 構造化条件が苦手 |
| rerank | 複数特徴をまとめて最終調整できる | 単独では候補を集めにくい |

---

## Phase 1 との対比 — 回帰 vs ランキング

- Phase 1: 住宅価格を **数値で予測** (RMSE / MAE で誤差を測れる)
- Phase 3: 検索結果を **順序で提示** (NDCG / MAP / Recall@K で品質を測る)
- 目的関数も違う — 二乗誤差最小化 ↔ LambdaRank (NDCG 直接最大化)
- 「正解の作りやすさ」も大きく異なる (後ろのスライドで詳述)

> ML の難しさの種類が違うことを最初に押さえる。

---

## 従来検索とハイブリッド検索の違い

| 観点 | 従来 (lexical のみ) | ハイブリッド検索 |
|---|---|---|
| マッチ方法 | キーワード一致 / BM25 | 一致 + 意味近傍 + 並べ替え |
| 言い換え | 弱い | 拾える |
| 構造化条件 | 強い | 強い (lexical 側で担当) |
| 改善余地 | 同義語辞書 | 特徴量・ログ・モデル |

→ 単一手法では拾えない検索意図を、3 段で補い合う。

---

## 3 段構成

```text
1. Meilisearch で候補取得
2. multilingual-e5 で意味類似度を計算
3. LightGBM LambdaRank で最終順位を決める
```

> この Phase の中核は `LightGBM + multilingual-e5 + Meilisearch`。

---

## Meilisearch を使う理由

- `Elasticsearch` より導入しやすい
- 学習用ローカル構成でも扱いやすい
- 厳密条件の絞り込みを高速に試せる
- BM25 ベースの lexical search を素直に体験しやすい

---

## semantic search の役割

- クエリと物件説明文を embedding に変換する
- 完全一致しない表現も「意味の近さ」で拾う

例:

- `静かな住宅街`
- `閑静なエリア`

> 表記一致ではなく意味近傍で候補を補う。

---

## ベクトル検索とベクトル DB

- 文書とクエリを Embedding でベクトル化 → 近さで検索
- 完全一致しなくても意味が近ければ拾える
- 格納先は Phase ごとに置換可能:
  - **PostgreSQL + pgvector** (Phase 3 採用)
  - BigQuery `VECTOR_SEARCH` (Phase 4)
  - Vertex AI Vector Search (Phase 5+)
  - Elasticsearch / OpenSearch / 専用 DB

> Port/Adapter で切り出してあるため、phase が進んでも置換だけで済む。

---

## ベクトルはどう動くか

1. 物件タイトル + 説明 + 設備を整形
2. `multilingual-e5` でベクトル化 (384 次元)
3. `embeddings` テーブル (pgvector) に保存
4. クエリも同じモデルでベクトル化
5. cosine 距離 (`<=>`) で近傍検索
6. 上位 N 件を **意味候補** として返す

> 物件側ベクトルは `make seed` で事前投入、クエリは毎回オンデマンド計算。

---

## rerank の役割と特徴量

`LightGBM LambdaRank` が見る 10 特徴量:

- 検索系: `me5_score` / `lexical_rank` / `semantic_rank`
- 物件属性: `rent` / `walk_min` / `age_years` / `area_m2`
- 行動系: `ctr` / `fav_rate` / `inquiry_rate`

→ 単一スコアではなく **総合判定** で並び替える。  
ME5 スコアは順位そのものではなく、特徴量の 1 つ。

---

## Redis の 2 役割 (Phase 7 仕様準拠)

Redis は **「単純なキャッシュ」ではなく 2 つの独立した役割** を持つ。同じ container を共有し、key prefix で完全分離。

| 役割 | キー prefix | 効くもの |
|---|---|---|
| **SYN-1 同義語辞書** | `syn:<token>` (SET) | **ランキング精度** (BM25 recall 改善) |
| **SYN-2 レスポンスキャッシュ** | `search:v1:<sha256>` (STRING + TTL) | **UI/UX (応答速度)** |

> 「ランキング改善」と「応答速度改善」は別軸の課題なので、Redis の役割も別 Port (`SynonymExpanderPort` / `SearchCachePort`) に分けて Adapter を独立させる。

---

## SYN-1 同義語辞書 (ranking 精度)

Lexical 投入 **直前** にだけクエリを膨らませる。

```text
"駅近 1K"
   ↓ RedisSynonymExpander.expand()
"駅近 駅徒歩 駅から徒歩 アクセス良好 1K ワンルーム 1R"
   ↓ Meilisearch BM25
「駅徒歩 ワンルーム」物件も lexical 候補に乗る
```

- ME5 encoder 側は **元クエリ** を渡す (embedding が同義語で希釈されないため)
- 辞書は `definitions/synonyms/real_estate_ja.yaml` を `make seed-synonyms` で投入

---

## SYN-2 `/search` レスポンスキャッシュ (UX/latency)

`SearchService.search` の入口で cache lookup → HIT で全段スキップ。

```text
SearchInput(query, filters, top_k, explain)
   ↓ sha256 ハッシュ
key: search:v1:<32hex>
   ↓ Redis SETEX (TTL=60s)
HIT: encoder/retriever/reranker/publisher を全部 skip
MISS: 通常通り走り、応答を Redis に書き戻す (write-through)
```

- HIT 時は **新しい request_id をスタンプ** して返す (tracking が壊れない)
- HIT 時は **ranking_log を再 publish しない** (元 MISS で済 → 重複防止)
- Redis 障害は MISS と同じ扱い (`/search` 可用性は Redis に依存しない)

---

## 処理の流れ

```text
/search 入口
  ↓
SYN-2 cache lookup ─[HIT]→ 即返却 (encoder/retriever/reranker/publisher 全 skip)
  ↓ MISS
SYN-1 同義語展開 (lexical 用クエリのみ)
  ↓
条件付き lexical 候補取得 (Meilisearch)
  ↓
query embedding 作成 (元クエリ、ME5)
  ↓
semantic score 計算 (pgvector)
  ↓
RRF 融合
  ↓
特徴量作成 (PostgreSQL feature_mart)
  ↓
LambdaRank rerank
  ↓
ranking_log publish + SYN-2 cache write-through
```

---

## 検索 MLOps の難所

- 正解データの作成が難しい
- 「良い検索結果」を数値化しにくい
- 行動ログには **表示位置バイアス** が乗る
- オフライン指標とオンライン成果が一致しない
- ユーザーごとに重視軸が違う (価格 / 駅距 / 築年 / 写真...)

> モデルだけでなく、**評価とログ設計** が改善の主戦場。

---

## なぜ精度評価が難しいか

- Phase 1 (住宅価格): 予測 3,000 万 vs 実際 3,100 万 → 誤差 100 万、明快
- Phase 3 (検索): 「札幌 1LDK 駅近」の正解は一意に決まらない
- そこで **複数指標** で見る:
  - Recall@K / Precision@K / NDCG@K / MRR
  - CTR / CVR / 問い合わせ率 / お気に入り率
- どの指標も万能ではない (クリック ≠ 良い結果)

---

## なぜ正解データ作成が難しいか

- Phase 1 の教師: 成約価格・査定価格 (数値、明確)
- Phase 3 の教師: クリック / お気に入り / 問い合わせ / 内見 / 成約
- 行動ログは「真の良さ」を表しているとは限らない
- 必要な工夫:
  - 表示位置バイアス補正
  - クエリごとの評価データ作成
  - 一部に **人手評価 / LLM-as-a-Judge**
  - オフライン / オンラインを分けて測る

---

## ディレクトリの見方

| パス | 役割 |
|---|---|
| `app/` | `/search` / `/feedback` API |
| `ml/data/` | embedding 補助、特徴量補助 |
| `ml/training/` | LambdaRank 学習 |
| `pipeline/` | 定常バッチ、学習、評価 |
| `definitions/` | PostgreSQL migration |

---

## この Phase で押さえること

1. 検索は段階分担 (lexical / semantic / rerank) で見る
2. ベクトル DB は意味検索の本体、Phase ごとに置換可
3. `LightGBM` は単一スコアでなく **総合判定** の道具
4. **Redis は 2 役割** — SYN-1 同義語辞書 (ranking 精度) + SYN-2 レスポンスキャッシュ (UX/latency)
5. 検索 MLOps の難所は **評価とログ設計** に集中する
6. **正解データの作り方** が品質改善の出発点

---

## 次の Phase へ

Phase 4 ではこの検索思想を保ったまま、  
GCP serverless 実行基盤へ移す。
