# 04_運用 (Phase 3: 不動産ハイブリッド検索 Local)

> 🎯 **2026-05-05 GOAL 達成済**。本手順は実測再現可能。検証ゲートはすべて PASS、
> Web UI 起動済。実測値は [`05_精度評価.md`](05_精度評価.md)、Wave 履歴は
> [`02_移行ロードマップ.md`](02_移行ロードマップ.md)。

Phase 3 のローカル運用手順。Docker Compose 1 stack で完結する。

## 1. 初回セットアップ

```bash
# 1. credentials のサンプルから実体を作る
cp env/secret/credential.yaml.example env/secret/credential.yaml

# 2. uv workspace を同期 (host 側で開発する場合)
make sync

# 3. Docker image を build
#    sentence-transformers の model は `hf_cache` volume 経由で初回起動時に download
#    (image には焼き込まない設計、2 回目以降の起動は cache hit で数秒)
make build
```

## 2. PDCA loop (検証実測済)

```bash
make up         # postgres (host port 5433) / meilisearch / redis を up + healthcheck wait
make seed       # 合成 1000 件投入 + Meilisearch index + pgvector embedding + feature_mart + Redis 同義語辞書
                # (内訳: pipeline.data_job.main → make seed-synonyms。Redis volume を持たないので
                #  redis container 再起動の度に make seed-synonyms を流し直す)
make seed-synonyms  # Redis 同義語辞書だけ再投入 (definitions/synonyms/real_estate_ja.yaml)
                    # 注: /search レスポンスキャッシュ (SYN-2) は seed 不要 (TTL 60s で自動転回)。
                    # 辞書を更新したら `redis-cli FLUSHDB` などでキャッシュも消したほうが
                    # 古い結果を引きずらない (TTL 期限切れまで待っても OK)。
make train      # LightGBM LambdaRank 学習 → ml/registry/artifacts/{run_id}/model.lgb
make evaluate   # NDCG@10 / MAP / Recall@20 を metrics.json に追記
docker compose up -d search-api    # search-api を background で起動
```

### 2.1 ブラウザで Web UI を開く 🆕

```
http://localhost:8000/
```

`/` から自動で `/ui/` に redirect され、Pico-admin dark theme の検索 UI が表示される。
- 自由文クエリ + フィルタ (max_rent / layout / max_walk_min / max_age / pet_ok / top_k)
- 結果カード/テーブル形式表示
- 結果アイテムの click ボタンで `/feedback` に POST (`feedback_events` 永続化)

API 仕様は [`/docs`](http://localhost:8000/docs) (FastAPI Swagger UI) で確認できる。

### 2.2 CLI から検索を叩く

```bash
# /search (3 段検索 = lexical + semantic + RRF + rerank)
curl -s -X POST http://localhost:8000/search \
  -H 'Content-Type: application/json' \
  -d '{"query":"駅近 2LDK","filters":{"max_rent":150000,"layout":"2LDK"},"top_k":10}' | jq

# /feedback (行動ログ)
curl -s -X POST http://localhost:8000/feedback \
  -H 'Content-Type: application/json' \
  -d '{"request_id":"req-0001","property_id":"prop-0001","action":"click"}'
```

`/search` レスポンスの各 item は `lexical_rank` / `semantic_rank` / `final_rank` が
**all non-zero** になっていること (3 系統が動いている証拠)。GOAL 達成時の実測抜粋:
`lexical_rank=5,2,1,7 / semantic_rank=12,23,4,6 / final_rank=1,2,3,4 / score=0.099,0.099,0.016,0.016` (詳細は [`05_精度評価.md` §4](05_精度評価.md))。

## 3. ローカル開発 (Docker なし、host で uvicorn)

```bash
# postgres / meilisearch / redis は make up で立てたまま
# host port は 5433 (5432 を使う他プロジェクトとの衝突回避)
export POSTGRES_DSN=postgresql://admin:password@localhost:5433/hybrid_search
export MEILI_BASE_URL=http://localhost:7700
export MEILI_MASTER_KEY=phase3-local-master-key
export REDIS_URL=redis://localhost:6379/0
export E5_MODEL_NAME=intfloat/multilingual-e5-small

# Phase 3 SYN-1 (同義語辞書) + SYN-2 (/search レスポンスキャッシュ) を有効化。
# どちらも Redis container に同居するが key prefix で分離されている。
# 学習用に切り替えたい場合: SYNONYM_BACKEND=none / SEARCH_CACHE_BACKEND=none。
export SYNONYM_BACKEND=redis
export SEARCH_CACHE_BACKEND=redis
export SEARCH_CACHE_TTL_SECONDS=60

make seed-synonyms  # 辞書を Redis に流す (初回 + redis container 再起動後に必要)
make api-dev        # uv run uvicorn --reload
```

## 4. テストと品質ゲート (検証実測済)

```bash
make test            # unit test (外部依存なし) — 24 passed (Wave 5 SYN-1/SYN-2 14 件追加済)
make check           # ruff + ruff format --check + mypy strict + pytest — PASS (mypy 89 source files)
make check-layers    # AST 境界検査 — ✓ 10 ルール、google.cloud/kserve/kfp/redis Port-Service 越え 0
make verify-pipeline # integration E2E — 5 passed in 3.37s (Docker Compose + search-api 起動済前提)
```

## 5. 完了判定 (Phase 3 ゴール) 🎯 全 PASS 済 (2026-05-05)

1. ✅ `make build` で 3 service `healthy`
2. ✅ `make seed && make train` 後 `curl /search` が 3 系統 all non-zero、`final_rank` の rerank score が学習済モデル由来 (fallback 値でない)
3. ✅ `make check` exit 0 (ruff + format + mypy strict + pytest)
4. ✅ `make check-layers` で `google.cloud` / `kserve` / `kfp` 違反ゼロ (10 ルール)
5. ✅ `make verify-pipeline` PASS (5 cases)、`ranking_log` row 1+、`/feedback` 200
6. ✅ `metrics.json` に `ndcg_at_10`=0.9822 / `map`=0.9967 / `recall_at_20`=1.0 (random ベースライン超え、run_id `20260505T042413Z-1aab0e91`)
7. ✅ Web UI が `http://localhost:8000/ui/` で表示される (Pico-admin dark theme、`/static/css/custom.css` / `/static/js/search_ui.js` 200)

## 6. トラブルシューティング

### 6.1 sentence-transformers の初回 download が遅い

Dockerfile では model を image に焼き込まない設計 (image size 削減のため)。
初回起動時に `hf_cache` volume (`docker-compose.yml` で定義) に model (~470MB) が
download される。2 回目以降は cache hit で数秒。

`E5_MODEL_NAME=intfloat/multilingual-e5-base` (768 dim) に変えたい場合は
`definitions/postgres/migrations/0001_init.sql` の `vector(384)` を `vector(768)` に
変更が必要 (volume を作り直す `make clean` も必要)。

### 6.2 pgvector index が無くて遅い

合成 1000 件レベルでは index なしの seq scan で十分速い (~10ms)。
数万件以上にスケールさせる場合は migration に追加:

```sql
CREATE INDEX ON embeddings USING hnsw (embedding vector_cosine_ops);
```

### 6.3 Meilisearch の filter で 400 エラー

`update_filterable_attributes` 完了前に `/search` が呼ばれている可能性。
`make seed` 完了直後に Meilisearch のインデックスタスクが完了するまで数秒待つ。
タスク状態は `curl http://localhost:7700/tasks` で確認。

### 6.4 `model.lgb` が無い (rerank fallback)

`composition_root.py` は `ml/registry/artifacts/latest/model.lgb` を探し、
無ければ `reranker_client=None` で起動。fallback モードでは `final_rank == lexical_rank`。
解消: `make train` を 1 回回す。`/readyz` で `rerank_enabled: true / model_path: ...` が返ることを確認。

### 6.5 ranking_log が空のまま訓練したい

`pipeline/training_job/main.py` は ranking_log が浅い場合 `synthetic_ranking_frames`
で fallback 学習する。`/search` を数回叩いてから再学習で実データ入りに切り替わる。

### 6.6 host port 5432 衝突 (別プロジェクトの postgres)

Phase 3 docker-compose.yml は **`5433:5432`** で host port を 5432 → 5433 にずらしている。
container 間通信は `postgres:5432` (内部) のままなので影響なし。`make api-dev` で host
側 export する際は `localhost:5433` を使う (§3 参照)。

別プロジェクトの postgres も止めたい場合:
```bash
docker stop $(docker ps -q --filter name=postgres) 2>/dev/null
```

### 6.7 Docker container 名衝突

`docker compose up --force-recreate` の途中で古い container が残ると重複名で fail する:
```text
Error response from daemon: Error when allocating new name: Conflict.
The container name "/phase3-search-api" is already in use by container "<hash>"
```

解決:
```bash
docker rm -f phase3-search-api    # 強制削除
docker compose up -d --no-deps search-api
```

### 6.8 postgres volume が古くて DB `hybrid_search` 不在

`docker compose down` (without `--volumes`) → 再起動の場合、旧 volume が再利用され
initdb が走らないため `hybrid_search` DB が不在のまま。symptom:

```text
psycopg.OperationalError: connection failed: ... FATAL: database "hybrid_search" does not exist
```

解決:
```bash
docker compose down --volumes    # volume ごと削除
make up                          # 新 volume + 0001_init.sql 再実行
docker exec phase3-postgres psql -U admin -d hybrid_search -c '\dt'   # 5 テーブル確認
```

### 6.9 `uv sync` で project install 失敗 (build 時)

Dockerfile は `uv sync --no-dev --no-install-project` を使う (project 自体は後で
`COPY app/ ml/ pipeline/` + `PYTHONPATH=/app` で読む)。古い Dockerfile で
`uv sync --no-dev` だけだと `app/ml/pipeline` が COPY されてないタイミングで
hatchling build が fail する。本リポは fix 済 (`infra/run/{services/api,jobs/pipeline}/Dockerfile` 参照)。

### 6.10 container 内で `python -m ...` が依存を見つけない

Dockerfile に `ENV PATH="/app/.venv/bin:${PATH}"` が必要 (uv が作る venv の Python を
default にするため)。古い Dockerfile で system Python が呼ばれると
`ModuleNotFoundError: No module named 'meilisearch'` 等が出る。本リポは fix 済。

### 6.11 同義語辞書を編集したのに `/search` 結果が古いまま (SYN-1 + SYN-2 の干渉)

Redis に **同義語辞書 (SYN-1)** と **`/search` レスポンスキャッシュ (SYN-2)** が同居しているため、
辞書だけ更新しても直前の `/search` 結果がキャッシュ HIT で返ってくる (~60s)。解消手順:

```bash
make seed-synonyms                              # 辞書を再投入
docker exec phase3-redis redis-cli --scan --pattern 'search:*' | \
  xargs -r docker exec -i phase3-redis redis-cli DEL      # SYN-2 だけ消す
# あるいは TTL=60s 待つ (本番運用時はこちら)
```

`syn:*` と `search:*` で key prefix が分かれているため、`syn:` 側を消さないことに注意。

### 6.12 `/search` キャッシュを完全に切りたい (デバッグ時)

`docker-compose.yml` の `search-api.environment` で `SEARCH_CACHE_BACKEND=none` に切り替え、
`docker compose up -d --force-recreate search-api` で再起動。`NoopSearchCache` が選択され、
全リクエストが live 実行になる (= Wave 1-4 と同等挙動)。SYN-1 (同義語) は別 backend なので
影響を受けない。

## 7. クリーンアップ

```bash
make down    # postgres volume を残したまま停止
make clean   # postgres / meilisearch / hf_cache volume + ml/registry/artifacts/* を全削除
```

## 8. 関連ドキュメント

- [`02_移行ロードマップ.md`](02_移行ロードマップ.md) — 権威 1 位、決定的仕様 + Wave 履歴
- [`01_仕様と設計.md`](01_仕様と設計.md) — Phase 3 固有の差分仕様 + Web UI 仕様
- [`03_実装カタログ.md`](03_実装カタログ.md) — dir / file / DB / API / adapter の 1:1 対応
- [`05_精度評価.md`](05_精度評価.md) — NDCG / MAP / Recall 実測 + 評価指標と LLM 評価の比較補論
- [`TASKS.md`](TASKS.md) — current sprint 進捗
