# 01_仕様と設計 (Phase 3: 不動産ハイブリッド検索 Local)

> **2026-05-05 GOAL 達成**: Wave 1-4 全完了。Phase 7 (canonical) からの引き算で Local
> Docker Compose 上に 3 段ハイブリッド検索 (Meilisearch + ME5 + pgvector + RRF +
> LightGBM LambdaRank) を完全動作させた。検証結果は [`05_精度評価.md`](05_精度評価.md)
> (NDCG@10=0.9822 / MAP=0.9967 / Recall@20=1.0、run_id `20260505T042413Z-1aab0e91`)。
>
> **2026-05-03 教育コード化方針の改訂**: Phase 3 は **コード化対象 Phase**。Phase 1 / 2 / 3 / 4 / 7 がコード化対象、Phase 5 / 6 は論理 Phase (Phase 7 完成版コードを参照)。詳細は親 [`README.md` §4 コード化方針](../../../README.md)。
>
> **canonical な Phase 横断仕様 (Phase 3-7 共通)** は [`7/study-hybrid-search-gke/docs/architecture/01_仕様と設計.md` §2](../../../7/study-hybrid-search-gke/docs/architecture/01_仕様と設計.md)。
> **教育設計の正本**は親 [`README.md`](../../../README.md)。
> **本ファイル**は Phase 3 固有の差分 (Local 完結 / 不動産ハイブリッド検索ドメイン初登場 / Phase 7 と完全同型 8 Port を Local Adapter で実装) を示す。

---

## 0. 位置付け (Phase 3: 不動産ハイブリッド検索の Local 完成形 + 最小行動ログ収集)

Phase 3 は **コード化対象 Phase**。題材を **不動産ハイブリッド検索** に切り替えた最初の phase で、**ローカル完結で 3 段検索 (lexical + semantic + rerank) を一巡** させる Local 完成形。**Phase 4 (GCP MLOps 土台 + 検索ログ正解データパイプライン)** は Phase 3 からの足し算で構築する起点でもある。

Phase 2 → Phase 3 の飛躍 (詳細は親 [`README.md` §「Phase 2 → 3 の接続」](../../../README.md)):

- ドメイン: California Housing 回帰 → **不動産検索 (lexical + semantic + rerank の多段構成)**
- ML タスク: 回帰 → **ランキング学習 (LambdaRank / NDCG)** — 評価指標の質的違いは [`05_精度評価.md` §7](05_精度評価.md) を参照
- Adapter 数: 増 (Meilisearch / multilingual-e5 / pgvector / Redis / LightGBM)

### Wave 5 予告 (将来展開-v3.md 反映、2026-05-05): 最小行動ログ収集 + 弱教師ラベル作成

v3 設計改訂で、Phase 3 に **検索ログ・表示ログ・クリックログ・資料請求ボタンクリックログを PostgreSQL に保存し、簡易ラベル作成までを行う** 工程を Wave 5 として追加予定 (詳細は [`../../docs/将来展開-v3.md`](../../../docs/将来展開-v3.md) §「ローカル構成」)。Phase 4 接続契約として:

- PostgreSQL に **`search_events` / `search_impressions` / `user_actions` の 3 テーブル + `action_type` enum 8 種** (`click` / `detail_view` / `favorite` / `request_button_click` / `request_complete` / `inquiry_complete` / `contract` / `bounce`) を追加
- **重み付き relevance label による弱教師 labeling job** (`click`=1, `detail_view`=2, `favorite`=3, `request_button_click`=4, `request_complete`=5, `inquiry_complete`=7, `contract`=10, `no_action`=0, `bounce`=0/-1)
- **LightGBM LambdaRank 用 training dataset 作成** (`(query_id, search_id, property_id, relevance_label, features...)` 形式)

新 Port 5 本 (`EventWriter` / `EventRepository` / `LabelRepository` / `TrainingDatasetRepository` / `MetricsRepository`) を追加し、Phase 3 では PostgreSQL adapter で実装、Phase 4 では Cloud Logging / GCS / BigQuery adapter に差し替える形で足し算する (詳細は §6.5)。

---

## 1. Phase 3 の中核 (不変) — 実装エンティティ対応表

中核 5 要素は Phase 3 / 4 / 7 のコード化対象で共通 (canonical は Phase 7 docs/01 §2.3)。Phase 3 では Local 実装で構成する:

| 段 | Phase 3 (Local) 実装エンティティ | Port (`app/services/protocols/`) |
|---|---|---|
| (1a) Lexical | `MeilisearchLexicalSearch` ([`app/services/adapters/meilisearch_lexical_search.py`](../app/services/adapters/meilisearch_lexical_search.py)) — Phase 7 流用 + 認証簡素化 (master key + Docker hostname) | `LexicalSearchPort` |
| Lexical 補助 | **`RedisSynonymExpander`** ([`app/services/adapters/redis_synonym_expander.py`](../app/services/adapters/redis_synonym_expander.py)) — Phase 3 SYN-1。`syn:<token>` SET を SMEMBERS して query expansion (例: `マンション` → `マンション アパート 共同住宅`)。BM25 投入直前にだけ拡張、ME5 encoder には素のクエリを渡す。辞書本体は [`definitions/synonyms/real_estate_ja.yaml`](../definitions/synonyms/real_estate_ja.yaml) → `make seed-synonyms` で Redis に流し込む | **`SynonymExpanderPort`** (Phase 7 SYN-1 と同型) |
| UX/Latency 補助 | **`RedisSearchCache`** ([`app/services/adapters/redis_search_cache.py`](../app/services/adapters/redis_search_cache.py)) — Phase 3 SYN-2。`/search` レスポンスを `(query, filters, top_k, explain)` の sha256 ハッシュをキーに `search:v1:<hash>` で SETEX (TTL 60s)。HIT で encoder/retriever/reranker/publisher を全て skip する write-through キャッシュ。`request_id` はキャッシュ層で剥がし、HIT 時は live request_id をスタンプして返すので tracking が壊れない。`syn:` と `search:` で key prefix が分離されているため Redis instance を共有できる | **`SearchCachePort`** (Phase 4+ で Cloud Memorystore に差し替え可) |
| (1b) Semantic encoder | `LocalE5Encoder` ([`app/services/adapters/local_e5_encoder.py`](../app/services/adapters/local_e5_encoder.py)) — sentence-transformers `intfloat/multilingual-e5-small` を in-process load | `EncoderClient` |
| (1b) Semantic vector store | `PgVectorSemanticSearch` ([`app/services/adapters/pgvector_semantic_search.py`](../app/services/adapters/pgvector_semantic_search.py)) — PostgreSQL + pgvector の `<=>` 演算子 (cosine distance) | `SemanticSearchPort` |
| (2) 融合 | [`app/services/ranking.py::rrf_fuse`](../app/services/ranking.py) (RRF, `RRF_K=60`) — **Phase 7 から無改変流用** | (Service 純粋関数) |
| (3) Rerank | `LocalLightGBMReranker` ([`app/services/adapters/local_lightgbm_reranker.py`](../app/services/adapters/local_lightgbm_reranker.py)) — `lightgbm.Booster.load_model()` で `ml/registry/artifacts/{run_id}/model.lgb` 直読み | `RerankerClient` |
| (補) Candidate retriever | `LocalCandidateRetriever` ([`app/services/adapters/local_candidate_retriever.py`](../app/services/adapters/local_candidate_retriever.py)) — lexical + semantic + RRF + Postgres `properties` enrichment を統合 | `CandidateRetriever` |
| (補) Feature fetcher | `PostgresFeatureFetcher` — `feature_mart_property_features_daily` 直接 SELECT | `FeatureFetcher` |
| (補) Ranking log | `PostgresRankingLogPublisher` — `ranking_log` INSERT (Pub/Sub の代替)。**Phase 4 では `EventWriter` Port (`cloud_logging_event_writer` / `gcs_raw_event_writer` adapter) と `EventRepository` Port (`bigquery_event_repository` adapter) に汎化される** (Wave 5 予定、§6.5 参照) | `RankingLogPublisher` |
| (補) Feedback | `PostgresFeedbackRecorder` — `feedback_events` INSERT。**Wave 5 で `event_type` を `action_type` enum 8 種** (`click` / `detail_view` / `favorite` / `request_button_click` / `request_complete` / `inquiry_complete` / `contract` / `bounce`) **に拡張し、Phase 4 で `cloud_logging_event_writer` adapter に汎化される** | `FeedbackRecorder` |

中核 5 要素 (Meilisearch BM25 / multilingual-e5 / ベクトルストア / RRF / LightGBM LambdaRank) の挙動・データフロー・デフォルト `/search` 応答は **Phase 3-7 共通で不変**。詳細は [Phase 7 docs/01 §2.3](../../../7/study-hybrid-search-gke/docs/architecture/01_仕様と設計.md)。

---

## 2. Phase 3 の主要コンポーネント

### 2.1 `app/`

- `app/main.py` — FastAPI entrypoint + lifespan + StaticFiles + 3 router + UI router register
- `app/composition_root.py` — `ContainerBuilder` / `Container` (DI 配線、`InfraBuilder` 等の分割なし版)
- `app/settings.py` — `ApiSettings` (pydantic-settings、`env/config/setting.yaml` + `env/secret/credential.yaml` を merge)
- `app/domain/` — `Candidate` / `RankedCandidate` / `LexicalResult` / `SemanticResult` / `SearchInput` / `SearchOutput` / `SearchFilters` (SDK 非依存、Phase 7 流用)
- `app/schemas/search.py` — FastAPI 入出力 Pydantic (Phase 7 流用)
- `app/services/protocols/` — 10 Port (Phase 7 と完全同型 + Phase 3 固有 SYN-2 = `SearchCachePort`)
- `app/services/adapters/` — 10 Phase 3 Local adapter (上記表 + `RedisSynonymExpander` + `RedisSearchCache`)
- `app/services/noop_adapters/` — production noop fallback (5 本: lexical / ranking_log / feedback / synonym_expander / search_cache)
- `app/services/{search_service,ranking,feedback_service}.py` — Phase 7 流用 (`popularity_scorer` 連携のみ削除)
- `app/api/{routers,mappers,middleware,dependencies}.py` — Phase 7 流用

### 2.2 `ml/`

- `ml/data/feature_engineering/{schema,ranker_features}.py` — Phase 7 流用 (`FEATURE_COLS_RANKER` 10 列 / `build_ranker_features`)
- `ml/training/{trainer,model_builder}.py` — Phase 3 用 minimal (GCS uploader / Vertex Experiments 撤去、`LocalArtifactStore` で書き出し)
- `ml/evaluation/metrics/{ranking,label_gain}.py` — Phase 7 流用 (numpy のみで `ndcg_at_k` / `mean_average_precision` / `recall_at_k` / `evaluate`)
- `ml/registry/artifact_store.py` — `LocalArtifactStore` (新規、Phase 7 `GcsArtifactUploader` の Phase 3 版)
- `ml/common/{config,logging,utils}/` — Phase 7 流用 (`TrainSettings` から GCS / Vertex 設定削除)

### 2.3 `pipeline/`

- `pipeline/data_job/main.py` — synthetic 1000 件不動産データ → PostgreSQL `properties` + Meilisearch index + pgvector `embeddings` + `feature_mart_property_features_daily` 投入
- `pipeline/training_job/main.py` — `ml.training.trainer.run()` の CLI ラッパ
- `pipeline/evaluation_job/main.py` — NDCG@10 / MAP / Recall@20 hold-out 再評価 → `metrics.json` 上書き

### 2.4 `definitions/`

- `definitions/postgres/migrations/0001_init.sql` — `CREATE EXTENSION vector` + 5 テーブル DDL (`properties` / `embeddings` (`vector(384)`) / `feature_mart_property_features_daily` / `ranking_log` / `feedback_events`)
- **Wave 5 予定 (将来展開-v3.md 反映)**: `0002_event_schema.sql` を追加し、Phase 4 接続契約として **`search_events` / `search_impressions` / `user_actions` / `ranking_labels` の 4 テーブル + `action_type` enum 8 種** (`click` / `detail_view` / `favorite` / `request_button_click` / `request_complete` / `inquiry_complete` / `contract` / `bounce`) を導入。既存 `feedback_events` の `event_type` 列を新 enum に拡張し、`PostgresFeedbackRecorder` を `PostgresEventWriter` に汎化する

---

## 3. API 仕様

| メソッド + Path | 役割 | router |
|---|---|---|
| `GET /health` | liveness probe | `app/api/routers/health_router.py` |
| `GET /readyz` | readiness (`rerank_enabled` / `model_path` 表示) | 同上 |
| `POST /search` | 自由文クエリ + フィルタ → 物件ランキング上位 N 件 (3 段検索) | `app/api/routers/search_router.py` |
| `POST /feedback` | 行動ログ受付 (click / favorite / inquiry → `feedback_events` 永続化)。**Wave 5 で action 列挙を 8 種** (`click` / `detail_view` / `favorite` / `request_button_click` / `request_complete` / `inquiry_complete` / `contract` / `bounce`) **に拡張予定** (将来展開-v3.md L100-109) | `app/api/routers/feedback_router.py` |
| `GET /` | `/ui/` へ 308 redirect | `app/api/routers/ui_router.py` |
| `GET /ui/` | Browser UI (Jinja2 render の HTML) | 同上 |
| `GET /static/{css,js}/*` | UI 静的アセット (Pico-admin theme + search_ui.js) | `app.mount("/static", StaticFiles)` |
| `GET /docs` | Swagger UI (FastAPI 自動生成) | FastAPI default |

`/search` レスポンスの各 item は `lexical_rank` / `semantic_rank` / `final_rank` / `score` (LightGBM 出力) / `me5_score` (cosine similarity) を持つ。3 系統 all non-zero が動作の証拠 (実測例は [`05_精度評価.md` §4](05_精度評価.md))。

---

## 4. Web UI 仕様

Phase 7 から流用した Pico-admin dark theme + Jinja2 templates + `search_ui.js` で、search-user 1 page を提供する。

### 4.1 構成

- **CSS** (Phase 7 から無改変流用、`app/static/css/`):
  - `pico-admin-pico.css` / `pico-admin-theme.css` / `pico-admin-layout.css` / `pico-admin-components.css` / `custom.css`
- **JS** (Phase 7 から無改変流用、`app/static/js/`):
  - `search_ui.js` (`HybridSearchUI.init({rootId, mode})` API、316 行)
- **Templates** (`app/templates/`):
  - `base.html` — **Phase 3 用に簡素化**: sidebar から Developer 詳細 (model-metrics / data / ops) を削除、segmented から「開発」「Ops」を削除、brand を `Phase 3 · Local Docker Compose` に変更
  - `index.html` (search-user) — Phase 7 流用
  - `_search_form.html` / `_search_results.html` / `_feedback_panel.html` — Phase 7 流用 (page_mode="user")
- **Router**: `app/api/routers/ui_router.py::build_ui_router(app_root)` — Phase 7 から `/dev` `/dev/model/metrics` `/dev/data` `/dev/ops` を削除した最小版

### 4.2 Phase 3 で扱わない UI ページ (Phase 4 以降の責務)

- `/ui/dev` — 開発者検索 (explain / popularity 等)
- `/ui/dev/model/metrics` — モデル精度
- `/ui/dev/data` — 学習データ
- `/ui/dev/ops` — 運用チェック

---

## 5. データフロー (Phase 3 = Local)

0. **Redis SYN-2** (`SearchCachePort`) で `(query, filters, top_k, explain)` キーで cache lookup。HIT なら 1〜6 を skip して即返却 (TTL 60s 以内の同条件再検索)
1. **Redis SYN-1** (`SynonymExpanderPort`) で query を同義語展開 (例: `マンション` → `マンション アパート 共同住宅`)。**lexical 側のみ** に流す (semantic side は素のクエリのまま、ME5 の意味類似に任せる)
2. **Meilisearch** で展開クエリを BM25 + filter にかけ候補を取る (lexical) → `LexicalResult[]`
3. **multilingual-e5** で **元クエリの** 意味類似度を計算 (semantic encoder + pgvector ANN) → `SemanticResult[]`
4. **RRF** (`rrf_fuse`) で異種 score の順位を統合し top-K に絞る
5. **PostgreSQL** から `properties` + `feature_mart_property_features_daily` で候補の特徴量を補完
6. **LightGBM LambdaRank** (`Booster.predict`) で最終順位を決める
7. **PostgreSQL** `ranking_log` に full pool を永続化、`/search` レスポンス返却
8. **Redis SYN-2** に応答を `search:v1:<sha256>` で SETEX (TTL=60s)。次の同条件は HIT で短絡

> **Redis の 2 役割は別 prefix で完全分離**: `syn:<token>` (SYN-1 同義語辞書、ranking 精度) と `search:v1:<hash>` (SYN-2 レスポンスキャッシュ、UX/latency)。同じ Docker container を共有するが衝突しない。

### 5.1 Wave 5 予定: 行動ログ → 弱教師ラベル → LightGBM 再学習用 dataset (将来展開-v3.md 反映)

オンライン経路完了後の付加処理として、**継続改善サイクル土台** をローカル最小構成で一巡させる:

9. **`feedback_events` への user action ingest** (Phase 3 PostgreSQL ローカル契約) — `POST /feedback` から `action_type` enum 8 種 + `action_value` (timestamp delta / click_dwell_ms 等) を永続化
10. **弱教師ラベル作成** (`make label` バッチ job) — `ranking_log` (impression) × `feedback_events` (action) を join し、**重み付き relevance** を計算: `click`=1, `detail_view`=2, `favorite`=3, `request_button_click`=4, `request_complete`=5, `inquiry_complete`=7, `contract`=10, `no_action_impression`=0, `bounce_after_click`=0/-1 → `ranking_labels` テーブルへ INSERT
11. **LightGBM LambdaRank 用 training dataset 作成** (`make build-training-dataset` バッチ job) — `ranking_labels` × `feature_mart_property_features_daily` を join し、`(query_id, search_id, property_id, relevance_label, features...)` 形式で local CSV/Parquet に書き出す → `ml/training/trainer.py` から読み取って Booster 再学習

クリックは「完全な正解」ではなく **弱教師信号** として扱う (将来展開-v3.md §「クリック」「資料請求ボタンクリック」は正解データか? 参照)。

オンライン経路の詳細シーケンス図は [Phase 7 docs/01 §2.2.2](../../../7/study-hybrid-search-gke/docs/architecture/01_仕様と設計.md) を参照 (Phase 3 でも論理は同じ、adapter 実装が違うのみ)。

---

## 6. Phase 3 で扱わないもの (Phase 4 以降 / Phase 7 完成版の責務)

| 技術領域 | 導入 phase | 位置づけ |
|---|---|---|
| Cloud Run / Cloud Run Jobs / BigQuery / GCS / Secret Manager / Terraform / WIF / Cloud Scheduler / Eventarc / Cloud Function / Artifact Registry / Cloud Build | **Phase 4** | コード化対象 (Phase 3 からの足し算) |
| Dataform / **BigQuery feature table / view** / **BigQuery `VECTOR_SEARCH`** | **Phase 4** | コード化対象 (Phase 3 からの足し算) |
| **検索ログ正解データパイプライン** (Cloud Run 構造化ログ → Cloud Logging → GCS raw event logs → BigQuery `raw_events` / `curated` / `ranking_labels` / `training_dataset` / `evaluation_metrics` (NDCG@10 / Recall@K / CTR / CVR) + Cloud Run Job labeling / retrain → GCS model artifact) | **Phase 4** | コード化対象 (v3 設計改訂、Phase 3 のローカル最小行動ログを GCP データパイプラインに足し算) |
| Vertex AI Pipelines (KFP v2) / Endpoint / Model Registry / Monitoring | **Phase 5 (論理 Phase)** | Phase 7 完成版コードを参照する資料化 |
| **Vertex AI Feature Store (Feature Group / Feature View / Feature Online Store)** | **Phase 5 (論理 Phase)** | Phase 7 完成版コードを参照する資料化 |
| **Vertex Vector Search** (ME5 ベクトルの本番 serving index、BQ は embedding 履歴正本) | **Phase 5 (論理 Phase)** | Phase 7 完成版コードを参照する資料化 |
| Cloud Composer / Managed Airflow Gen 3 (本線 orchestration) / BQML / Dataflow Flex Template / TreeSHAP Explainability / Monitoring SLO + burn-rate / Composer-managed BQ monitoring query | **Phase 6 (論理 Phase)** | Phase 7 完成版コードを参照する資料化 |
| GKE Autopilot / KServe / Gateway API / External Secrets Operator / Workload Identity / GMP / HPA / IAP / NetworkPolicy / Helm provider | **Phase 7 (canonical = 教材コード完成版)** | 本実装 (Phase 5 / 6 の技術要素も Phase 7 配下に集約) |
| 全 Phase 教材対象外 (Agent Builder / Vizier / Model Garden / Gemini RAG / W&B / Looker Studio / Doppler) | — | 親 [`README.md` §1](../../../README.md) |

---

## 6.5 将来展開: Phase 4 接続点 (v3 設計改訂、2026-05-05)

将来展開-v3.md で再定義された **継続改善サイクル土台** (行動ログ → 正解データ → 再学習 → 評価 → deployment gate) を、Phase 3 → Phase 4 → Phase 7 で同型に保つための新 Port / Adapter 配置:

| Port (`app/services/protocols/`) | Phase 3 (Local) | Phase 4 (GCP MLOps 土台) | Phase 7 (canonical / 継続改善 MLOps サイクル) |
|---|---|---|---|
| **`EventWriter`** (`emit_search_event` / `emit_impression` / `emit_user_action`) | `PostgresEventWriter` (既存 `RankingLogPublisher` + `FeedbackRecorder` を Wave 5 で汎化) | `cloud_logging_event_writer` (Cloud Run 構造化ログ → Cloud Logging) + `gcs_raw_event_writer` (Eventarc → GCS raw bucket) | `cloud_logging_event_writer` (search-api on GKE) |
| **`EventRepository`** (`read_search_events` / `read_impressions` / `read_user_actions`) | `PostgresEventRepository` (3 テーブル直 SELECT) | `bigquery_event_repository` (BigQuery `raw_events` / `curated` SELECT) | `bigquery_event_repository` (Composer DAG が呼び出す) |
| **`LabelRepository`** (`write_ranking_labels` / `read_ranking_labels`) | `PostgresLabelRepository` (`ranking_labels` テーブル) | `bigquery_label_repository` (BigQuery `ranking_labels` テーブル + labeling SQL) | `bigquery_label_repository` (Composer DAG `retrain_orchestration` が呼ぶ) |
| **`TrainingDatasetRepository`** (`write_training_dataset` / `read_training_dataset`) | `LocalTrainingDatasetRepository` (CSV/Parquet local filesystem) | `gcs_training_dataset_repository` (GCS object) | `gcs_training_dataset_repository` (Vertex AI Pipelines が consume) |
| **`MetricsRepository`** (`write_evaluation_metrics` / `read_evaluation_metrics`) | `JsonMetricsRepository` (`runs/{run_id}/metrics.json`) | `bigquery_metrics_repository` (BigQuery `evaluation_metrics` テーブル) | `bigquery_metrics_repository` (Composer DAG `monitoring_validation` が deployment gate 評価に使う) |

orchestration adapter (Phase 7 のみ): `composer_dag_adapter` / `vertex_pipeline_adapter` は `pipeline/<verb>_job/adapters/` 側に配置。

> **アプリ本体が保存先を知らない構成** にすべき (将来展開-v3.md §「Port / Adapter リファクタリング案」)。Phase 3 の `event_writer.emit_search_event(...)` 呼び出しは、Phase 4 / 7 で adapter を差し替えるだけで GCP / Composer 経路に配線される。
>
> **本 spec 反映後の follow-up**: [`4/study-hybrid-search-gcp/docs/01_仕様と設計.md`](../../../4/study-hybrid-search-gcp/docs/01_仕様と設計.md) を `phase-doc-sync` skill で同期し、Phase 4 自身の正本も将来展開-v3.md に整合させる必要がある (本 spec 反映の scope 外)。

---

## 7. 不変ルール

- 題材は不動産ハイブリッド検索 (Phase 3 / 4 / 7 のコード化対象 + Phase 5 / 6 論理 Phase で共通)
- 中核 5 要素 (Meilisearch BM25 / multilingual-e5 / ベクトルストア / RRF / LightGBM LambdaRank) は Phase 3-7 で不変 (Phase 4 = BQ `VECTOR_SEARCH` / Phase 7 = Vertex Vector Search)
- Meilisearch は学習用 substitute (実案件想定は Elasticsearch + Redis 同義語辞書、詳細は Phase 5 docs/01 §「実案件想定の reference architecture」)
- Cloud / Vertex / GKE 系の責務は持ち込まない (Phase 4 で GCP 土台を足し算、Vertex AI / Composer / GKE / KServe は Phase 7 完成版コードに集約)
- 設計思想 (Port/Adapter / `core → ports ← adapters` / DI / FastAPI lifespan) は **Phase 2 から継承して不変**
- **Phase 7 と完全同型の 8 Port を Wave 2 で確認済**: `scripts/ci/layers.py` 10 ルールで AST 境界検査、`google.cloud` / `kserve` / `kfp` 違反 0 件 (`make check-layers` で常時 PASS)
- LightGBM Booster format 互換: `lightgbm>=4.5,<5` で Phase 3-7 共通固定 (`pyproject.toml`)

---

## 8. 関連ドキュメント

- 本 phase: [`02_移行ロードマップ.md`](02_移行ロードマップ.md) (権威 1 位、決定的仕様 + Wave 履歴) / [`03_実装カタログ.md`](03_実装カタログ.md) (実装スナップショット) / [`04_運用.md`](04_運用.md) (PDCA / トラブルシューティング) / [`05_精度評価.md`](05_精度評価.md) (実測 metrics + 評価指標と LLM 評価の比較補論) / [`TASKS.md`](TASKS.md) (current sprint) / [`教育資料/`](教育資料/)
- 親リポ: [`README.md`](../../../README.md) (Phase 一覧 + コード化方針) / [`docs/architecture/01_仕様と設計.md`](../../../docs/architecture/01_仕様と設計.md) (薄いナビゲーションハブ)
- canonical (Phase 7): [`7/study-hybrid-search-gke/docs/architecture/01_仕様と設計.md`](../../../7/study-hybrid-search-gke/docs/architecture/01_仕様と設計.md) (Phase 横断仕様 §2 + Composer §3)
- 前 phase: [`2/study-ml-app-pipeline/docs/01_仕様と設計.md`](../../../2/study-ml-app-pipeline/docs/01_仕様と設計.md) (Phase 2 App + Port/Adapter)
- 経緯: [`../IMPLEMENTATION_REMOVED.md`](../IMPLEMENTATION_REMOVED.md) (Wave 開始前の旧実装削除記録、現在は GOAL 達成済のため経緯のみ意味を持つ)
