# フォルダ・ファイル

この文書は、`study-gcp-mlops` を教材として横断的に読むための構造索引です。  
方針は単純で、**Phase 7 時点の最大集合を基準にし、各 Phase はそこから必要なものだけを持つ**とみなします。

詳細な命名規約は [命名規約.md](命名規約.md) を正本とします。  
この文書は「どこに何があるか」、`命名規約.md` は「なぜその名前か」を扱います。

## ルート構成

```text
.
├── 1/ ... 7/                各学習 phase
├── .github/                 ルート共通の agent / skill / GitHub 設定
├── docs/                    ルート共通 docs
├── mk/                      共通 Make 補助
└── tools/                   ルート共通ツール
```

## まず見る順

1. 対象 phase の `README.md`
2. 対象 phase の `CLAUDE.md`
3. 対象 phase の `Makefile`
4. 対象 phase の `docs/01_仕様と設計.md`
5. 対象 phase の `docs/03_実装カタログ.md`
6. 対象 phase の `docs/04_運用.md`
7. 実装本体として `app/` `ml/` `pipeline/` `scripts/`

## 最大集合

教材としての標準形は次です。  
各 Phase はこの集合から不要なものが抜けるだけ、という読み方をします。

```text
<phase>/
├── README.md
├── CLAUDE.md
├── Makefile
├── pyproject.toml
├── app/                    # API / UI / serving
├── common/                 # 共通設定・logging
├── ml/                     # 機能別 ML 実装
├── pipeline/               # <verb>_job + workflow
├── scripts/                # make から呼ぶ CLI
├── infra/                  # terraform / run / manifests
├── env/                    # config / secret
├── monitoring/             # 監視 SQL
├── docs/                   # 01/03/04 を中核に置く
├── tests/                  # unit / integration / e2e
├── tools/
├── logs/                   # 生成物
└── dist/                   # 生成物
```

### 共通フォルダの意味

| フォルダ | 役割 |
| --- | --- |
| `app/` | API / UI / serving 層。Phase 7 では DI 全面整備済 (詳細下記) |
| `common/` | 共通設定・logging・shared utility (Phase 2/3 のみ root 直下、Phase 4-7 は `ml/common/` に集約) |
| `ml/` | 機能別 ML 実装 |
| `pipeline/` | pipeline / job orchestration |
| `scripts/` | `make` の公開 API を支える CLI |
| `infra/` | Terraform / Cloud Run / GKE / manifest / Cloud Build 設定 |
| `env/` | `config` と `secret` |
| `monitoring/` | BigQuery SQL や監視補助 |
| `docs/` | 仕様・実装・運用・教材 |
| `tests/` | `unit` `integration` `e2e` |
| `logs/` | monitor 出力などの生成物 |
| `dist/` | compile / export 済み生成物 |

#### `app/` の内訳 (Phase 7 で確立した DI 構造)

| 配下パス | 役割 |
| --- | --- |
| `app/main.py` | FastAPI entrypoint。`create_app()` 内で `lifespan` を生やし `ContainerBuilder.build()` を呼ぶ |
| `app/composition_root.py` | `Container` (frozen dataclass) + `ContainerBuilder`。DI 配線の単一箇所 |
| `app/container/{infra,search,ml}.py` | DI builder 分割 (Pub/Sub-BQ / search path / ML optional path) |
| `app/container/_optional_adapter.py` | `enable_xxx` flag 付き optional adapter 構築の共通 helper |
| `app/observability.py` | `Observability` frozen dataclass — service_name / logger_factory / `expose_prometheus()` を束ねる |
| `app/settings/api.py` | `ApiSettings` (pydantic-settings) |
| `app/domain/` | `Candidate` / `SearchFilters` / `SearchInput` / `SearchOutput` 等 (Pydantic schema とは別) |
| `app/api/handlers/` | 1 endpoint 1 file (search / rag / feedback / retrain / model / health / ui) |
| `app/api/mappers/` | Pydantic ↔ domain 変換 |
| `app/api/middleware/` | `RequestLoggingMiddleware` (request_id 発行) |
| `app/api/dependencies.py` | `get_container` / `get_search_service` 等 FastAPI Depends 解決 |
| `app/services/protocols/` | Port (Protocol、1 Protocol 1 file) |
| `app/services/adapters/` | 具象実装 (KServe / Meilisearch / Pub/Sub / BQ / Gemini / BQML 等) |
| `app/services/fakes/` | production noop / in-memory 実装 (test stub は `tests/fakes/` 別) |
| `app/services/{search,feedback,rag,model_metrics}_service.py` | Service 層 (Port のみ import、adapter 直 import 禁止) |
| `app/schemas/` | Pydantic request / response schema |
| `app/templates/` `app/static/` | operator UI (Jinja2 + 静的アセット、`/ui/` 配下に閉じ込め) |

## 標準命名

### `ml/`

`ml/` は **機能別** を正とします。

```text
ml/
├── common/
├── data/
├── training/
├── evaluation/
├── registry/
├── serving/
├── streaming/
└── experiment/
```

- `adapters` `ports` `core` は `ml/` 直下ではなく、必要なら各機能ディレクトリの内側で使うのが標準です

### `pipeline/`

`pipeline/` は **`<verb>_job`** を正とします。

```text
pipeline/
├── data_job/
├── training_job/
├── evaluation_job/
├── batch_serving_job/
└── workflow/
```

- `train_job` は `training_job` に寄せる
- `predict_job` は `batch_serving_job` に寄せる
- `seed_job` は `data_job` に寄せる
- Phase 3 のレイヤー構成は `pipeline/*_job/` の内側に閉じ込めるのが標準です

### `scripts/`

`scripts/` は Phase 4-7 で確立した構成を正本にします。

```text
scripts/
├── _common.py
├── setup/
├── deploy/
├── ops/
├── ci/
├── sql/
├── bqml/
├── dev/                    # deprecated wrapper
└── local/                  # deprecated wrapper
```

- `Make target = 公開 API`
- `scripts/setup` `scripts/deploy` `scripts/ops` = canonical
- `scripts/dev` `scripts/local` は移行期間中だけ残る互換 wrapper
- `core.py` は将来的に `_common.py` へ寄せる対象です

### `docs/`

`docs/` の基本番号は固定です。

```text
docs/
├── 01_仕様と設計.md
├── 02_移行ロードマップ.md
├── 03_実装カタログ.md
├── 04_運用.md
├── architecture/
├── decisions/
└── archive/
```

- `01` `03` `04` は全 Phase の中核 docs
- `02` は前 Phase からの差分説明が必要な Phase のみ
- 一時的な refactor plan は完了後 `archive/` へ移すのが標準です

### `tests/`

標準は次の三層です。

```text
tests/
├── unit/
├── integration/
└── e2e/
```

- package 直下の `app/tests` `common/tests` は教材としては例外形です
- 可能なら `tests/` 集約に寄せるのが標準です

## 各要素の役割と実装形態

旧 `docs/パイプラインとジョブ.md` を統合 (`docs/archive/パイプラインとジョブ.md` に履歴あり)。
Cloud Run Job / Service という実装媒体は Phase ごとに変わる (Phase 4-6 = Cloud Run、Phase 7 = GKE Deployment + KServe InferenceService + Vertex AI Pipelines) ため、列名は **「実装形態」** に一般化している。

| 要素 | 目的 | 役割 | 実装形態 (Phase 7 基準) | 性質 |
|---|---|---|---|---|
| `infra` | MLOps 全体が動作する土台を用意する | クラウド基盤、DB、Storage、IAM、Scheduler、ネットワーク等 | Terraform / Helm / k8s manifest (基盤) | パイプラインを「載せる」基盤 |
| `ml/data` + `pipeline/data_job` | 学習・評価・推論で使えるデータを整備する | データ収集、加工、前処理、特徴量生成 | Vertex AI Pipelines (Phase 5 まで Cloud Run Job)。Dataform で SQL 整備 | バッチ実行で完了する処理 |
| `ml/training` + `pipeline/training_job` | モデルを作成し、再学習できる状態を保つ | 学習ジョブ、再学習、実験トラッキング | Vertex AI Pipelines + Vertex Experiments | 典型的なバッチ |
| `ml/evaluation` + `pipeline/evaluation_job` | モデル品質を判定し、本番採用可否を決める | 指標計算、比較、閾値判定、レポート生成 | Vertex AI Pipelines (offline)、API `/model/metrics` (online 簡易) | 実行完了型 |
| `ml/registry` | 採用モデルと成果物を一元管理する | モデル成果物保存、バージョン管理、production alias 管理 | Vertex Model Registry + GCS | 管理領域 (実行主体ではない) |
| `ml/serving` | モデルを実際の業務利用に供する | オンライン推論 API、batch 推論、predictor 実装 | KServe InferenceService (encoder / reranker)、Vertex CPR (`/explain` Pod)、`pipeline/batch_serving_job` (batch) | online = Service 常駐、batch = Job |
| `app/` | ユーザや業務システムに機能を提供する | 業務 API (`/search` `/rag` `/feedback`)、operator UI (`/ui/`)、retrain orchestration | GKE Deployment (FastAPI) | リクエスト待受型 (常駐) |
| `monitoring/` + `infra/terraform/modules/{monitoring,slo}` | 障害・劣化・異常を継続監視する | ジョブ失敗、API 異常、性能劣化、データドリフト、ログ・メトリクス監視 | Cloud Monitoring + GMP + Scheduled Query (横断機能) | 補助的に Job / Service を使うことはあるが横断レイヤー |
| `pipeline/workflow` | 上記 job 群の orchestration | KFP compile / Cloud Function trigger / PipelineJobSchedule | Vertex AI Pipelines + Eventarc + Cloud Scheduler | 複数 job のつなぎ |
| `ml/streaming` (Phase 6 T2 追加) | リアルタイム集計 (CTR 等) | ranking_log → hourly window CTR 集計 | Apache Beam → Dataflow Flex Template Job | streaming 型 |

### 一言でまとめると

- **infra** = 土台
- **data** = 材料準備
- **training** = モデル作成
- **evaluation** = 品質判定
- **registry** = 採用モデル管理
- **serving** = 推論提供
- **app** = 利用者接点 (API + operator UI)
- **monitoring** = 全体監視
- **streaming** = リアルタイム集計 (Phase 6+)

### Phase ごとの実装形態の段差

| 要素 | Phase 1-3 | Phase 4 | Phase 5 | Phase 6 | Phase 7 |
|---|---|---|---|---|---|
| `app` (online API) | Docker Compose `uvicorn` | Cloud Run Service | 同左 | 同左 | **GKE Deployment** |
| `ml/serving` (推論) | local | local + Cloud Run Service | Vertex AI Endpoint | 同左 | **KServe InferenceService** + Vertex CPR (`/explain`) |
| `pipeline/*_job` | local script | Cloud Run Jobs | Vertex AI Pipelines | 同左 | 同左 (継承) |
| `ml/streaming` | — | — | — | **Dataflow Flex Template** (Phase 6 T2) | 同左 |
| `infra` | Docker Compose | Terraform + Cloud Run | + Vertex AI primitives | + BQML / Gemini / SLO | + GKE Autopilot + KServe Helm + manifests |

中核思想 (Port-Adapter / 機能別 dir / `<verb>_job` / hybrid 検索 3 段構成) は phase を跨いで不変、実装形態だけが段階的に差し替わる。

## Phase ごとの差分

### Phase 1

パス: `1/study-ml-foundations`

特徴:
- `app/` と `common/` を持たない
- ML / pipeline 基礎を主題にしている
- `scripts/core.py` という初期形が残る

主要フォルダ:

```text
1/study-ml-foundations/
├── ml/
├── pipeline/
├── scripts/
├── infra/
├── env/
├── docs/
├── tests/
├── artifacts/
└── logs/
```

注目点:
- `ml/common` `ml/data` `ml/training` `ml/evaluation` `ml/registry`
- `pipeline/data_job` `pipeline/training_job` `pipeline/evaluation_job` `pipeline/batch_serving_job`
- `scripts/core.py` は後続 phase の `_common.py` に相当する初期形

### Phase 2

パス: `2/study-ml-app-pipeline`

特徴:
- `app/` と `common/` が入る
- `ml/` が `adapters` `ports` `core` で切られており、他 Phase と比べると例外形
- `pipeline/` は `data_job` `training_job` `batch_serving_job` を持つが、`evaluation_job` と `workflow` は未整備

主要フォルダ:

```text
2/study-ml-app-pipeline/
├── app/
├── common/
├── ml/
├── pipeline/
├── scripts/
├── env/
├── infra/
├── docs/
├── tests/
└── tools/
```

注目点:
- `app/api` `app/services` `app/schemas`
- `common/config.py` `common/logging.py`
- `ml/adapters` `ml/ports` `ml/core` は将来 `ml/<機能>/...` に寄せる対象
- `pipeline/data_job` `training_job` `batch_serving_job` は Phase 1 由来のジョブ構成を引き継いだ移行途中の形

### Phase 3

パス: `3/study-hybrid-search-local`

特徴:
- ローカル hybrid search の完成形
- `ml/` はかなり標準形に近い
- `pipeline/` はレイヤー構造が前面に出ており、他 Phase と比べると外形が揃っていない

主要フォルダ:

```text
3/study-hybrid-search-local/
├── app/
├── common/
├── definitions/
├── ml/
├── pipeline/
├── scripts/
├── infra/
├── env/
├── docs/
├── tests/
└── tools/
```

注目点:
- `ml/common` `ml/data` `ml/training` `ml/evaluation` `ml/registry` `ml/serving`
- `pipeline/batch_serving_job/{adapters,application,repositories,services}` と `pipeline/batch/{features,evaluation,maintenance}`
- `scripts/ci` `scripts/dev` `scripts/local`
- `docs/リランキング最適化ロジック.md` は長期的には `docs/archive/` 側へ整理したいタイプのメモ

### Phase 4

パス: `4/study-hybrid-search-gcp`

特徴:
- GCP への移行 phase
- `scripts/setup` `scripts/deploy` `scripts/ops` を canonical として読む
- `scripts/dev` `scripts/local` は互換層

主要フォルダ:

```text
4/study-hybrid-search-gcp/
├── .github/
├── app/
├── common/
├── ml/
├── pipeline/
├── scripts/
├── infra/
├── monitoring/
├── env/
├── docs/
├── tests/
└── logs/
```

注目点:
- `scripts/_common.py`
- `scripts/setup/` = tf / setup / deploy-all / destroy-all
- `scripts/deploy/` = deploy / monitor / checker
- `scripts/ops/` = livez / search / ranking / feedback / report
- `scripts/sql/` = SQL 正本
- `monitoring/validate_feature_skew.sql`

### Phase 5

パス: `5/study-hybrid-search-vertex`

特徴:
- Vertex AI 移行
- `dist/pipelines/` が入る
- `scripts/ops/sync_meili.py` `register_model.py` など運用補助が増える

主要フォルダ:

```text
5/study-hybrid-search-vertex/
├── .github/
├── app/
├── ml/
├── pipeline/
├── scripts/
├── infra/
├── monitoring/
├── env/
├── docs/
├── tests/
├── dist/
└── logs/
```

注目点:
- `pipeline/workflow`
- `scripts/setup/setup_encoder_endpoint.py`
- `scripts/setup/setup_model_monitoring.py`
- `scripts/setup/create_schedule.py`
- `scripts/setup/print_github_variables.py`
- `scripts/setup/upload_encoder_assets.py`
- `scripts/deploy/api_local.py`
- `scripts/deploy/training_job_local.py`
- `scripts/ops/sync_meili.py`
- `scripts/ops/register_model.py`

### Phase 6

パス: `6/study-hybrid-search-pmle`

特徴:
- PMLE / BQML / enrichment が加わる
- `ml/streaming` が入る
- `scripts/bqml` `scripts/enrichment` が入る

主要フォルダ:

```text
6/study-hybrid-search-pmle/
├── .github/
├── app/
├── ml/
├── pipeline/
├── scripts/
├── infra/
├── monitoring/
├── env/
├── docs/
├── tests/
├── dist/
└── logs/
```

注目点:
- `ml/streaming`
- `scripts/bqml/train_popularity.py`
- `scripts/setup/setup_reranker_endpoint.py`
- `scripts/ops/slo_status.py`
- `scripts/ops/promote.py`

### Phase 7

パス: `7/study-hybrid-search-gke`

特徴:
- GKE / KServe serving に差し替え (Phase 6 PMLE 8 技術は完全継承)
- `infra/manifests/` が加わる (`search-api/` + `kserve/` の k8s manifest 群)
- `scripts/deploy/api_gke.py` `kserve_models.py` が入る
- `app/` の **DI 全面整備** (Port-Adapter / fakes 分離 / Service 層抽出 / Handler 分割)
- `app/composition_root.py` + `app/container/{infra,search,ml,_optional_adapter}.py` + `app/observability.py` で DI 配線を集約
- `infra/run/services/{search_api,encoder,reranker}/` で Dockerfile + cloudbuild.yaml を **co-located**

主要フォルダ:

```text
7/study-hybrid-search-gke/
├── .github/
├── app/
├── ml/
├── pipeline/
├── scripts/
├── infra/
├── monitoring/
├── env/
├── docs/
└── tests/
```

注目点:
- `infra/manifests/{search-api,kserve}/` — Deployment / Service / Gateway / HPA / IAP Policy / NetworkPolicy / PodMonitoring + InferenceService
- `infra/run/services/<svc>/` — Dockerfile + cloudbuild.yaml が svc ごとに co-located
- `scripts/deploy/api_gke.py` (Cloud Build → AR → `kubectl set image`)
- `scripts/deploy/kserve_models.py` (Vertex Model Registry production alias → InferenceService の `storageUri` patch)
- `scripts/bqml/` (Phase 6 から継承)
- `scripts/ops/slo_status.py` `scripts/ops/vertex_*.py` (Vertex 機能別検証)
- `app/composition_root.py` + `app/container/` + `app/observability.py` (DI seam)
- `app/services/{protocols,adapters,fakes}/` — 1 Protocol 1 file、1 Adapter 1 file
- `tests/fakes/` — unit test 共通 stub (production noop は `app/services/fakes/` 別)

## 教材として見るときの読み替え

### 「存在しない」は欠陥ではなく差分

- Phase 1 に `app/` がないのは、まだ API を扱わないから
- Phase 5 以前に `scripts/bqml/` がないのは、まだ BQML を導入していないから
- Phase 7 に `infra/manifests/` があるのは、GKE / KServe を扱うから

### 今後そろえると効果が大きい項目

1. `pipeline/` の job 命名を `<verb>_job` に統一
2. `ml/` を機能別で統一し、`adapters` `ports` `core` は内側へ寄せる
3. 各 Phase の `docs/01_仕様と設計.md` `03_実装カタログ.md` `04_運用.md` を必須の基本セットとして維持

## 関連文書

- [命名規約.md](命名規約.md) — 同階層 (命名規約)
- [Makefile規約.md](Makefile規約.md) — 同階層 (Make Command Matrix)
- [スクリプト規約.md](スクリプト規約.md) — 同階層 (scripts 命名標準)
- [Docker配置規約.md](Docker配置規約.md) — 同階層 (Docker 配置)
- [../README.md](../README.md) — ルート docs README
- [../../README.md](../../README.md) — リポルート README
