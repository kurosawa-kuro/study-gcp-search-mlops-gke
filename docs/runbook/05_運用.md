# 05_運用

最小運用手順のみを定義する。

## 1. 正本フロー

```bash
make deploy-all
make run-all-core
make destroy-all
```

## 2. 初回準備

```bash
make doctor
make sync
gcloud auth application-default login
gcloud config set project mlops-dev-a
```

`env/config/setting.yaml` の `project_id` / `region` / `github_repo` / `oncall_email` を確認。

## 3. 日常コマンド

### 3.1 構築

```bash
make deploy-all
make deploy-api
make deploy-kserve-models
```

### 3.2 検証

```bash
make run-all-core
make ops-livez
make ops-search-components
make ops-accuracy-report
```

### 3.3 破棄

```bash
make destroy-all
```

## 4. インシデント即応

### 4.1 `destroy-all` がバケット残存で失敗

```bash
gcloud storage rm --recursive "gs://<bucket-name>" || true
make destroy-all
```

### 4.2 `ops-search-components` が失敗

```bash
make ops-livez
make sync-elasticsearch
make ops-vertex-vector-search-smoke
make ops-vertex-feature-group
make ops-search-components
```

### 4.3 学習/評価が失敗

```bash
make ops-train-now
make ops-train-wait
make ops-accuracy-report
```

## 5. コスト運用

- セッション終了時は必ず `make destroy-all`
- 長時間放置は Composer / GKE / VVS / FOS のコストが支配

## 6. 参照

- 仕様: `docs/architecture/01_仕様と設計.md`
- 実装所在: `docs/architecture/03_実装カタログ.md`
- 進行管理: `docs/tasks/TASKS.md` / `docs/tasks/TASKS_ROADMAP.md`

# 05_運用

本書は「日常運用で実行する最小手順」のみを記載する。  
詳細背景・長文ログは `docs/architecture/03_実装カタログ.md` と `docs/tasks/` を参照。

---

## 1. 運用の正本フロー（PDCA）

```bash
make deploy-all
make run-all-core
make destroy-all
```

- `deploy-all`: 構築・seed・同期・API反映
- `run-all-core`: canonical 検証
- `destroy-all`: no-prompt teardown

---

## 2. 事前準備（初回のみ）

```bash
make doctor
make sync
gcloud auth application-default login
gcloud config set project mlops-dev-a
```

`env/config/setting.yaml` の最低確認項目:
- `project_id`
- `region`
- `github_repo`
- `oncall_email`

---

## 3. 日常コマンド

### 3.1 構築・反映

```bash
make deploy-all
make deploy-api
make deploy-kserve-models
```

### 3.2 検証

```bash
make run-all-core
make ops-livez
make ops-search
make ops-search-components
make ops-accuracy-report
```

### 3.3 破棄

```bash
make destroy-all
```

---

## 4. 最小運用チェック（毎日）

1. `make ops-livez` が 200
2. `make ops-search-components` が all non-zero
3. `make ops-accuracy-report` で gate 条件を満たす
4. 異常時は `destroy-all` まで含めて再現性確認

---

## 5. 代表インシデントと即応

### 5.1 `destroy-all` 失敗（バケット残存）

症状:
- `force_destroy` なしバケット削除エラー

対応:
```bash
gcloud storage rm --recursive "gs://<bucket-name>" || true
make destroy-all
```

### 5.2 `search-api` がダミーイメージで起動

症状:
- `gcr.io/cloudrun/hello` で起動

対応:
```bash
make deploy-api
kubectl -n search get deploy/search-api -o jsonpath='{.spec.template.spec.containers[0].image}'
```

### 5.3 `ops-search-components` が失敗

確認順:
1. `make ops-livez`
2. `make sync-elasticsearch`
3. `make ops-vertex-vector-search-smoke`
4. `make ops-vertex-feature-group`
5. `make ops-search-components`

### 5.4 学習/精度ゲート不達

```bash
make ops-train-now
make ops-train-wait
make ops-accuracy-report
```

---

## 6. コスト運用（要点）

- 使わない時間は必ず `make destroy-all`
- 長時間放置は Composer / GKE / VVS / FOS が主要コスト
- 検証はまとめて実行し、環境稼働時間を短くする

---

## 7. 参照先

- 仕様: `docs/architecture/01_仕様と設計.md`
- 実装所在: `docs/architecture/03_実装カタログ.md`
- 短期タスク: `docs/tasks/TASKS.md`
- 長期ロードマップ: `docs/tasks/TASKS_ROADMAP.md`

# 05_運用

> 注記: 移行履歴の説明で旧パスが一部出現する。運用時の参照は現行構成 (`app/`, `ml/{common,data,training,serving,streaming,evaluation,registry}/`, `pipeline/`, `scripts/{deploy,setup,ops,local,...}/`) を優先する。

hybrid-search-gke MLOps パイプラインの **PDCA ループ (deploy-all / run-all / destroy-all) / 環境構築の個別 STEP / 定常運用 / インシデント対応 / ロールバック** を扱う。**学習目的の PDCA 高速回転** を最優先のユースケースとし、`make deploy-all` で全 provisioning + search-api の GKE rollout を一発で済ませ、`make run-all` で検証を一連実行し、`make destroy-all` でゼロに戻す運用を §1 で先に押さえる。granular な STEP 1–17 は §2 に残し、PDCA 標準フローで詰まった時だけ参照する位置付け。

> **本書のスコープ — 「やる」**: deploy / run / destroy / 監視 / インシデント対応の手順。
> **検証 (「OK か判定する」) は別建て**: [`04_検証.md`](./04_検証.md) (ローカル検証 L1-L5 / GCP ゲート G1-G6 / 退化検知ベースライン / 一過性エラー recovery / 依頼プロンプト例)。
> 順序は **`04_検証.md` → 本書**: 変更後はまず `04_検証.md §2` (ローカル) で弾けるものを弾き、`make deploy-all` 後に `04_検証.md §3` の G3 (3 種が all non-zero) を満たしたものだけが §3 定常運用に進む資格を持つ。

詳細設計は [`../architecture/01_仕様と設計.md`](../architecture/01_仕様と設計.md) と [`../tasks/TASKS_ROADMAP.md`](../tasks/TASKS_ROADMAP.md)、実装カタログは [`../architecture/03_実装カタログ.md`](../architecture/03_実装カタログ.md) を参照。

> `PROJECT_ID` / `REGION` / `API_SERVICE` / `TRAINING_JOB` / `ARTIFACT_REPO` / `GITHUB_REPO` / `ONCALL_EMAIL` の値は **`env/config/setting.yaml`** が single source of truth。Python (`scripts/_common.py::DEFAULTS`) と Make (awk で読み出し) の両方がここから読む。別プロジェクトを触る場合は yaml を編集するか `PROJECT_ID=xxx REGION=yyy make <target>` で env 上書き。

---

## 1. PDCA メインフロー (`make deploy-all` → `make run-all` → `make destroy-all`)

### 1.0 設計意図

**学習用 dev project (`mlops-dev-a`) で PDCA を最速回転させる**。本リポは試行錯誤の場であり、確認プロンプト / 古いイメージの混入 / 半端な手順は全部排除している:

- **`make deploy-all`** で **provisioning ゼロから search-api の GKE rollout 完了まで一発**。途中で確認は出ない。encoder / reranker の KServe InferenceService は Model Registry に `production` alias が付いてから `make deploy-kserve-models` で storageUri 更新 (deploy-all からは外してある)。
- **`make run-all`** (= `make run-all-core`) で **疎通・検索・学習トリガ・日次観測までの検証を一連実行**。検証漏れを減らす。
- **`make destroy-all`** は **プロンプトなし** で全 Terraform リソースを破壊する (BQ table 10 件 + online serving view 1 件、計 11 件の `deletion_protection` も自動で剥がす)。
- イメージは **immutable tag** (`<sha>-<epoch>`) で毎回新規 push し、その tag を `kubectl set image deployment/search-api` で deploy する。tag 削除先行は行わない (`ImagePullBackOff` レース回避)。
- tfstate バケットと API 有効化は teardown 後も残す → `make deploy-all` を再実行すれば数分で戻る。

```
       ┌────────────────────────┐
   ┌──>│   make deploy-all      │  (約 30-40 分: VVS attach 長待機を含む full provisioning + rollout)
   │   └───────────┬────────────┘
   │               ↓
   │       実 GCP で実験 / 観測 / コード修正
   │               ↓
   │   ┌────────────────────────┐
   │   │   make ops-* で疎通    │  (livez / search / ranking / feedback / label-seed)
   │   └───────────┬────────────┘
   │               ↓
   │     コード変更 → 再度 make deploy-all (image は immutable tag で再 rollout)
   │               │
   │               ↓
   │   ┌────────────────────────┐
   └───┤   make destroy-all     │  (約 3-5 分: protect 解除 + terraform destroy)
       └────────────────────────┘
```

### 1.1 1 度だけ済ませる前提 (PDCA 周回前の準備)

ここは PDCA ループの**外側**。マシン交換時 / 新規メンバー初回のみ実行する。

```bash
# (a) 前提ツール — uv / terraform / gcloud / make / jq (詳細は §2 STEP 1)
make doctor                                        # 全行 OK で次へ

# (b) Python 依存
make sync

# (c) GCP 認証 + project 固定
gcloud auth application-default login
gcloud config set project mlops-dev-a              # = env/config/setting.yaml::project_id

# (d) env/config/setting.yaml の確認 (要なら編集)
cat env/config/setting.yaml
# project_id / region / github_repo / oncall_email 等が自分の環境で正しいか
```

`make doctor` は `WARN VIRTUAL_ENV=...` を出すことがある (外側で別 venv を `source` 済の場合)。害はないが、混乱回避のため `deactivate` してから make を叩くのを推奨。

### 1.2 PDCA 1 サイクル (典型操作)

```bash
# (1) 立てる
make deploy-all
#   → tf-bootstrap → tf-init → _recover_wif_state → sync-dataform
#   → tf-plan → terraform apply(stage1 core infra → kube-ready wait → stage2 full graph)
#   → seed-lgbm-model → seed-test → sync-elasticsearch → (sync-synonyms; opt-in) → backfill-vvs → trigger-fv-sync
#   → apply-manifests → overlay-configmap → scripts.deploy.api_gke
#   初回は 30-40 分を見込む。2026-05-02 実測では VVS deployed index attach が 26m21s。
#   ※ encoder/reranker の KServe InferenceService storageUri は別途 `make deploy-kserve-models`
#     (Vertex Model Registry に `production` alias が付いた後)。

# (2) 検証を一括実行
make run-all           # = run-all-core + リアルタイム監視 (ops-run-all-monitor)
#   → check-layers → seed-test → sync-elasticsearch → ops-train-now → ops-train-wait
#   → ops-livez/search/search-components → ops-vertex-vector-search-smoke
#   → ops-vertex-feature-group → feedback/ranking
#   → ops-label-seed → ops-daily → ops-accuracy-report
#   疎通 + 主要運用チェックをまとめて確認

# (3) 試したい変更を加える (コード / Terraform / Dataform / manifests)
$EDITOR ...

# (4) 再 rollout + 再検証
make deploy-all
make run-all
#   tfstate / バケット / API 有効化はそのまま、差分だけ apply。image は毎回 immutable tag を作って
#   `kubectl set image` で rollout。

# (5) PDCA セッション終了 → 全部消してコスト止める
make destroy-all
#   プロンプトなし。約 3-5 分で terraform destroy 完了。
#   tfstate バケットは残る → 翌日 (1) から再開可能。
```

### 1.3 `make deploy-all` の内訳 (15 step)

`scripts/setup/deploy_all.py` が以下を順に Python module 直 import で実行する。**個別 step を単独 make target で叩いても等価**:

| # | 中身 | 個別 target | 所要時間 (目安) |
|---|---|---|---|
| 1 | API 有効化 + tfstate バケット作成 (冪等) | `make tf-bootstrap` | ~30s (初回)、再実行 ~5s |
| 2 | tfstate バケット preflight + `terraform init` | `make tf-init` | ~10s |
| 3 | WIF pool / provider の soft-delete 復元 (直前に destroy した場合のみ必要、冪等) | (組込、Python) | <10s |
| 4 | `env/config/setting.yaml` → `pipeline/data_job/dataform/workflow_settings.yaml` 生成 | `make sync-dataform-config` | <1s |
| 5 | `terraform plan -out=tfplan` (vars は setting.yaml から) | `make tf-plan` | ~20s |
| 6 | `terraform apply -auto-approve` を 2 段実行: stage1 で core infra (`module.gke` など) → kubeconfig refresh + Kubernetes API ready wait → stage2 で full graph (`module.kserve` 含む) | (組込) | ~10-30 分 (初回、VVS attach が支配) / ~2-5 分 (再 apply) |
| 7 | synthetic reranker model を `gs://.../lgbm/latest/` へ seed | `make seed-lgbm-model` | <10s |
| 8 | PDCA smoke 用 5 物件を BigQuery へ seed | `make seed-test` | ~10s |
| 9 | `feature_mart.properties_cleaned` を Elasticsearch `properties` index へ sync | `make sync-elasticsearch` | ~10-30s |
| 9-bis | (SYN-1, **opt-in**) 同義語辞書 `definitions/synonyms/real_estate_ja.yaml` を Cloud Memorystore `phase7-synonym` へ投入。`enable_redis_synonym=false` (default) では skip | `make sync-synonyms` | ~5-10s |
| 10 | `feature_mart.property_embeddings` を Vertex Vector Search index へ backfill | (組込、Python) | ~10-30s |
| 11 | Feature View manual sync を起動し完了まで待機 | (組込、Python) | 数十秒〜数分 |
| 12 | `kubectl apply -k infra/manifests/` (Gateway / Deployment / ISVC / policies) | `make apply-manifests` | ~10s |
| 13 | ConfigMap overlay (`elasticsearch_url` + VVS/FOS outputs 注入 + canonical backend auto-flip) | (組込、Python) | <10s |
| 14 | Composer DAG GCS upload (`pipeline/dags/*.py` → `composer_dag_bucket`)。`enable_composer=false` なら no-op | `make composer-deploy-dags` | ~2-3 分 |
| 15 | search-api: Cloud Build → Artifact Registry → `kubectl set image deployment/search-api` (immutable tag) | `make deploy-api` | ~5-7 分 |

step 6 の `terraform apply` で **Cloud Composer 環境** (`enable_composer=true` 時) も同時に立ち上がる。Composer 環境の作成は GCP 側で 15-25 min かかるため、初回 `make deploy-all` の所要時間は **50-65 min** (従来 30-40 min + Composer 創出分)。

**ログをファイルへリダイレクトして実行している場合**（検証・エージェント実行など）、別シェルで進捗を追う:

```bash
tail -f logs/verification/deploy-all.latest.log
```

`helm_release.cert_manager: Still creating...` の経過秒が増える、`deploy-all  step N/15` が進む、などが見えれば **停止ではなく処理中**であることが多い。

**step 6 補足（Vertex Feature Store・2026-05）**: stage1 の前に `scripts/domain/gcp/vertex_feature_store_wait.py` が Feature Group / Feature Online Store の **名前解放** を Vertex REST list API で待つ（`destroy-all` 直後は非同期削除が未完で **409** になり得る）。stage1 apply は 409 / “being deleted” 検知時に **自動リトライ**（`TF_APPLY_STAGE1_MAX_ATTEMPTS` 既定 5、`TF_APPLY_STAGE1_RETRY_SLEEP_SEC` 既定 120）。**V6 e2e** は [`tests/e2e/README.md`](../../tests/e2e/README.md) のとおり **既存 deploy 上の acceptance** と **`destroy-all`→`deploy-all` の full recreate** をファイル分割済み。

**deploy-all に含まれない** (手動で叩く):

| target | タイミング | 用途 |
|---|---|---|
| `make deploy-kserve-models` | Vertex Model Registry に `production` alias 付与後 | `property-encoder` / `property-reranker` InferenceService の `storageUri` を `kubectl patch` |
| `make ops-composer-trigger DAG=<dag_id>` | Composer DAG を手動 smoke trigger したい時 | `gcloud composer environments run ... dags trigger -- <dag_id>` |
| `make ops-composer-list-runs DAG=<dag_id>` | DAG run の状態確認 | `gcloud composer environments run ... dags list-runs -- --dag-id <dag_id>` |
| `make ops-train-now` | run-all 内で自動。手動で再学習したいとき | Vertex AI Pipelines (`property-search-train`) を submit |
| `make ops-train-wait` | `ops-train-now` の直後 / latest run が SUCCEEDED したか確認したいとき | latest `property-search-train` run を poll して `SUCCEEDED` まで待機 |
| `make ops-pipeline-run TARGET=embed` | raw.properties が差分更新された時 | `property-search-embed` pipeline を submit → `feature_mart.property_embeddings` |

詰まった step は §2 の STEP に対応 (1↔STEP 8、2↔STEP 9 head、5-6↔STEP 9 後半、7↔ `scripts.deploy.api_gke`)。

`deploy-all` 完了時点で canonical lane の前提が揃う:

- lexical: `sync-elasticsearch` 済みなので `ops-search-components` が `lexical=0` で止まりにくい
- semantic: `backfill-vvs` 済みなので `ops-vertex-vector-search-smoke` が `0 neighbors` で止まりにくい
- feature: `trigger-fv-sync` 済みなので `ops-vertex-feature-group` が seed 未同期 404 で止まりにくい

### 1.4 `make destroy-all` の内訳

`scripts/setup/destroy_all.py`:

1. `terraform apply -auto-approve -var=enable_deletion_protection=false -target=<10 BQ tables + 1 GKE cluster>` — 計 10 table (`mlops.training_runs` / `mlops.search_logs` / `mlops.ranking_log` / `mlops.feedback_events` / `mlops.validation_results` / `feature_mart.property_features_daily` / `feature_mart.property_features_online_latest` / `feature_mart.property_embeddings` / `mlops.model_monitoring_alerts` / `mlops.ranking_log_hourly_ctr`) + GKE cluster の `deletion_protection` 属性だけを state-flip。`-target` が load-bearing — 半 destroy 状態で SA recreate が fail するのを回避するため、apply 範囲を保護対象に限定する。新しい protected table を `infra/terraform/modules/data/main.tf` に追加したら、`scripts/setup/destroy_all.py::PROTECTED_TARGETS` にも同じアドレスを追記する。
2. Vertex AI Endpoint (`property-encoder-endpoint` / `property-reranker-endpoint`) の DeployedModel を undeploy (canonical 構成では KServe に serving を移譲しており、Vertex Endpoint は shell リソースのみ。残留 DeployedModel があると destroy が詰まるため事前に `scripts/setup/destroy_all.py::VERTEX_ENDPOINTS` 経由で消す)。
3. `force_destroy=false` な GCS バケット (`-models` / `-artifacts` / `-pipeline-root`) を `gcloud storage rm --recursive` で空に。
4. `terraform -chdir=infra/terraform/environments/dev destroy -auto-approve -var=enable_deletion_protection=false -var=github_repo=... -var=oncall_email=...` (vars は env > setting.yaml)。

**所要時間**:
- `enable_composer=false` (Stage 1/2 default): **~3-5 分** (state-flip apply ~30s + endpoint undeploy + bucket wipe + destroy ~2-4 分)
- `enable_composer=true` (Stage 3 以降): **~12-17 分** (上記 + Composer 環境 destroy ~8-12 分。backing GKE cluster + Cloud SQL の解体が支配)

**残るもの** (再 deploy のため意図的に保持):
- tfstate バケット `gs://mlops-dev-a-tfstate`
- API enablement (リソース不在時はゼロ課金)
- **Vertex Vector Search Index と Index Endpoint** (2026-05-03 追加、`docs/tasks/TASKS_ROADMAP.md §4.9`) — undeployed Index と空の Endpoint は **無料** で残せる仕様を活用。Index build に 5-15 min、Endpoint 作成 + DNS propagation に数分かかるため、PDCA cycle ごとに作り直すと deploy-all 全体の短縮効果が消える。`module.vector_search` の Index / Endpoint には `lifecycle.prevent_destroy = true` を設定し、`scripts/setup/destroy_all.py::PERSISTENT_VVS_RESOURCES` で destroy 対象から除外する。**deployed_index (replica = 課金 resource) は通常通り destroy** され、再 deploy 時に新規 attach (~10-15 min) する。

deploy-all 所要時間の目安:
- 初回 (Index / Endpoint 新規 build) — 27-30 min (VVS attach 27 min + 他)
- 2 回目以降 (Index / Endpoint 永続化済) — **deployed_index attach のみで ~10-15 min** (60% 短縮)

完全クリーンスレートにしたい場合 (Index 再 build 込み):
- `module.vector_search` の `prevent_destroy = true` を一時的に外す → `terraform destroy -target=module.vector_search` を手動で叩く → コードを元に戻す
- 加えて destroy 後に `gcloud storage rm --recursive gs://mlops-dev-a-tfstate` を手動実行

### 1.4-emergency 緊急 kill switch (`make destroy-all` が hang した時の最終手段)

**経緯** (2026-05-03 incident、`docs/tasks/TASKS_ROADMAP.md §4.9` 参照): `lifecycle.prevent_destroy = true` で Index / Endpoint を保護していたところ、依存閉包内で touch されて `Error: Instance cannot be destroyed` で `make destroy-all` step `[6/6]` 本体 destroy が **2 連続で hang** し、Composer (RUNNING) + GKE (RUNNING) + Cloud Run + Feature Online Store が課金継続。`prevent_destroy` は state rm pattern (§4.9 K fix) に置換済だが、新たな block パターンで詰まった時の **escape hatch** を本節に明記する。

#### 寝る前 / 帰宅前の緊急 cleanup (主要課金を数分で止める)

```bash
# Composer (DCU-hour 課金が最大、最優先): backing GKE + Cloud SQL の解体に 8-12 min
gcloud composer environments delete hybrid-search-orchestrator \
  --location=asia-northeast1 --project=mlops-dev-a --async --quiet

# GKE Autopilot (1h ¥40 程度、放置で月額化注意): control plane が即停止
gcloud container clusters delete hybrid-search \
  --region=asia-northeast1 --project=mlops-dev-a --async --quiet

# Cloud Run を運用している環境のみ (このリポ標準構成では通常不要)
gcloud run services delete <service-name> \
  --region=asia-northeast1 --project=mlops-dev-a --quiet

# Feature Online Store (Optimized は per-node 課金): destroy 順序依存で残ることがある
gcloud ai feature-online-stores delete property_features \
  --region=asia-northeast1 --project=mlops-dev-a --async --quiet 2>/dev/null || true
```

#### 緊急 cleanup 後の tfstate 整合性回復

`gcloud delete --async` で GCP 側を直接消すと、tfstate には orphan entry が残る (= terraform から見ると「あるはず」のリソースが GCP 側で消えた状態)。**重要**: 次回 `make deploy-all` を走らせる前に **必ず `make state-recover` で existing GCP resources を state へ import する**。これを忘れると次の罠に落ちる:

##### ⚠ bare `state rm` の罠 (2026-05-03 incident)

緊急 cleanup で `gcloud delete --async` を Composer / GKE のみに叩いた場合でも、**他の resources (IAM SA / BigQuery / Pub/Sub / Cloud Function / Eventarc) は GCP 側に残置**している。それを知らずに「state list 全件を state rm で 0 にする」と:

- 例: `module.composer` / `module.gke` だけ消しても他 module は残る

- 次回 `make deploy-all` の stage1 tf-apply が **`Error: alreadyExists`** で多数 fail
- 特に **IAM SA は soft-delete 30 日 window** があるため `gcloud iam service-accounts delete` で消しても 30 日間は同名の SA が作れず、deploy-all が完全 block する

##### 正しい cleanup 手順 (state recovery → state rm の順)

```bash
cd <phase7-canonical-root>

# 1. まず existing GCP resources を state へ import で取り込む (alreadyExists 罠の予防)
make state-recover  # = uv run python -m scripts.domain.gcp.state_recovery
# IAM SA / BQ dataset+table / Pub/Sub topic+sub / Cloud Function / Eventarc / Cloud Run
# の 6 種を gcloud list して terraform import で state へ吸収。idempotent。

# 2. state list を確認 (本当に削除された resources のみ orphan のはず)
terraform -chdir=infra/terraform/environments/dev state list | wc -l

# 3. それでも残っている orphan (= GCP 側で確実に消えた resource) を絞って state rm
#    どの resource が消えているかは gcloud で確認:
for addr in $(terraform -chdir=infra/terraform/environments/dev state list | \
              grep -E "module\.(composer|gke|kserve)"); do
  # Composer/GKE/KServe の existence を gcloud で確認してから state rm 推奨
  terraform -chdir=infra/terraform/environments/dev state rm "$addr"
done

# 4. make destroy-all で残りを片付ける (state rm + import pattern が完走するはず)
make destroy-all
```

##### bare `state rm` を使わざるを得ない場合 (= state recovery で吸収しきれない場合)

`scripts/domain/gcp/state_recovery.py` の `IAM_SA_NAMES` / `BQ_TABLES` / `PUBSUB_TOPICS` 等の mapping が新規 resource を網羅していないと recovery が漏れる。その場合は `state_recovery.py` の mapping を更新するのが本筋 (contract test `test_state_recovery_iam_sa_mapping_matches_terraform` が drift 検出)。

> `cd <phase7-canonical-root>` は、現時点では `cd 7/study-hybrid-search-gke`、repo ルート昇格後は `cd <repo-root>` を意味するプレースホルダ。

#### 緊急 cleanup 後の状態確認チェックリスト

- [ ] `gcloud composer environments list --project=mlops-dev-a` が空
- [ ] `gcloud container clusters list --project=mlops-dev-a` が空
- [ ] `gcloud run services list --project=mlops-dev-a --region=asia-northeast1` が空
- [ ] `gcloud ai feature-online-stores list --project=mlops-dev-a --region=asia-northeast1` が空
- [ ] `gcloud ai indexes list --project=mlops-dev-a --region=asia-northeast1` で **Index は残っている** (永続化対象、`docs/tasks/TASKS_ROADMAP.md §4.9` 参照)
- [ ] `gcloud ai index-endpoints list --project=mlops-dev-a --region=asia-northeast1` で **Endpoint も残っている** (同上)
- [ ] `terraform state list | wc -l` が 0 (or 永続化対象 2 entries のみ)

### 1.4-bis Composer / canonical 構成 フル構成のコスト見積もり (asia-northeast1、当日 destroy 前提)

本 phase は **GCP Vertex 技術学習用 dev project** で、当日学習後に **必ず `make destroy-all`** する運用。コストはこの前提に従って計算する。

まず見るべきなのは **金額より先に、何にお金がかかるのか** である。

| 課金対象 | この phase の実体 | 何で増えるか |
|---|---|---|
| Composer 環境 | Cloud Composer Gen 3 SMALL | 環境を起動している時間。DAG を流していなくても環境が生きていれば増える |
| Kubernetes serving | GKE Autopilot 上の `search-api` / KServe `property-encoder` / `property-reranker` | Pod request と起動時間。serving を立てている間じゅう増える |
| 入口ネットワーク | Gateway / Load Balancer / 周辺ネットワーク | 公開入口を持たせている時間 |
| ベクトル serving | Vertex Vector Search deployed index | deployed index を生かしている時間 |
| online feature serving | Feature Online Store / Feature View | online serving を有効化している時間と access |
| ML 実行 | Vertex Pipelines training job | job を何回 submit したか、どれだけ実行したか |
| build / 配布 | Cloud Build / Artifact Registry | image build 回数と artifact 保持量 |
| 分析 / 観測 | BigQuery / Pub/Sub / Logging / Monitoring | scan 量、メッセージ量、ログ量、監視量 |
| lexical serving | Elasticsearch | リクエスト量とインデックス同期頻度 |

この文書では、上のうち **Composer / GKE / Gateway / Vector Search / Feature Online Store** を「環境を生かしている間の常駐系」、**Vertex Pipelines / Cloud Build / BigQuery / Pub/Sub / Logging / Monitoring** を「実行した分だけ増える従量系」として分けて考える。

- **Composer**: `ENVIRONMENT_SIZE_SMALL`。Terraform は scheduler / web_server / worker を各 `0.5cpu / 2Gi`、worker `min_count=1 max_count=3` で立てる
- **GKE / KServe**: `search-api` = `500m / 1Gi`、encoder = `1cpu / 2Gi`、reranker = `250m / 512Mi`、いずれも `minReplicas=1`。Autopilot の最小課金単位と system overhead を見込んでも、3h セッションでは中位 2 桁円台後半〜 3 桁円前半に収まりやすい
- **Vertex Vector Search**: Terraform は `min_replica_count=1`、`max_replica_count=1`、`SHARD_SIZE_SMALL`。autoscale 前提の上振れが構造的に消えているため、1 replica 常駐ぶんにレンジが狭まる
- **Feature Online Store**: Terraform は `optimized {}` を採用し、Feature View sync は `0 * * * *`。Bigtable mode ではないが、optimized online serving でも node-hour 課金の下限があるため、ここは軽くならない
- **Cloud Scheduler / Eventarc / Cloud Functions v2**: 本線ではないが smoke / 軽量代替経路として terraform 上は残る。ただし学習セッション規模なら無料枠に収まりやすく、実費はほぼゼロ〜数円

したがって、**総額を決める支配変数は「有効化した構成要素」と「destroy までの生存時間」** である。以下は「canonical full 構成を 3 時間起動し、Cloud Build 1 回、Vertex Pipelines 学習 job 1 回を流し、終了後に destroy する」前提の rough order。

**canonical full 学習 1 回 (3h 起動前提)**:

| サービス | 3h コスト (USD) | 3h コスト (JPY) |
|---|---|---|
| Composer Gen 3 SMALL | $1.90-2.65 | ¥300-420 |
| GKE Autopilot (`search-api` + KServe 2 Pod 相当) | $0.38-0.63 | ¥60-100 |
| Gateway / LB / 周辺ネットワーク | $0.12-0.24 | ¥20-40 |
| Vertex Vector Search deployed index (`min=max=1`, `SHARD_SIZE_SMALL`) | $1.19-1.44 | ¥190-230 |
| Vertex Feature Online Store (`optimized {}` + hourly sync) | $1.13-1.44 | ¥180-230 |
| Elasticsearch (query + index sync) | <$0.12 | ¥10-20 |
| Cloud Build (search-api 1 build) | $0.00-0.06 | ¥0-10 |
| Vertex Pipelines (KFP train job 1 回) | $0.10-0.18 | ¥15-30 |
| Artifact Registry | <$0.06 | ¥10 未満 |
| BigQuery / Pub/Sub / Logging / Monitoring | $0.18-0.38 | ¥30-60 |
| Cloud Functions v2 / Eventarc | ~$0.00 | ¥0 (無料枠内) |
| Cloud Scheduler | $0.00-0.06 | ¥0-10 |
| **合計 (3h、即 destroy)** | **$5.43-7.50** | **約 ¥870-1,200** |

**1 日学習セッション (当日 destroy 前提) シナリオ**:

| シナリオ | 1 日コスト |
|---|---|
| Stage 3 学習 1 回 (3h) | **~¥870-1,200** |
| 同日 2 回まわす | **~¥1,740-2,400** |
| 長尺セッション (deploy 1 回 + 8h 環境放置 + destroy) | **~¥2,000-2,500** |
| 月 10 回 (週 2-3 回学習) | **~¥8,700-12,000/月** |

**destroy 漏れリスク (参考、当日 destroy 契約下ではゼロ)**:

| 漏れシナリオ | コスト |
|---|---|
| 24h 放置 | ~¥4,000-6,000 |
| 1 週間放置 | ~¥30,000-40,000 |
| 月放置 | ~¥130,000-180,000 |

→ 読み方としては、**通常時コスト** と **destroy 漏れ時の延長コスト** を分けて扱う。学習用途では前者が意思決定の主対象で、後者は「止める理由」ではなく **自動 destroy / budget alert で機械的に潰すリスク** として扱う。

`enable_composer=false` で運用する場合 (Stage 1/2 default) は Composer 関連コストはゼロで、上記表から Composer 分を引いた **~¥570-900 / 3h** が目安になる。

### 1.5 PDCA を成立させている技術的保証

PDCA を回す上で「触るたびに違う結果」になると致命的。本リポは以下で再現性を担保:

| 機構 | 場所 | 効果 |
|---|---|---|
| `setting.yaml` を single source of truth に集約 | `env/config/setting.yaml` + `tests/integration/parity/test_dataform_workflow_settings.py` | drift を CI が検知。Make / Python / Dataform yaml が同じ値を見る |
| **immutable tag deploy** (`<sha>-<epoch>` を build→deploy で固定) | `scripts/deploy/api_gke.py` | tag 削除先行による `ImagePullBackOff` を回避しつつ、どの build を deploy したかを一意に追跡 (Cloud Build → Artifact Registry → `kubectl set image`) |
| **no-prompt** (確認プロンプトなし) | `scripts/setup/destroy_all.py` ほか | PDCA を `make` 1 発で完走、自動化を妨げない |
| placeholder image + `lifecycle.ignore_changes` | `infra/manifests/search-api/deployment.yaml` の初期 image + Deployment 側で image 差し替えを許容 | 初回 apply 時に image が無くても Deployment 作成成功、後続 `deploy-api` で real image に差し替え可 |
| WIF pool/provider の soft-delete 復元 | `scripts/setup/deploy_all.py::_recover_wif_state` | GCP は WIF pool を delete 後 30 日保持する仕様。直前に `destroy-all` した直後の `deploy-all` で 409 にならないよう undelete + state import を自動実行 |
| tfstate / API 有効化を destroy で消さない | `scripts/setup/destroy_all.py` | 翌サイクルの `deploy-all` が即座に開始可能 (初回 を毎回やらない) |
| feature parity invariant (6 ファイル lockstep) | `tests/integration/parity/test_feature_parity_*.py` | 試行錯誤中に schema が崩れて scheduled query / Vertex Feature Group が FAIL するのを CI で防ぐ |

### 1.6 PDCA で踏みやすいハマり (修正済)

`make deploy-all` を初回叩いたとき、過去に踏んで **infra/scripts 側で恒久対処済** のハマりを置いておく (再発時にここを起点に追える):

| 症状 | 原因 | 対処箇所 |
|---|---|---|
| `Cannot find metric(s) that match type = "logging.googleapis.com/user/search_api_5xx"` | log-based metric 直後 ~60s は Monitoring API の index が追いつかず alert policy が参照不可 | `infra/terraform/modules/monitoring/main.tf` の `time_sleep.wait_for_log_metric_indexing` (90s) + 両 alert policy の `depends_on` |
| `Cannot create a transfer in JURISDICTION_US when destination dataset is located in REGION_ASIA_NORTHEAST_1` | `google_bigquery_data_transfer_config.location` 既定が US で、`asia-northeast1` の dataset と不整合 | `infra/terraform/modules/monitoring/main.tf` に `location = var.region`、root から `region` を渡す |
| `Failed to find a valid credential. The field 'version_info' or 'service_account_name' must be specified.` | DTS は `version_info` (user OAuth) か `service_account_name` (SA 代理) のいずれか必須、provider 既定はどちらも付けない | DTS に `service_account_name = var.service_accounts.dataform.email`、`infra/terraform/modules/data/main.tf` に `dataform_mlops_editor` を追加 |
| `terraform apply` が `Error: Post "https://.../api": dial tcp: lookup`/ `connect: connection refused` | `kubernetes` / `helm` provider が cluster 未作成時点で activate されようとしている | 初回のみ `-target=module.iam -target=module.data -target=module.gke` で cluster を先行作成 (`infra/terraform/environments/dev/provider.tf` の 2 段階 apply コメント参照)。2 回目以降の `make deploy-all` は全体 apply でよい |
| `ImagePullBackOff` on `deployment/search-api` after `kubectl set image` | Artifact Registry への push 未完了の tag を deploy に回した (Cloud Build のレース) | `scripts/deploy/api_gke.py` は Cloud Build 完了を待ってから `kubectl set image` を叩くように組み立て済。自前で並列化しない |
| `make tf-plan` で `ONCALL_EMAIL is required` (backslash 改行で trailing whitespace 混入) | shell の line continuation 破壊 | setting.yaml が既定値を持つ + Make を 1 行で叩く |
| GKE Deployment `/healthz` probe が持続的に失敗 | `readiness_probe` が encoder 未ロード時に 503 を返す正しい挙動。起動直後のみ ~30-60s 503 を許容 | `deployment.yaml` の `startupProbe.failureThreshold` を余裕持たせる (現状 30 回 × 10s = 5 分) |
| Elasticsearch URL / index の設定漏れで search-api が 5xx | ConfigMap overlay の `elasticsearch_url` / `elasticsearch_index` を確認し、`make deploy-all` step 13 以降を再実行する | `env/config/setting.yaml` と `kubectl -n search get configmap search-api-config -o yaml` で状態確認 |

### 1.7 検証サイクル

→ 別ドキュメントに分離: [`04_検証.md`](./04_検証.md)。

「**やる** (本ドキュメント)」と「**OK か判定する** (`04_検証.md`)」を切り分けるため、検証ゲート (L1-L5 / G1-G6)、一過性エラー recovery、性能ベースライン、依頼プロンプト例は `04_検証.md` に集約。本 §1.6 と `04_検証.md §4` の境界:

- §1.6 = **コード/インフラで恒久対処済のハマり** (再発したらバグ — issue を立てる)
- `04_検証.md §4` = **遭遇したらリトライで通せる一過性エラー** (体質と運用のトレードオフ)

---

## 2. 個別 STEP 詳細 (PDCA から外れた手動操作が必要な場合)

`make deploy-all` で完結しない場面 — ローカルだけで API を立てたい (GCP 認証ゼロ) / 1 step だけ手動で叩きたい / CI を有効化したい — の参考用。フローは旧 STEP 1–17 をそのまま残してある。

**STEP 1–6** まではローカル完結 (GCP 認証不要)。お試し / CI 目的はここで終了。
**STEP 7–8** は PDCA 周回開始前の 1 回だけ (※ `make deploy-all` 内部でも実行されるので、deploy-all を回す予定なら個別実行は不要)。
**STEP 9–17** は `make deploy-all` のサブステップを分解したもの + ステージング疎通の中身。

### STEP 1: 前提ツールをインストール

| ツール | 用途 | 最低バージョン |
|---|---|---|
| Python | ランタイム | 3.12 |
| [`uv`](https://docs.astral.sh/uv/) | パッケージ / ワークスペース管理 | 0.4 |
| [`terraform`](https://developer.hashicorp.com/terraform/install) | IaC (STEP 8 以降) | 1.9 |
| [`gcloud`](https://cloud.google.com/sdk/docs/install) CLI | GCP 操作 (STEP 7 以降) | 最新 |
| `make` | ワークフロー一括実行 | 3.8 |
| `jq` | `make ops-check-retrain` の JSON パース | 任意 |
| `docker` | ローカル Dockerfile ビルド確認 | 任意 |

Ubuntu / WSL のインストール例:

```bash
# uv
curl -LsSf https://astral.sh/uv/install.sh | sh
source $HOME/.local/bin/env    # PATH を反映 (新ターミナルで不要)

# Terraform (HashiCorp 公式 APT)
wget -O- https://apt.releases.hashicorp.com/gpg | \
  sudo gpg --dearmor -o /usr/share/keyrings/hashicorp-archive-keyring.gpg
echo "deb [signed-by=/usr/share/keyrings/hashicorp-archive-keyring.gpg] \
  https://apt.releases.hashicorp.com $(lsb_release -cs) main" | \
  sudo tee /etc/apt/sources.list.d/hashicorp.list
sudo apt update && sudo apt install terraform

# gcloud CLI
sudo apt install google-cloud-cli

# jq (任意)
sudo apt install jq
```

### STEP 2: ツール到達確認

```bash
make doctor
```

全行 `OK` で次へ。`MISS` が出たら STEP 1 に戻る。`python3` のバージョンは `pyproject.toml` の `requires-python = ">=3.12"` を満たせば何でもよい (3.12 / 3.13 / 3.14 いずれも可)。

`WARN VIRTUAL_ENV=... は project 外` が出たら、外側で別の venv を `source` している。uv は repo 直下の `.venv/` を使うため活動中の venv は無視される (害はないが混乱の元)。`deactivate` してから以降の `make` を叩くのを推奨。

### STEP 3: Python 依存を入れる

```bash
make sync
```

uv workspace (`common` / `app` / `jobs`) と `dev` グループ (ruff / mypy / pytest / httpx) を一括導入。`.venv/` が root に作られる (`uv` が自動管理するので手動で activate 不要 — `make` 系は常に `uv run` 経由で解決する)。

**所要時間**: 初回 ~2-3 分 (`sentence-transformers` が引く `torch` ~2GB ほか ML deps の download + 展開)。再実行時はキャッシュヒットで数秒。

### STEP 4: 静的検証 + テスト

```bash
make check
```

ruff / ruff-format / mypy strict / pytest を順に実行。CI と同じ。全緑で次へ。

### STEP 5: 合成データで学習 smoke

```bash
make train-smoke-persist
```

`/tmp/hybrid-search-cloud-smoke-model.txt` に LightGBM LambdaRank モデルが書き出される。BQ/GCS は触らない。

### STEP 6: API をローカル起動 (GCP 認証ゼロ)

```bash
make api-dev
```

uvicorn が **フォアグラウンドで** `http://127.0.0.1:8000` に bind する (`ENABLE_SEARCH=false` 既定なので `/api/v1/search` は 503 を返す)。別ターミナルで疎通:

```bash
curl -sSf http://127.0.0.1:8000/healthz
```

/search を local で叩きたい場合は、後続の `make api-dev-hybrid` を使う。
確認後 **Ctrl+C で api-dev を停止** してから STEP 7 へ。GCP にデプロイしない場合はここで終わり。

### STEP 6b: ハイブリッド検索を local で起動 (推奨)

`make api-dev` は疎通確認専用。**実際に hybrid 検索 (`/api/v1/search`) まで local で動かす標準手順は `make api-dev-hybrid`** とする。

```bash
make api-dev-hybrid
```

このターゲットは `scripts/setup/local_hybrid.py` を呼び、以下をまとめて処理する:

1. `env/config/setting.yaml` から **非秘密値** を読む
2. `env/secret/credential.yaml` から **ローカル用秘密値** を読む
3. `ELASTICSEARCH_API_KEY` が local に無ければ `env/secret/credential.yaml` / 環境変数から解決する
4. `/tmp/hybrid-search-cloud-smoke-model.txt` が無ければ synthetic LightGBM model を作る
5. local encoder (`127.0.0.1:18081`) / local reranker (`127.0.0.1:18082`) / app (`127.0.0.1:8000`) を起動する
6. app は Elasticsearch + BigQuery + local encoder/reranker を使って **production と同じ app 契約** で `/api/v1/search` を処理する

確認:

```bash
curl -sSf http://127.0.0.1:8000/healthz
TARGET=local LOCAL_API_URL=http://127.0.0.1:8000 \
  uv run python -m scripts.ops.search
```

期待値:

- `healthz` が `{"status":"ok"}`
- `/api/v1/search` が 200
- title に寄せた query では `lexical_rank != 10000` の候補が混ざる

前提:

- `gcloud auth application-default login` 済み

設定の置き場所:

- **非秘密値**: `env/config/setting.yaml`
  - `project_id`
  - `region`
  - `elasticsearch_url`
  - `elasticsearch_index`
  - `local_api_port` / `local_encoder_port` / `local_reranker_port`
  - `local_reranker_model_path`
- **秘密値**: `env/secret/credential.yaml`
  - `elasticsearch_api_key` (必要な環境のみ)

この分離により、「local だけ別の env 名 / 別の値の置き場を使って app 契約が drift する」事故を避ける。

---

### STEP 7: GCP 認証 (ADC)

```bash
gcloud auth application-default login      # ブラウザで OAuth
gcloud config set project mlops-dev-a      # 以降の gcloud / bq のデフォルト project
```

### STEP 8: 初回ブートストラップ (プロジェクト新規時のみ、1 回)

Terraform の**外側**で実施する初期設定。API 有効化と tfstate バケット作成を冪等に行う:

```bash
make tf-bootstrap
```

- プロジェクト Owner 相当 (Service Usage Admin + Storage Admin) の権限が必要
- 既存プロジェクトに途中参加なら既に完了しているはずなので再実行しても害なし (冪等)
- 作成されるバケット: `gs://mlops-dev-a-tfstate` (`infra/backend.tf` と一致)
- **所要時間**: 初回 ~30s (15 API 有効化 + バケット作成)、再実行 ~5s

### STEP 9: Terraform apply (インフラ構築)

> **PDCA 標準フローでは `make deploy-all` がここを step 4-5 として内包する**。本 STEP は手動で plan → apply を分けたいとき用。

```bash
make tf-init                                                      # バケット存在 + provider (google / time / kubernetes / helm) を preflight
make tf-plan                                                      # GITHUB_REPO / ONCALL_EMAIL は env/config/setting.yaml が既定値
terraform -chdir=infra/terraform/environments/dev apply tfplan    # 保存済み plan を apply
```

別 GitHub repo / 別 oncall を試す場合のみ env で上書き:

```bash
make tf-plan GITHUB_REPO=other/repo ONCALL_EMAIL=oncall@example.com
```

ポイント:
- `GITHUB_REPO` は **GitHub リポジトリのパス**。WIF の `attribute.repository` 制約に焼き込まれる — 間違えると CI が auth できなくなる。setting.yaml の `github_repo` が canonical。
- `ONCALL_EMAIL` は monitoring alert policy の通知先。`@` を含まないと `variables.tf` + `scripts/setup/tf_plan.py` の validation で reject される。setting.yaml の `oncall_email` が canonical。plan 段階では通知送信されないので、練習用途なら自身の address でよい。
- `terraform -chdir=... apply` を `-var` 無しで直接叩くと対話プロンプトで vars を再入力させられ、`yes` 確認と混同しやすい。**必ず plan を `-out=tfplan` で保存して `apply tfplan` する** (Makefile の `tf-plan` が `-out=tfplan` を付けている)。
- オフラインで syntax チェックだけしたい場合は `make tf-validate` (バックエンド接続なし)。
- `make tf-plan` を **backslash 改行で書かない** (貼り付け時に trailing whitespace が混入すると line continuation が壊れる)。**1 行で叩く** のが安全。
- **2 段階 apply が必要な場面**: 完全ゼロから (GKE Autopilot cluster 未作成で) 一発 apply すると `kubernetes` / `helm` provider が cluster 未作成時点で activate されて失敗することがある。その場合は一度 `terraform apply -target=module.iam -target=module.data -target=module.gke -auto-approve` で cluster を先行作成してから、`terraform apply tfplan` を再度回す。`provider.tf` にも同じ手順がコメントで載っている。
- 初回 apply の所要時間は ~8-12 分 (GKE Autopilot cluster 作成 + KServe Helm chart apply + `time_sleep.wait_for_log_metric_indexing` 90s 含む)。
- 初回は search-api Deployment が placeholder image (例: `gcr.io/cloudrun/hello`) で起動 (Artifact Registry が空のため)。real image は `make deploy-api` で差し替える (PDCA 標準フローでは `deploy-all` が step 7 として連続実行)。
- encoder / reranker の KServe InferenceService も初回は `storageUri = "gs://.../placeholder"` のまま PENDING。Vertex AI Pipelines (`make ops-train-now` や pipeline schedule) が Model Registry に `production` alias を付けた後、**別途** `make deploy-kserve-models` で storageUri を `kubectl patch` する。
- 過去に踏んだ初回 apply のハマりは **§1.6 の表にまとめて記載**。再発時はそちらを参照。

### STEP 10: Secret Manager に値を投入

Terraform は secret container (`search-api-iap-oauth-client-secret` on GCP Secret Manager) まで作る。本 STEP は値の投入を扱い、GKE 側の K8s Secret は External Secrets Operator が同期する。

- **IAP を有効化する場合**: `search-api-iap-oauth-client-secret` を Secret Manager に投入すると、`search/search-api-iap-oauth-client-secret` に自動同期される

責務分担:

- **Secret Manager**: 本番の正本。実値を持つ
- **`env/config/setting.yaml`**: secret そのものは置かず、参照情報のみを保持
- **`env/secret/credential.yaml`**: local 開発だけで使う override。必要な場合のみ実値を置く

```bash
# IAP を有効化する場合のみ: OAuth client secret を投入
echo -n "<oauth-client-secret>" | \
  gcloud secrets versions add search-api-iap-oauth-client-secret --data-file=-
make kube-creds
kubectl -n search get externalsecret search-api-iap-oauth-client-secret
```

`terraform apply` と `make apply-manifests` 後に値を投入し、ExternalSecret の同期を確認してから `make deploy-api` で search-api Pod を rollout すること。手順詳細は `env/secret/README.md`。

### STEP 11: GitHub Actions Repository variables (CI 経由 deploy をしたい場合のみ)

CI 経由 deploy をしないなら **skip 可** (`make deploy-api` で local から代替できる)。CI を有効化する場合は、`terraform -chdir=infra/terraform/environments/dev output` で 2 つの値を取り、GitHub の `Settings → Secrets and variables → Actions → Variables` に登録:

```bash
terraform -chdir=infra/terraform/environments/dev output workload_identity_provider
terraform -chdir=infra/terraform/environments/dev output github_deployer_sa_email
```

| GitHub Variables | 登録する値 |
|---|---|
| `WORKLOAD_IDENTITY_PROVIDER` | `workload_identity_provider` output (`projects/.../locations/global/workloadIdentityPools/github/providers/github-oidc`) |
| `DEPLOYER_SERVICE_ACCOUNT` | `github_deployer_sa_email` output (`sa-github-deployer@mlops-dev-a.iam.gserviceaccount.com`) |
| `ONCALL_EMAIL` | terraform.yml が `-var=oncall_email=...` で参照する |

### STEP 12: Dataform リポジトリの確認

`google_dataform_repository` は Terraform で作成済 (`modules/data/main.tf`)。STEP 9 の apply 後、GCP コンソール (`console.cloud.google.com/bigquery/dataform`) で repository `hybrid-search-cloud` が存在することを確認する。`deploy-dataform.yml` が `definitions/**` 変更時にこのリポジトリへ compilationResults を POST する。

### STEP 13: Terraform 経路の smoke 確認

```bash
make ops-runs-recent   # 空でも OK (まだ学習が走っていない初期状態)
```

この時点では `mlops.training_runs` は空。データ投入と初回疎通は STEP 14 以降で行う。

---

### STEP 14: 不動産データ seed 投入 (ステージング疎通の下準備)

`raw.properties` / `mlops.feedback_events` はインフラ側の骨しか無い (実データはアプリ外注 or seed ツールで投入)。疎通確認だけが目的なら最低 50 件の物件で足りる。

```bash
# 物件 seed (サンプル CSV を raw.properties に load)
bq load --project_id=mlops-dev-a \
  --source_format=CSV --autodetect \
  raw.properties \
  /tmp/properties_sample.csv

# 最低ラインの疎通確認には feedback_events は空でよい (/search の結合が LEFT JOIN)
```

CSV 列は `raw.properties` スキーマ (`property_id / title / description / city / ward / rent / management_fee / layout / area_m2 / age_years / walk_min / pet_ok / created_at / updated_at`) に合わせる。本番投入は別の ETL が受け持つ前提。

### STEP 15: 初回 embedding バッチ実行 (Vertex AI Pipelines)

canonical 構成では embedding batch も Vertex AI Pipelines (`property-search-embed`) 経由。`make ops-pipeline-run` で 1 回回して `feature_mart.property_embeddings` を埋める。初回は全件 encode するので物件数 × ~50ms/件。

```bash
# Vertex AI Pipelines (property-search-embed) を手動 submit
make ops-pipeline-run TARGET=embed

# 結果確認 (property_id の件数 = raw.properties 件数、embedding 次元 = 768)
bq query --use_legacy_sql=false --project_id=mlops-dev-a '
  SELECT COUNT(*) AS n, AVG(ARRAY_LENGTH(embedding)) AS dim
  FROM `mlops-dev-a.feature_mart.property_embeddings`'
```

`dim = 768` / `n > 0` が期待値。`dim` が異なる場合は encoder の checkpoint (multilingual-e5-base) が取り違っている — pipeline パラメータの `encoder_model` を確認する。

**所要時間**: pipeline 起動オーバーヘッド ~1-2 分 + 物件数 × ~50ms/件。50 物件なら ~2 分、1,000 物件 ~3 分、10,000 物件 ~10 分。進捗は `Vertex AI Console → Pipelines` で確認できる (make target は submit のみで wait しない)。

### STEP 16: VECTOR INDEX 作成 (手動 SQL)

`google_bigquery_vector_index` リソースは 2026-04 時点の Terraform google provider では未対応なので **BQ コンソールまたは bq CLI で手動作成**。小データでは INDEX 無しでも `VECTOR_SEARCH(...)` は brute-force で動くが、物件 10,000 件超で必須:

```bash
bq query --use_legacy_sql=false --project_id=mlops-dev-a '
  CREATE OR REPLACE VECTOR INDEX property_embeddings_idx
  ON `mlops-dev-a.feature_mart.property_embeddings`(embedding)
  OPTIONS(
    index_type = "IVF",
    distance_type = "COSINE"
  )
'
```

作成には数分かかる (物件 10k で ~2-5 分、100k で ~10-20 分)。進捗は `INFORMATION_SCHEMA.VECTOR_INDEXES`:

```bash
bq query --use_legacy_sql=false --project_id=mlops-dev-a '
  SELECT index_name, coverage_percentage, last_refresh_time
  FROM `mlops-dev-a.feature_mart.INFORMATION_SCHEMA.VECTOR_INDEXES`'
```

`coverage_percentage` が 100 に到達したら疎通確認へ。`property_embeddings` を更新するたびに BQ が incremental refresh する (手動再実行不要)。

### STEP 16b: Elasticsearch index 同期 (手動初期化)

hybrid 検索では lexical 候補抽出に Elasticsearch を使う。`properties_cleaned` を index `properties` へ同期する:

```bash
uv run python -m scripts.ops.sync_elasticsearch \
  --project-id mlops-dev-a \
  --table feature_mart.properties_cleaned \
  --es-url "http://elasticsearch.search.svc.cluster.local:9200" \
  --index properties
```

同期後は Elasticsearch の `_count` / `_stats` でドキュメント件数を確認し、`property_embeddings` 件数と大きく乖離していないことを確認する。

### STEP 17: `/api/v1/search` `/api/v1/feedback` ステージング疎通 (完了ゲート)

疎通条件の 5 項目を逐一確認する。**rerank 無し** で `final_rank == lexical_rank` が返ることが合格ライン (booster はまだ load しない):

```bash
# search-api の ConfigMap を更新して rolling restart
# search-api は GKE Deployment なので `kubectl set env` で env を注入し、Pod を restart する。
# 所要時間 ~1-2 分: ConfigMap / env 差し替え + rolling restart + encoder 層ロード。
# encoder 本体は KServe InferenceService 側で常駐しているので、search-api の再起動自体は軽い。
kubectl -n search set env deployment/search-api \
  ENABLE_SEARCH=true \
  KSERVE_ENCODER_URL="http://property-encoder.kserve-inference.svc.cluster.local/v1/models/property-encoder:predict" \
  KSERVE_RERANKER_URL="http://property-reranker.kserve-inference.svc.cluster.local/v2/models/property-reranker/infer"
kubectl -n search rollout status deployment/search-api

# 1) /search 200 応答 + 上位 20 件返却
URL=$(make -s ops-api-url)
TOKEN=$(gcloud auth print-identity-token)
curl -sSf -X POST -H "Authorization: Bearer $TOKEN" \
  "$URL/search" -H 'content-type: application/json' \
  -d '{"query":"赤羽駅徒歩10分 ペット可","filters":{"max_rent":150000},"top_k":20}' \
  | jq '.results | length'
# → 20 (上限)

# 2-3) final_rank == lexical_rank / score=null (rerank 未配線)
curl -sSf -X POST -H "Authorization: Bearer $TOKEN" \
  "$URL/search" -H 'content-type: application/json' \
  -d '{"query":"新宿駅","top_k":5}' \
  | jq '.results[] | {property_id, lexical_rank, final_rank, score}'
# → すべての行で lexical_rank == final_rank、score は null

# 4) ranking_log に 1 リクエストあたり候補 100 件分の行が流れる
# (Pub/Sub → BQ Subscription 経由。反映まで数分)
bq query --use_legacy_sql=false --project_id=mlops-dev-a '
  SELECT request_id, COUNT(*) AS candidates
  FROM `mlops-dev-a.mlops.ranking_log`
  WHERE ts >= TIMESTAMP_SUB(CURRENT_TIMESTAMP(), INTERVAL 10 MINUTE)
  GROUP BY request_id
  ORDER BY candidates DESC LIMIT 5'
# → candidates = 100 (pool sizeと一致)

# 5) /feedback publish 疎通
REQ_ID=$(curl -sSf -X POST -H "Authorization: Bearer $TOKEN" \
  "$URL/search" -H 'content-type: application/json' \
  -d '{"query":"品川駅","top_k":1}' | jq -r .request_id)
PROP_ID=$(curl -sSf -X POST -H "Authorization: Bearer $TOKEN" \
  "$URL/search" -H 'content-type: application/json' \
  -d '{"query":"品川駅","top_k":1}' | jq -r '.results[0].property_id')
curl -sSf -X POST -H "Authorization: Bearer $TOKEN" \
  "$URL/feedback" -H 'content-type: application/json' \
  -d "{\"request_id\":\"$REQ_ID\",\"property_id\":\"$PROP_ID\",\"action\":\"click\"}"
# → {"accepted": true}

# feedback_events への反映 (数分)
bq query --use_legacy_sql=false --project_id=mlops-dev-a '
  SELECT COUNT(*) AS n
  FROM `mlops-dev-a.mlops.feedback_events`
  WHERE ts >= TIMESTAMP_SUB(CURRENT_TIMESTAMP(), INTERVAL 10 MINUTE)'
# → n >= 1
```

5 項目すべて PASS でステージング疎通完了。以降は rerank 有効化設定 (`/api/v1/search` で `score` を返す経路) に進む。トラブル時は §4.7 を参照。

#### 短縮: ops-* で同等チェック

各 step で叩く curl 群は `make ops-*` ですべて wrap 済 (env で QUERY / TOP_K 等を上書き):

```bash
make ops-livez            # /livez (= 旧 /healthz、Cloud Run GFE 予約回避)
make ops-search QUERY="赤羽駅 ペット可" TOP_K=20 MAX_RENT=150000
make ops-ranking QUERY="新宿駅" TOP_K=5     # results[].lexical_rank/final_rank/score/me5_score を整形
make ops-feedback QUERY="品川駅" ACTION=click
make ops-label-seed                            # click/favorite/inquiry を seed
make ops-search-volume                         # /search 24h volume from mlops.search_logs
```

`make ops-search` は `TimeoutError` / `URLError` / `OSError` を transient とみなして自動リトライする (cold-start / rolling restart 中の偶発 timeout を救う safety net)。挙動を上書きしたい場合は env で:

| env | default | 用途 |
|---|---|---|
| `SEARCH_RETRIES` | `3` | リトライ含む試行回数 (例: `0` で即 fail) |
| `SEARCH_RETRY_SLEEP` | `2.0` | リトライ間隔 (秒) |

---

## 3. 定常運用

### 3.0 環境 (参照)

| 項目 | 値 |
|---|---|
| GCP プロジェクト | `mlops-dev-a` |
| リージョン | `asia-northeast1` |
| 通知先 | `oncall@example.com` (placeholder — `infra/terraform/modules/monitoring/` で差し替え) |
| API URL | `make ops-api-url` |
| ログ基盤 | Cloud Logging 構造化 JSON |

### 3.1 定常サイクル (自動)

| 時刻 (JST) | イベント | 実行系 |
|---|---|---|
| 03:00 | Dataform スケジュール → `feature_mart.property_features_daily` 更新 + assertion | Dataform |
| 03:30 | Vertex AI Pipelines `property-search-embed` → `feature_mart.property_embeddings` 差分更新 | Vertex AI Pipelines Schedule |
| 04:00 | Cloud Scheduler `check-retrain-daily` → search-api `/ops/jobs/check-retrain` (GKE Gateway endpoint) | Cloud Scheduler → GKE Gateway → search-api |
| 04:00+ | 条件成立時 → `retrain-trigger` publish → Eventarc → Vertex AI Pipelines `property-search-train` 起動 | Eventarc / Vertex AI Pipelines |
| 05:00 | Scheduled Query `property_feature_skew_check` → `mlops.validation_results` に記録 | BQ Data Transfer |
| リアルタイム | `/api/v1/search` → Pub/Sub `ranking-log` / `/api/v1/feedback` → `search-feedback` → BQ `mlops.ranking_log` / `feedback_events` | search-api (GKE Pod) + Pub/Sub BQ Subscription |

### 3.2 日次チェック (朝会の前に 5 分)

```bash
make ops-daily
```

が下記 3 つを順に実行する (`ops-skew-latest` / `ops-search-volume` / `ops-runs-recent`)。Dataform assertion は別途:

```bash
bq query --use_legacy_sql=false --project_id=mlops-dev-a \
  'SELECT * FROM `mlops-dev-a.feature_mart_assertions.property_features_quality` LIMIT 10'
# 空行 = PASS
```

`status = 'FAIL'` / 検索 0 件 / run が 7 日以上更新されていない のどれかなら §4 のインシデント対応へ。

個別実行:

| 目的 | コマンド |
|---|---|
| スキュー検出結果 (当日) | `make ops-skew-latest` |
| 検索ログ件数 (直近 24h) | `make ops-search-volume` |
| 直近の学習 run 5 件 | `make ops-runs-recent` |

### 3.3 手動操作

#### 学習ジョブを強制実行

```bash
make ops-train-now            # Vertex AI Pipelines (property-search-train) を submit
```

完了後に `mlops.training_runs` に新 `run_id` と `gs://mlops-dev-a-models/lgbm/{date}/{run_id}/model.txt` が増えていることを `make ops-runs-recent` で確認。Vertex Model Registry には新バージョンが登録され、`production` alias を `make ops-promote-reranker VERSION=vN APPLY=1` で付け替える。

**所要時間**: 合成 / 小規模 ~3-5 分 (Vertex Pipeline 起動オーバーヘッド込み)、実データ ~5-10 分 (feedback 件数 × feature 列数に比例)。make target 自体は submit のみで block しない。

#### API / KServe に新モデルを反映

```bash
# (a) search-api Pod の ConfigMap / env 変更を即時反映 (KServe URL 等)
make ops-reload-api           # kubectl rollout restart deployment/search-api

# (b) Model Registry `production` alias の artifact URI で KServe を更新
make deploy-kserve-models     # property-encoder / property-reranker の storageUri を kubectl patch
```

`ops-reload-api` は Pod ローリング再起動のみ (~30-60s)、`deploy-kserve-models` は KServe InferenceService の storageUri を書き換えて再 pull を起こす (~2-3 分、KServe 側の image pull + predictor 起動込み)。

#### 再学習トリガーを手動確認

```bash
make ops-check-retrain
```

返却 JSON の `reasons` で (a)/(b)/(c) のどれが発火したか、`published` で `retrain-trigger` に publish されたかを確認。`jq` が必要。

#### スキュー検出 SQL をアドホック実行

```bash
make ops-skew-run
```

`infra/sql/monitoring/validate_feature_skew.sql` を BQ に流し、当日の `validation_results` を上書き。

#### Dataform 手動トリガー

Dataform コンソール → repository `hybrid-search-cloud` → *CREATE NEW WORKFLOW EXECUTION*。CLI からは `deploy-dataform.yml` 末尾の `compilationResults` POST を参照 (make 化していない)。

### 3.4 変更運用 (コード / 設定変更時)

#### 特徴量を追加・変更する

以下 **6 ファイル** を **同一 PR で** 全部直す (片方だけだとスキュー検知 / Vertex Feature Group で FAIL する):

1. `pipeline/data_job/dataform/features/property_features_daily.sqlx`
2. `ml/data/feature_engineering/ranker_features.py::build_ranker_features`
3. `ml/data/feature_engineering/schema.py` (`FEATURE_COLS_RANKER` / `LABEL_GAIN`)
4. `infra/terraform/modules/data/main.tf` の `ranking_log.features` RECORD スキーマ
5. `infra/sql/monitoring/validate_feature_skew.sql` の UNPIVOT (property-side N 列)
6. `infra/terraform/modules/vertex/main.tf::google_vertex_ai_feature_group_feature` × N (feature 1 列 = feature group feature 1 件)

ローカルで `make check` が通ることを確認 → PR → マージで `deploy-dataform.yml` → `deploy-api.yml` → `deploy-trainer-image.yml` の順で反映。初回は `make ops-train-now` で新特徴量を含む run を作り、`make ops-promote-reranker VERSION=vN APPLY=1` で production alias 昇格 → `make deploy-kserve-models` で KServe に反映 → `make ops-reload-api` で search-api も再起動。

#### ハイパーパラメータを変える

`ml/common/config/training.py::TrainSettings` を修正 → `make test` → PR → `deploy-trainer-image.yml` → `make ops-train-now`。

#### Deployment / KServe リソースを変える

`infra/manifests/search-api/{deployment,hpa}.yaml` (GKE 側) と `infra/manifests/kserve/{encoder,reranker}.yaml` (KServe 側) を変更。`CLAUDE.md` の "non-negotiable defaults" (requests/limits、replicas、HPA threshold) を変更する場合は理由をコミットメッセージに明記。変更前に `kubectl apply --dry-run=server -k infra/manifests/` で差分を目視し、apply 後は `kubectl -n search rollout status deployment/search-api` を確認。

#### Dataform スケジュール変更

`infra/terraform/modules/monitoring/main.tf::google_bigquery_data_transfer_config.property_feature_skew_check.schedule` と Dataform スケジュール (UI) を揃える。04:00 再学習チェック前に 03:00 Dataform 完了の順序を崩さない。

#### 再学習条件を変える

`app/services/retrain_policy.py` の 3 定数 (`NEW_FEEDBACK_ROWS_THRESHOLD` / `NDCG_DEGRADATION` / `STALE_DAYS`) を変更。リグレッションを避けるため `tests/unit/app/test_retrain.py` に追加ケースを書き、`make test` が通ること。

### 3.5 シークレットローテーション

Terraform は secret container + IAM のみ管理。値のローテは make 対象外 (秘密値を Make の引数に取らない):

```bash
# GCP Secret Manager
gcloud secrets versions add search-api-iap-oauth-client-secret --data-file=- <<< "$NEW_IAP_SECRET"
```

古いバージョンは `gcloud secrets versions destroy` で無効化。自動ロールバックを考慮し最低 1 世代は残す。新しいバージョンが有効化されたら `make ops-reload-api` で search-api Pod を rolling restart する。

### 3.6 コスト確認

月次で以下を確認。PoC レンジ (¥10k〜¥30k/月) 超過時はまず BQ スキャン量 / GKE Autopilot Pod リソース / KServe predictor 起動時間を疑う:

```bash
make ops-bq-scan-top
```

GKE Autopilot `search-api` の `requests=500m/1Gi` / `limits=2/2Gi` / `minReplicas=1 maxReplicas=10` は意図的 (CLAUDE.md の非負制約)。HPA が CPU 70% / Mem 80% で発火するので、通常時は 1 Pod だけ動く。KServe predictor (`property-encoder` / `property-reranker`) は `minReplicas=1` 既定で常時 1 Pod (コールドスタート回避のため 0 に落とさない)。

---

## 4. インシデント対応

### 4.1 search-api 5xx > 1% アラート発火

1. ログ確認: Cloud Logging で `resource.type="k8s_container" resource.labels.namespace_name="search" resource.labels.container_name="search-api" severity>=ERROR`
2. 直近デプロイ確認: `kubectl -n search rollout history deployment/search-api`
3. ロールバック: §5 ロールバック早見表の「search-api イメージ」を実行
4. Pub/Sub publish エラーなら API は 200 を返すが `ranking_log` / `feedback_events` 行数が減る — `make ops-search-volume` と `bq query ... mlops.ranking_log` で追跡
5. KServe 側の失敗なら `kubectl -n kserve-inference logs -l component=predictor --tail=200` で encoder / reranker predictor のログを見る

### 4.2 p95 レイテンシ > 500ms

- `jsonPayload.latency_ms` が大きい request_id を特定
- 原因切り分け: KServe predictor が詰まっているか (`kubectl -n kserve-inference top pods`) / search-api CPU 飽和か (`kubectl -n search top pods`) / BQ VECTOR_SEARCH が遅いか (`INFORMATION_SCHEMA.JOBS_BY_PROJECT` で query 時間)
- search-api CPU 飽和なら HPA が自動で Pod 増。超えても治らない場合は `deployment.yaml` の `limits` を一時拡張 (コスト影響あり、復旧後戻す)
- KServe predictor `minReplicas=1` を 0 に落とさない (コールドスタートで model load に数秒〜十数秒)

### 4.3 スキュー検出 FAIL (`status = 'FAIL'`)

- どの特徴量か: `make ops-skew-latest` で `feature_name` をチェック
- 推論側ソース (呼び出し元) が変わっていないか → API アクセスログと照合
- 学習分布が古い場合は `make ops-check-retrain` → 条件成立で自動キック / 手動なら `make ops-train-now`
- **しきい値を引き上げる前に必ず原因を特定**

### 4.4 再学習が走らない / 走り過ぎる

- `make ops-check-retrain` を叩き `reasons` を確認
- 閾値は `app/services/retrain_policy.py` の `NEW_FEEDBACK_ROWS_THRESHOLD=10_000` / `NDCG_DEGRADATION=0.03` / `STALE_DAYS=7`
- Cloud Scheduler: `gcloud scheduler jobs describe check-retrain-daily --location asia-northeast1`
- Eventarc 配送: Cloud Logging で `resource.type="eventarc.googleapis.com/Trigger"` を絞る
- Vertex AI Pipelines の submit 失敗: `Vertex AI Console → Pipelines` で FAILED run の error を確認

### 4.5 Dataform assertion 失敗

- `feature_mart_assertions.property_features_quality` の行を目視
- NULL 率・範囲外はだいたい生データ (`raw.properties`) の upstream 問題
- `property_features_freshness` 失敗 = 過去 2 日分のデータ欠損 → ETL 側を確認

### 4.6 ranker booster ロード失敗 (rerank 有効化時)

- `/livez` が 200 だが `/api/v1/search` の `score` が null 固定: KServe `property-reranker` InferenceService の `storageUri` が placeholder のまま or `production` alias 未付与 — `make ops-promote-reranker VERSION=vN APPLY=1` → `make deploy-kserve-models` → `kubectl -n kserve-inference get inferenceservice property-reranker` で READY=True を確認
- rerank を有効化する前は `/api/v1/search` が `score=null` / `final_rank == lexical_rank` を返すのが正 (rerank 無し時の正常挙動)
- `mlops.training_runs` に `finished_at IS NOT NULL` の行が無い場合はそもそも学習ができていない → `make ops-train-now`

### 4.7 `/api/v1/search` が 503 / 200 空配列

- `/readyz` → `search_enabled: false` の場合: `ENABLE_SEARCH=true` と `KSERVE_ENCODER_URL` / `KSERVE_RERANKER_URL` が Pod env に入っているか確認 (`kubectl -n search get deployment/search-api -o yaml | grep -A1 env`)
- `/readyz` → `search_enabled: true` なのに `/api/v1/search` が 503: KServe encoder predictor が READY になっていない (`kubectl -n kserve-inference get inferenceservice`)、あるいは cluster-local DNS 解決失敗 (NetworkPolicy が search → kserve-inference を block していないか確認 — `infra/manifests/policies/{search-api-networkpolicy,kserve-networkpolicy}.yaml`)
- `/api/v1/search` が 200 だが `results = []`: (a) `property_embeddings` が空 (STEP 15 を再実行) / (b) VECTOR INDEX が未作成で brute-force でも top_k=0 を返した (サンプルデータが少なすぎる) / (c) フィルタで全部落ちた (`max_rent` 等を外して再実行)
- `ranking_log` に行が流れない: Pub/Sub topic `ranking-log` と BQ Subscription が Terraform 側で作成済みか確認。`gcloud pubsub subscriptions list --filter="topic ~ ranking-log"` で Subscription の `bigquery_config.table` が `mlops.ranking_log` を指しているか

### 4.8 VECTOR INDEX が効かない / coverage が 100% 未満のまま

- `INFORMATION_SCHEMA.VECTOR_INDEXES` で `coverage_percentage` を確認。数時間経っても 100% に満たない場合は `property_embeddings` が絶え間なく更新されている可能性 (Vertex AI Pipeline `property-search-embed` スケジュールを一時停止し、INDEX 作成を完了させる)
- `coverage_percentage = 0` のまま: INDEX 作成クエリ自体が失敗している。`INFORMATION_SCHEMA.JOBS` で DDL ジョブのエラーを確認
- 応急: `VECTOR_SEARCH` は INDEX 無しでも動く (brute-force)。物件 10,000 件未満なら体感差は小さい

### 4.9 KServe InferenceService が READY にならない

`kubectl -n kserve-inference get inferenceservice` で `READY=False` が続く場合:

```bash
# (1) predictor Pod の状態
kubectl -n kserve-inference get pods -l serving.kserve.io/inferenceservice=property-encoder
kubectl -n kserve-inference describe inferenceservice property-encoder

# (2) storage access (Model Registry artifact GCS への Workload Identity)
kubectl -n kserve-inference logs -l serving.kserve.io/inferenceservice=property-encoder \
  -c storage-initializer --tail=100

# (3) predictor ランタイムログ
kubectl -n kserve-inference logs -l serving.kserve.io/inferenceservice=property-encoder \
  -c kserve-container --tail=200
```

典型原因:
- `storageUri` が placeholder のまま (Model Registry に production alias 未付与) → `make deploy-kserve-models`
- KSA (`kserve-inference/encoder` / `reranker`) が Workload Identity で GCS 読み取り権限を持っていない → `infra/terraform/modules/iam/main.tf` で `roles/storage.objectViewer` on `*-models` バケットが付いているか確認
- multilingual-e5 の image pull が重い → 初回のみ ~3-5 分待つ。`describe` で `ImagePullBackOff` が続くならレジストリ / ネットワーク側を疑う

---

## 5. ロールバック早見表

tag など動的な値が必要な操作は make 化していない (誤操作防止):

| 変更対象 | 戻し方 |
|---|---|
| search-api イメージ | `kubectl -n search rollout undo deployment/search-api`、あるいは `kubectl -n search set image deployment/search-api search-api=<prev-tag>` |
| KServe encoder / reranker モデル | Model Registry で旧バージョンに `production` alias を付け替え → `make deploy-kserve-models` (storageUri が `kubectl patch` で戻る) |
| Terraform | PR revert → `make tf-plan GITHUB_REPO=<owner>/<name>` で差分確認 → `terraform -chdir=infra/terraform/environments/dev apply` (state 破壊を伴う変更は手動確認) |
| Dataform | `pipeline/data_job/dataform/**` revert コミット push (`deploy-dataform.yml` が走る) |
| モデル成果物 | `make ops-runs-recent` で旧 `run_id` の `model_path` を確認し、`make ops-promote-reranker VERSION=<prev> APPLY=1` で production alias 付け替え → `make deploy-kserve-models` |
| `/api/v1/search` を一時停止 | `kubectl -n search set env deployment/search-api ENABLE_SEARCH=false` (リクエストはすべて 503 に) → 解除は `ENABLE_SEARCH=true` を上書き |
| VECTOR INDEX | `DROP VECTOR INDEX property_embeddings_idx ON ...` → brute-force fallback。再作成は STEP 16 |
| Elasticsearch index 内容 | `make sync-elasticsearch` を再実行し、`_count` / `_stats` を確認 |
