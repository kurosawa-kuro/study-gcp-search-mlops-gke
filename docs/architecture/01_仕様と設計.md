# 01_仕様と設計

> 本ドキュメントは canonical 仕様。  
> 権威順位: `TASKS_ROADMAP > TASKS > 01_仕様と設計 > README > CLAUDE`

---

## 文書構成

- §0 不変条件
- §1 ハイブリッド検索仕様
- §2 Composer 本線 orchestration
- §3 Serving (GKE + KServe)
- §4 正解データパイプライン Port/Adapter
- §5 PMLE 統合技術
- §6 実装境界 (Port/Adapter/DI)
- §7 実装状態
- §8 LightGBM 接続死守ライン
- §9 Workflow Contract
- §10 読み順

---

## §0. 位置付け / 不変

- 題材は不動産ハイブリッド検索で固定
- 中核 5 要素は不変:
  - Elasticsearch BM25
  - multilingual-e5
  - Vertex AI Vector Search
  - RRF
  - LightGBM LambdaRank
- Composer は上位 orchestrator、Vertex Pipelines は下位 executor
- Feature Store (Feature View / Online Store) を canonical 経路として維持

### §0.1 ⛔ ゴール劣化禁止

- 「本線」「必須」「canonical」要件は未達で完了扱いにしない
- 必須項目を「別 sprint」に逃がさない

---

## §1. ハイブリッド検索の仕様と設計

### 1.1 API と挙動

- 公開 API:
  - `POST /api/v1/search`
  - `POST /api/v1/feedback`
- 検索フロー:
  1. query + filters 受領
  2. lexical(ES) と semantic(ME5+VVS) 取得
  3. RRF 融合
  4. Feature View 経由特徴量取得
  5. KServe reranker で最終順位
  6. ranking/feedback を非同期記録

### 1.2 設計原則

- core は固定し、差分は adapter で吸収
- 6 軸 Port を維持（lexical / semantic / encoder / reranker / feature / log）
- Local は健全性確認、GCP は canonical 成立確認

### 1.3 reference

- 実案件参照構成: Elasticsearch + Redis 同義語 + ME5 + VVS + LightGBM

---

## 3. Cloud Composer の位置づけ (Phase 7 で本実装、後方派生で Phase 6 へ引き算)

**Phase 7 で本線オーケストレーターとして本実装**し、Phase 6 相当環境はこの canonical 実装から引き算で派生させる。
`train/evaluate/register` は Vertex Pipelines に寄せ、Composer は上位 orchestration に専念する。
Vertex `PipelineJobSchedule` → **完全撤去**。

### 3.6 Phase 7 (= canonical) / 引き算で派生する Phase 6 で禁止する状態

- Composer 本線 DAG を無効化して Cloud Scheduler 側を本線化すること
- Vertex `PipelineJobSchedule` を再導入して二重起動経路を作ること
- Feature View 経路を省略して serving 側の参照契約を崩すこと

## §2. Cloud Composer 本線 orchestration

### 2.1 役割分離

- Composer: 全体制御・運用オーケストレーション
- Vertex Pipelines: train/evaluate/register 実行

### 2.2 本線ポリシー

- 本線 retrain schedule は Composer のみ
- `PipelineJobSchedule` は併存禁止
- Cloud Scheduler / Eventarc / Cloud Functions は smoke/manual 用途に限定

### 2.3 3 DAG の責務

- `daily_feature_refresh`: 特徴量・index 更新
- `retrain_orchestration`: labeling〜retrain〜登録
- `monitoring_validation`: 評価〜gate〜反映

### 2.4 運用 surface

- `make composer-deploy-dags`
- `make ops-composer-trigger`
- `make ops-composer-list-runs`
- `make ops-composer-task-states`

---

## §3. Serving 層 (GKE + KServe)

### 3.1 構成

- `search-api`: GKE Deployment
- `property-encoder` / `property-reranker`: KServe InferenceService
- Gateway + IAP + NetworkPolicy で境界管理
- Model Registry alias を KServe `storageUri` へ反映

### 3.2 課金の見方

- 常時系: Composer / GKE / Gateway / VVS / FOS / Elasticsearch
- 実行量系: Vertex Pipelines / Cloud Build / BigQuery / PubSub / Logging/Monitoring
- 詳細試算は `runbook/05_運用.md`

---

## §4. 検索ログ正解データパイプライン Port / Adapter

- `EventWriter` (Cloud Logging)
- `EventRepository` / `LabelRepository` / `MetricsRepository` (BigQuery)
- `TrainingDatasetRepository` (GCS)
- orchestration adapter は Composer/Vertex の上下関係に従う

---

## §5. PMLE 統合技術

- BQML popularity
- Dataflow streaming（opt-in）
- TreeSHAP explainability
- SLO + burn-rate alert

中核検索経路を壊さない前提で統合する。

---

## §6. Port / Adapter / DI 境界

### 6.1 構造

- `app`: domain / api / container / services(protocols, adapters)
- `ml` / `pipeline`: ports + adapters

### 6.2 DI

- `main.py` lifespan で Container 構築
- router は `Depends` 経由で service を受け取る

### 6.3 検証

- `make check-layers`
- `tests/unit/arch`

---

## §7. 実装状態

- Wave 6 (Elasticsearch on GKE/ECK): ✅ 収束済み
- `ops-search-components` all non-zero: ✅
- 学習再実行 + 精度ゲート (`ndcg_at_10=1.0`): ✅
- 詳細実行ログは `03_実装カタログ.md` を参照

---

## §8. LightGBM 接続死守ライン

- `pipeline/training_job/main.py` から
  `ml/data/loaders/ranker_repository.py` 経由で実データ学習する
- synthetic-only 学習への退行を禁止

---

## 8. Workflow Contract が守るべきゴール

- G-W1. PDCA は `deploy-all -> run-all -> destroy-all` の 1 本線で完結する
- G-W2. seed 後同期の成立
- G-W3. runtime 設定再現性
- G-W4. canonical serving path を検証本線に含める
- G-W5. failure 診断可能性
- G-W6. destroy 後再現性
- FastAPI boot / import / `/livez` 200 は ADC なし local でも成立する

## §9. Workflow Contract

- G-W1: `deploy-all -> run-all -> destroy-all` 一貫性
- G-W2: seed 後同期の成立
- G-W3: runtime 設定再現性
- G-W4: canonical serving path 実証
- G-W5: failure 診断可能性
- G-W6: destroy 後再現性

---

## §10. 読み順

1. `TASKS_ROADMAP.md` で現在地確認
2. 本書 §1〜§3 で構造理解
3. `03_実装カタログ.md` で実装位置確認
4. `runbook/05_運用.md` で実行手順確認
5. `TASKS.md` で短期作業を進行

