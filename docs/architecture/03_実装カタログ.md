# 03_実装カタログ

本書は「どこに何があるか」を最短で掴むための実装スナップショット。  
詳細手順は runbook、進行管理は tasks を正本とする。

---

## 関連ドキュメント

| 役割 | 場所 |
|---|---|
| canonical 仕様 | [`01_仕様と設計.md`](01_仕様と設計.md) |
| current sprint | [`../tasks/TASKS.md`](../tasks/TASKS.md) |
| 長期ロードマップ | [`../tasks/TASKS_ROADMAP.md`](../tasks/TASKS_ROADMAP.md) |
| 運用手順 | [`../runbook/05_運用.md`](../runbook/05_運用.md) |
| 検証手順 | [`../runbook/04_検証.md`](../runbook/04_検証.md) |

---

## 検証状況（`mlops-dev-a`）

- Wave 6 (Elasticsearch on GKE/ECK): ✅ 収束済み
- `ops-search-components`: ✅ `lexical=4 semantic=3 rerank=5`、`readyz_rerank_enabled=True`
- VVS / Feature View / KServe rerank: ✅ 動作確認済み
- 学習再実行 + 精度ゲート (`ndcg_at_10=1.0 hit_rate_at_10=1.0 mrr_at_10=1.0`): ✅ 達成済み
- **M-Wave9 公開ドメイン (`https://gcp-search-mlops-gke.dev`): ✅ 全 Step 完了 (2026-05-11)** — `make deploy-all` 15/15 完走 / Certificate Manager cert `search-api-cert` `managed.state=ACTIVE` / `dig +short gcp-search-mlops-gke.dev` = 静的グローバル IP / `make ops-search TARGET=gcp` (= `https://gcp-search-mlops-gke.dev/api/v1/search`) HTTPS 200 / `curl -I https://gcp-search-mlops-gke.dev` HTTP/2 + Google Trust Services cert
- **`make run-all-core` 完走 (2026-05-11): ✅ 16/16 step PASS、total 9m16s** — `ops-livez → ops-search → ops-search-components → vertex VVS/FG smoke → ops-feedback → ops-ranking → ops-label-seed → label-build (ranking_labels 61 件 materialize) → ops-train-now → ops-train-wait (Vertex pipeline `state=SUCCEEDED`) → ops-daily → ops-accuracy-report (`ndcg=hit_rate=mrr=1.0`)`。step 順を「behavior seeding (`ops-label-seed`) + ラベル materialize (`label-build`) を `ops-train-now` の前」に修正済 (旧順は `ops-train-now` が先で `load_features` に空 frame が渡り `train-reranker` が crash していた)
- `make check`: ✅ 840 passed / 2 skipped (`ruff` + `ruff format --check` + `mypy --strict app ml pipeline` + `pytest`)

詳細ログは `TASKS.md` ではなく本書末尾の「直近完了ログ」を参照。

---

## デプロイ・運用の要約

- `make deploy-all`: 初回 50-72 分目安（**step 6 tf-apply が支配** — GKE cluster + Composer Gen3 環境作成 + Vertex Vector Search index deploy。VVS Index/Endpoint が GCP 残置されている場合は import で no-op 化され短縮、残置 ID が古く再作成になると延びる）
- `make run-all-core`: ~9-15 分目安（全 16 step、`ops-train-wait` + `label-build` + `seed-test` が支配）
- `make destroy-all`: 12-17 分目安
- `sync-elasticsearch`: `feature_mart.properties_cleaned` を Elasticsearch index へ同期する canonical lexical 経路
- **per-step wall time の自動記録 + ETA**: `deploy-all` / `destroy-all` / `run-all` の各 step 完了時に `logs/step_timings.csv`（gitignore 対象、`flow` カラム付き）へ追記、起動時に過去 run の median から ETA + 重い step トップ3 を表示する（共有モジュール `scripts/lib/step_timing.py`。`run-all-core` は bash recipe ではなく Python orchestrator `scripts/ops/run_all.py` 経由）

詳細ステップと例外対応は [`../runbook/05_運用.md`](../runbook/05_運用.md) を正本とする。

---

## 1. ディレクトリ構成（要約）

```text
.
├── app/                # FastAPI search-api
├── ml/                 # training / serving / evaluation / data
├── pipeline/           # Vertex Pipelines + Composer DAG
├── infra/
│   ├── terraform/      # modules (gke/kserve/elasticsearch含む)
│   ├── manifests/      # k8s manifests
│   └── run/services/   # Cloud Build + Dockerfile
├── scripts/            # setup/deploy/ops/ci/bqml/sql
├── tests/              # unit/integration/e2e + fakes
├── env/                # setting.yaml + secret
└── docs/               # architecture/runbook/tasks/decisions
```

---

## 2. 実装マップ

### 2.1 `app/`（search-api）

- `main.py`: FastAPI entrypoint
- `composition_root.py`: DI コンテナ構築
- `api/routers/`: `/api/v1` `/ops` `/ui` の handler
- `services/protocols/`: Port 定義
- `services/adapters/`: 外部接続 adapter（Elasticsearch / KServe / PubSub / BQ）
- `services/noop_adapters/`: フォールバック実装

### 2.2 `ml/`

- `data/`: feature 組み立て・repository
- `training/`: LightGBM 学習
- `serving/`: encoder/reranker 推論
- `evaluation/`: metrics/validator/report
- `registry/`: Vertex Model Registry adapter

### 2.3 `pipeline/`

- `data_job/`: embedding 系 pipeline + Dataform
- `training_job/`: reranker train pipeline
- `evaluation_job/`: offline 評価 pipeline
- `dags/`: Composer 3 DAG
- `workflow/`: compile/trigger 補助

### 2.4 `infra/terraform/`

主要 module:
- `iam`, `data`, `messaging`, `monitoring`, `vertex`, `slo`
- `vector_search`, `gke`, `kserve`, `composer`, `elasticsearch`

### 2.5 `infra/manifests/`

- `search-api/`: deployment/service/gateway/hpa/config
- `kserve/`: encoder/reranker InferenceService
- `elasticsearch/`: ECK CR + Kibana + Service + NetworkPolicy
- `policies/`: IAP / NetworkPolicy

### 2.6 `scripts/`

- `setup/`: deploy-all / destroy-all / seed 系
- `deploy/`: API / KServe deploy
- `ops/`: livez/search/components/accuracy/vertex 系検証
- `ci/`: sync/layer check

---

## 3. データ層（BigQuery）

### 3.1 主要 dataset

- `mlops`
- `feature_mart`
- `predictions`

### 3.2 主要 table（抜粋）

- `mlops.search_events`
- `mlops.search_impressions`
- `mlops.user_actions`
- `mlops.ranking_labels`
- `mlops.evaluation_metrics`
- `mlops.ranking_log`
- `mlops.feedback_events`
- `feature_mart.property_features_daily`
- `feature_mart.property_embeddings`

完全なカラム定義は Terraform と parity test を正本とする。

---

## 4. API surface（要約）

### 4.1 Public

- `POST /api/v1/search`
- `POST /api/v1/feedback`

### 4.2 Operations

- `POST /ops/jobs/check-retrain`
- `GET /ops/model/metrics`
- `GET /ops/model/info`
- `GET /ops/model/data`
- `GET /ops/destroy-check`
- `GET /ops/search-volume`
- `GET /ops/runs-recent`

### 4.3 Probe / Metrics / UI

- Probe: `/livez` `/healthz` `/readyz`
- Metrics: `/metrics`
- UI: `/ui/*`

---

## 5. GCP リソース接続（要約）

- GKE Autopilot (`search-api`)
- KServe (`property-encoder`, `property-reranker`)
- Elasticsearch (ECK on GKE)
- Vertex AI (Pipelines / Vector Search / Feature Store / Model Registry)
- Composer (3 DAG orchestration)
- Pub/Sub + BigQuery subscription
- Cloud Monitoring + SLO + burn-rate

---

## 6. 直近完了ログ

### 2026-05-11 (M-Wave9 公開ドメイン全 Step 完了 + run-all-core 完走 + step-timing 計測)

- **M-Wave9 Step 6 完了**: `make deploy-all` 15/15 完走 (~72 min)、`gcp-search-mlops-gke.dev` の Certificate Manager cert `managed.state=ACTIVE`、`make ops-search TARGET=gcp` = `https://gcp-search-mlops-gke.dev/api/v1/search` HTTPS 200、`make ops-search-components` `lexical=4 semantic=3 rerank=5` → **M-Wave9 (独自ドメイン + HTTPS + DNS) 全 Step 完了**。current backlog から外れた
- **deploy-path regression 5 件修正** (M-Wave8.6 reorg が deploy 専用パスに残した欠陥、`make check` をすり抜け): `tf_plan.py` の `terraform_var_args()` 漏れ / `subprocess.run(..., capture=True)` 3 箇所 (`state_recovery` ×2 + `vertex_feature_store_wait`) / `feature_view_sync.py`・`state_recovery.py::main` の `parents[2]` ずれ。AST-scan guard test 2 種追加 (詳細: §7.4)
- **step-timing 計測の仕組みを 3 flow に追加**: 共有モジュール `scripts/lib/step_timing.py` で各 step の wall time を `logs/step_timings.csv` (`flow` カラム付き) に記録、起動時に過去 run の median から ETA を表示。`deploy_all.py` / `destroy_all.py` を instrument、`run-all-core` を Python orchestrator `scripts/ops/run_all.py` 化 (同時に step 順を `ops-label-seed → label-build → ops-train-now` に修正 — 旧順は空 `ranking_labels` で `train-reranker` crash していた)
- **`make run-all-core` 完走確認**: 16/16 step PASS、9m16s、`label-build` が `ranking_labels` 61 件 materialize → `ops-train-now`/`ops-train-wait` で Vertex pipeline `state=SUCCEEDED` → `ops-accuracy-report` `ndcg=hit_rate=mrr=1.0`
- `make check` 818 → 840 passed / 2 skipped、`make check-layers: OK`

### 2026-05-10 (deploy-all 一発復活 + 4 incident 全永続化)

- ECK reconcile stall の **真の root cause** 特定 (NetworkPolicy 漏れ + ECK 8.x default HTTPS と canonical URL http の不整合)
- ES manifest を学習用 HTTP + anonymous superuser へ ((a') 解、production 化は parked → [`docs/backlog/production-hardening.md`](../backlog/production-hardening.md))
- NetworkPolicy に `elastic-system` ns ingress 追加 (ECK Operator block 解消)
- `make deploy-all` 全 step 完走 (resume 経由、step 1-15 全 PASS)
- `precondition` framework を `DeployStep` に追加 → `wait_until_es_healthy` を main loop が呼ぶ構造に移行
- terraform_lock parser ANSI escape 対応 + bg pipe 偽 exit 0 罠を CLAUDE.md にチェックリスト化
- 新 contract test 6 件追加: NetworkPolicy / ES manifest / precondition framework / terraform_lock ANSI / sync-elasticsearch precondition / destroy_all step 対称化
- T1 (`make verify-live-acceptance`) 走行は次の打ち手

### 2026-05-06

- Terraform stage1 + kserve apply + manifests apply を復旧
- `sync_elasticsearch` と `search-api -> ES` 接続を復旧
- ECK の readiness/endpoint 問題を解消
- `ops-search-components` で all non-zero を再確認
- 学習再実行と精度ゲートを通過
- docs を Elasticsearch canonical に統一
- `make check` を 7 fail から 0 fail へ収束（`779 passed / 2 skipped`）

---

## 7. 移管ログ（TASKS/TASKS_ROADMAP から集約）

### 7.1 Wave 2-6 完了ログ（要約）

- Wave 2: Event schema / label 契約を実装・固定
- Wave 3: EventWriter / EventRepository を配線し feedback ログ経路を実装
- Wave 4: `ranking_labels` 起点で LightGBM 学習配線を接続
- Wave 5: 継続改善サイクルと deployment gate 判定を live 検証
- Wave 6: Elasticsearch canonical 化と lexical lane 復旧を完了

### 7.2 Wave 8 収束トリオ（完了）

1) Terraform module structure
- 症状: `elasticsearch` module の required file 契約 fail
- 直接原因: `outputs.tf` / `versions.tf` 欠落
- 是正: 2 ファイル追加で契約回復

2) deploy-all workflow contract
- 症状: ConfigMap overlay の ES URL 契約 fail
- 直接原因: 既定値が `https://...`
- 是正: `http://elasticsearch.search.svc.cluster.local:9200` へ統一

3) docs canonical contract
- 症状: `01_仕様と設計.md` / `03_実装カタログ.md` の contract wording fail
- 直接原因: pin 文言欠落と `sync-elasticsearch` 表記不足
- 是正: 必須見出し/文言と canonical キーワードを追記

4) ground truth contract (reranker image)
- 症状: reranker Dockerfile contract fail
- 直接原因: apt cache mount 欠落
- 是正: cache mount を追加

### 7.3 マイルストーン履歴（実装 + 検証完了）

TASKS_ROADMAP §5 から移管。完了済 ID は本表を正本。

順序は **(A) 基盤 prelude → (B) M-Wave 番号順 → (C) satellite 改善** の 3 ブロック。日付は各行末尾 `(YYYY-MM-DD)` に統一。

#### A. 基盤 prelude

| ID | 概要 | エビデンス |
|---|---|---|
| M-Pivot | Phase 形式廃止 + docs 撤去 (2026-05-06) | README / CLAUDE / AGENTS / `docs/architecture/01,03` から Phase 概念撤去完了 |
| M-RunbookLocal | `04_検証.md` §2 相当 (2026-05-06) | `verify-local-*` + search-api イメージ L2-L4、Cloud 復旧で `ops-search-components` / VVS / Feature Group まで PASS |

#### B. M-Wave 番号順

| ID | 概要 | エビデンス |
|---|---|---|
| M-Wave0 | Makefile 止血 (2026-05-09) | Makefile 内 `\` 継続による 5 行超 shell block が 0 件 (recipe 99 target を静的走査で確認) |
| M-Wave1 | API 4 軸再設計 (2026-05-09) | `app/main.py` で `/api/v1` `/ops` `/ui` prefix 分離 + `tests/integration/parity/test_api_route_prefixes.py` 9 件 PASS (4 軸所属 / canonical endpoint 存在 / 308 redirect / probes 非 namespace / metrics root / OpenAPI 整合 / IAP policy prefix 単位) |
| M-Wave2 | 正解データ仕様確定 | 6 canonical 場所 (Pydantic / Terraform / labeling YAML / labeling SQL / EventWriter Port / `ranking_labels`) で `action_type` enum と weight が一致 |
| M-Wave3 | アプリ側 正解データログ実装 | `/api/v1/feedback` → Pub/Sub → BQ Subscription → BigQuery `mlops.user_actions` を `make ops-feedback` smoke で確認 (EventWriter Pub/Sub 統一後) |
| M-Wave4 | LightGBM 接続死守ライン | `pipeline/training_job/main.py` → `ml/data/loaders/ranker_repository.py` 配線、実 `ranking_labels` 由来 retrain → Vertex Model Registry 登録 |
| M-Wave5 | 継続改善サイクル MVP (2026-05-10) | `make verify-live-acceptance` PASS (e2e: `tests/e2e/test_live_acceptance_gate.py::test_live_acceptance_on_existing_env` 22.68s)。`ops-search-components` で **lexical=4 (ES) / semantic=3 (VVS) / rerank=5 (KServe)** all non-zero、`readyz_rerank_enabled=True`、`model_path=property-reranker-predictor.kserve-inference.svc.cluster.local/v2/models/property-reranker/infer`。canonical 経路 (Elasticsearch BM25 + multilingual-e5 + Vertex AI Vector Search + RRF + LightGBM LambdaRank) 全動作実証。前提: deploy-all resume 497s で復旧、ECK incident 解決 (NetworkPolicy + HTTP+anonymous superuser、(a') 解は学習用、production 化は M-Wave8.7) |
| M-Wave6 | Elasticsearch 移行 (GKE 上) | `search-api` 本番イメージ化 / `sync_elasticsearch` 成功 / ECK endpoint 復帰 / `ops-search-components` lexical+semantic+rerank all non-zero、Wave 6 以前の Cloud Run 検索基盤は完全削除 |
| M-Wave7 | Makefile / 実行系の canonical 化 (2026-05-10) | (a) **bg-pipe 罠の構造的予防** (旧称 C4): 全 `uv run python -m scripts.*` を `uv run python -u -m scripts.*` (line buffer 強制) に統一 — tee block buffer / 偽 exit 0 罠を Makefile target 層で予防。(b) **本格整理**: `mk/base.mk` (multi-phase skeleton) 削除、`PHASE_NAME` / `NA_TARGETS` / `print-canonical-targets` / `BASE_CANONICAL_TARGETS` の dead vocabulary 撤去、`tools/generate_makefile_md.sh` を flat repo 用に rewrite (Phase Support Matrix 撤去、`# ----- <section> -----` ヘッダ起点 markdown table)、`docs/conventions/Makefile規約.md` 自動生成、Legacy alias `deploy-all-direct` / `verify-all` 撤去、`destroy-phase7-learning` → `destroy-coast-down` rename、`.SHELLFLAGS := -eu -o pipefail -c` 追加 (C5)。contract test 3 件 (`test_makefile.py` / `test_setup_policy_guard.py` / `test_deploy_all_contract.py`) を rename に追従、「legacy 名が leak back していない」否定 assertion 追加 |
| M-Wave8 (drift 解消) | runbook 2 本 drift 解消 (2026-05-09) | `04_検証.md` / `05_運用.md` の旧 path (`/search` `/feedback` `/jobs/check-retrain`) → canonical 4 軸 (`/api/v1/*` `/ops/*`) に統一、旧 lexical 検索基盤 (Wave 6 以前の Cloud Run service) の完全削除を確認 (historical 注記のみ残置)、`make check` 779 passed / 2 skipped。Phase 完全撤廃は M-Wave8.5 で別 sprint 切り出し |
| M-Wave8.5 | Phase 概念完全撤廃 (2026-05-10) | `grep -rE "Phase [0-9]" docs/ tests/integration/workflow/` が **0 件** (ADR `docs/decisions/` 除く)。100+ 件の `Phase 7/6/5/4/3/2/1/0` を固有名へ一括置換 (`canonical 構成` / `Composer なし派生` / `rerank 期` / `encoder 期` / `seed 期` / `初回ブートストラップ` 等)。ADR + 残 GCP リソース ID (e2e test fixture `phase7_*.py`) は historical 残置、production リソース ID は別 satellite で rename。`pytest` 800 passed / 2 skipped、ruff/mypy 全 PASS |
| M-Wave8.6 Phase 1 | orchestrator のドメイン分離 (2026-05-09) | `_run_tf_apply` (88 行) を `scripts/setup/tf_apply.py` に切り出し → `deploy_all.py` の `_run_*` を全て thin wrapper 化 (orchestrator only)。`destroy_all.py` を 8 step 分離 + `--from-step / --to-step` slicing で `deploy_all` と対称化。`tf-apply` 冒頭に `recover_wif_main()` idempotent 前置き hook を追加 (WIF 409 incident 再発防止)。新 contract test 2 件 |
| M-Wave8.6 Phase 2 | scripts/domain 物理再配置 (2026-05-10) | `scripts/infra/*` (12 module) → `scripts/domain/{gcp,k8s,terraform}/` に物理再配置。terraform 系 3 module は接頭辞除去で rename (`terraform_lock.py` → `lock.py` 等)。外部 import ~14 件を sed 一括置換、multi-import 6 件を解体・分散 import 化、Makefile `state-recover` target / docstring / contract test path pin も追従。`scripts/infra/` は完全消滅。`pytest` 795 passed / 2 skipped |
| M-Wave8.6 Phase 3 | adapters 構造確立 (2026-05-10) | `scripts/adapters/{__init__,kubectl,gcloud,terraform}.py` 新設。`kubectl_run` / `gcloud_run` / `terraform_run` 統一シグネチャ、各 adapter の args 構築・kwargs 伝播・chdir 挿入を contract test 5 件で固定 (`test_adapters.py`) |
| M-Wave8.6 後段 | caller migration 完了 (2026-05-10) | `subprocess.run(["kubectl"\|"gcloud"\|"terraform", ...])` を **0 件** まで削減 (acceptance ≤ 5)。10 module を migrate: `state_recovery._gcloud_json` (9 callsite), `vertex_import._gcloud_first` (2 callsite), `kube_cleanup`, `stage_apply` (apply + force-unlock), `lock` (force-unlock), `api_gke._dump_rollout_diagnostics` (5 diag), `kserve_models._dump_diagnostics` (5 diag), `destroy_check._run_json` (10 callsite), `composer_dag`, `composer_task_states`。並行で `_common.run` / 3 adapter に `input` / `env` kwarg を追加。latent `subprocess.run(..., capture=True)` 5 件 (doctor / promote x2 / api_gke_local / build_kserve_images_local) を修正。test 22 件を `patch.object(module, "run", ...)` から adapter-level mock に migrate。`pytest` 802 passed / 2 skipped、`make check` 全 PASS |
| M-Wave8.6 後段 (移行漏れ後処理) | adapter signature mismatch 修正 + capture 統一 (2026-05-11) | GCP 検証 (`make tf-bootstrap`) 着手で発覚した adapter 移行漏れ 3 件を修正: `tf_bootstrap.py` / `tf_init.py` の `gcloud_run(..., stdout=subprocess.DEVNULL, stderr=subprocess.DEVNULL)` → `gcloud_run(..., capture=True)`、`slo_status.py::_terraform_output` の `terraform_run(..., cwd=, text=, stdout=, stderr=)` → `terraform_run(..., chdir=, capture=True)`。**根因**: contract test が adapter を `_run` レベルで mock しており実 args shape の不整合を捕捉できなかった (移行 script が `subprocess.run` の kwarg をそのまま残した)。併せて `_common.run` の `capture=True` を **stdout/stderr 両捕捉** (`subprocess.run(capture_output=True)` 相当) に統一 — 移行 script の `capture_output=True → capture=True` 置換が `_common.run` 旧仕様 (stdout のみ) と semantic mismatch しており `destroy_check._run_json` 等の `.stderr` 参照が常に空になっていた regression を同時解消。`make check` 808 passed / 2 skipped |
| M-Wave9 (Step 1-5) | 公開ドメイン基盤コード (2026-05-11) | ドメイン `gcp-search-mlops-gke.dev` 購入 + Cloud DNS public zone `gcp-search-mlops-gke-dev` 作成 (Step 1-2、console)。Step 3-5: 新 module `infra/terraform/modules/dns/` (`google_compute_global_address` 予約グローバル IP + apex `google_dns_record_set` A + Certificate Manager DNS-01: `dns_authorization` + 検証 CNAME + `certificate` (managed, global) + `certificate_map` + `certificate_map_entry` + `data "google_dns_managed_zone"` で既存 zone 参照) / `dev/main.tf` 配線 + `dev/outputs.tf` に 5 出力 / `apis.tf` に `dns.googleapis.com` / gateway.yaml に `spec.addresses` static IP pin + `networking.gke.io/certmap` annotation + hostname → 実ドメイン (self-signed Secret は listener placeholder で残置、CN→`var.public_domain`) / `env/config/setting.yaml` に `public_domain` + `dns_zone_name` / Makefile が `-var` で流す / `_common.terraform_var_args()` を `CANONICAL_TF_VAR_NAMES` 既定にリファクタ (caller を `()` 呼びに統一) / `resolve_api_target(TARGET=gcp)` を「`PUBLIC_DOMAIN` あり → `https://<domain>` + `verify_tls=True` / なし → Gateway IP fallback」に二段化 / `composer/main.tf` の `API_HOST_HEADER` `API_INSECURE_TLS` 注入 + `pipeline/dags/_pod.py` の hardcoded default を撤去 (正規 cert なので不要)。新 contract test: `test_public_domain_consistency.py` + `test_resolve_api_target.py` の `TARGET=gcp` 書換え。`make check` 818 passed / 2 skipped、`make tf-validate` / `tf-fmt` PASS |
| M-Wave9 (Step 6 — deploy + 公開ドメイン疎通検証) | **全 Step 完了 (2026-05-11)** | `mlops-dev-a` で `make deploy-all` 15/15 完走 (~72 min、step 6 tf-apply が 58 min: GKE + Composer Gen3 環境作成 + VVS endpoint 再作成 & index deploy)。検証: `gcloud certificate-manager certificates describe search-api-cert --format='value(managed.state)'` = `ACTIVE` / `dig +short gcp-search-mlops-gke.dev` = 静的グローバル IP / `make ops-search TARGET=gcp` (= `https://gcp-search-mlops-gke.dev/api/v1/search`) HTTPS 200 (初回は KServe encoder cold start で 504、warm 後 200) / `curl -I https://gcp-search-mlops-gke.dev` HTTP/2 + Google Trust Services cert / `make ops-search-components` `lexical=4 semantic=3 rerank=5`。**前置きで判明した deploy-path regression 5 件を修正** (M-Wave8.6 reorg で deploy 専用パスに残った欠陥、`make check` がすり抜けていた): (1) `tf_plan.py` が `terraform_var_args()` 未使用 → step 5 で `public_domain`/`dns_zone_name` 未渡し fail → `*terraform_var_args()` に統一。(2-4) `state_recovery.py`(×2 `_recover_dataform`/`_aiplatform_get`) / `vertex_feature_store_wait.py`(`_rest_get`) が stdlib `subprocess.run(..., capture=True)` → step 6 で `TypeError: Popen ... unexpected kwarg 'capture'` → `capture_output=True, text=True`。(5) `feature_view_sync.py` / `state_recovery.py::main` の `Path(__file__).resolve().parents[2] / "infra"` (reorg で 1 階層浅く `scripts/infra/...` を参照) → step 12 で `terraform output -json failed` → `parents[3]`。regression test 追加: `test_public_domain_consistency.py::test_tf_plan_feeds_terraform_the_canonical_var_set` / 新 file `test_subprocess_run_kwargs_guard.py` (AST scan で `subprocess.run(..., capture=...)` を `scripts/app/ml/pipeline` 全域から検出) / 新 file `test_repo_relative_paths.py` (`Path(__file__).resolve().parents[N] / "<top-level dir>"` が実在 dir を指すか AST scan)。`make check` 833 → 840 passed / 2 skipped |
| Step-timing 計測 + run-all-core orchestrator 化 | per-step wall time の CSV 記録 + ETA、run-all 順修正 (2026-05-11) | 共有モジュール `scripts/lib/step_timing.py` 新設 — `record(flow, step_no, step_name, elapsed, status)` / `baselines(flow)` (直近 10 run の median) / `print_eta(flow, step_names)` / `fmt_duration`、`logs/step_timings.csv` (gitignore、`flow` カラム付き、4000→8000 行で古い行を auto trim、書込みは best-effort)。`deploy_all.py` / `destroy_all.py` の `_step_done` が `step_timing.record` を呼び、起動時に `print_eta` で ETA + 重い step トップ3 を表示。失敗 step は `status=failed` で記録 (baseline 除外、でも「どこで何秒で落ちたか」は残る)。`run-all-core` を bash recipe (`$(MAKE) step; ...`) → Python orchestrator `scripts/ops/run_all.py` に変換 (`STEPS` tuple が canonical 順、`make <step>` を順次実行 + timing 記録 + fail-fast)。同時に **step 順を修正**: `ops-label-seed` (behavior 投入) + `label-build` (`pipeline/labeling_job/main.py` で `ranking_labels` materialize) を `ops-train-now` の前へ、`ops-livez`/`ops-search` も前倒し (encoder warm 後に behavior 投入)。新 contract test: `test_step_timing.py` (7件) / `test_run_all_orchestrator.py` (4件: STEPS 順序契約・`ops-label-seed`/`label-build` < `ops-train-now`・fail-fast・Makefile 委譲) + 既存 `test_deploy_all_step_timing.py` / `test_deploy_all_contract.py` / `test_elasticsearch_workflow_contract.py` を新構造に追従。`make check` 840 passed / 2 skipped、`check-layers: OK` |

#### C. Satellite 改善 (Wave 番号外、構造改善 / 名前空間整備 / 新機能)

| ID | 概要 | エビデンス |
|---|---|---|
| Step.precondition framework | step 前置検証フレームワーク (2026-05-10) | `DeployStep` / `DestroyStep` に `precondition: Callable[[], object] \| None` field 追加、main loop が `step.run()` 直前に `step.precondition()` を呼ぶ。`wait_until_es_healthy` を step 10 (`sync-elasticsearch`) の precondition に登録。新 contract test 4 件 |
| GCP リソース ID rename | `phase7-*` → 命名規約準拠 (2026-05-10) | (1) Memorystore: `phase7-synonym` → `mlops-synonym`。(2) Secret Manager: `phase7-synonym-redis-auth` → `mlops-synonym-redis-auth`。(3) ExternalSecret remoteRef key 同期。(4) Make target `destroy-phase7-learning` → `destroy-coast-down`。(5) Docker tag `phase7-ml-base:local` → `mlops-ml-base:local`。`enable_redis_synonym=false` default のため infra side-effect 無し。e2e test fixture 名 (`tests/e2e/test_phase7_*.py`) は historical fixture として残置 |
| EventWriter Pub/Sub 統一 | canonical 3-table emit silent gap 解消 (2026-05-10) | 旧 `CloudLoggingEventWriter` は Cloud Logging へ書くが downstream BQ sink が未配線で `mlops.{search_events, search_impressions, user_actions}` が**空のまま**だった silent gap を構造的解消。新規: (1) `PubSubEventWriter` adapter — 3 emit を 3 topic へ。(2) `module.messaging` に 3 topic + 3 BQ Subscription (`use_table_schema=true`) + 3 IAM publisher binding。(3) `MessagingSettings.search_events_topic` / `search_impressions_topic` / `user_actions_topic`。(4) `build_event_writer` 選択: 3 topic 全 set → PubSub、欠落 → CloudLogging fallback (bootstrap)。新 contract test 6 件、`test_local_boot_contract.py` の `enable_search=false` Pub/Sub 禁止 assertion を `PubSubEventWriter` まで拡張。`pytest` 808 passed / 2 skipped、`make tf-validate` PASS |
| `.claude` / `.cursor` / `.github` / `.codex` modernize | agent/skill/rule の legacy 残骸刷新 (2026-05-10) | `phase-subtraction-derivator.agent.md` → `derivation-by-subtraction-reviewer` (Phase 1-7 + 旧 lexical engine 表記を撤去、中核 5 要素ベースの subtraction 表に書換え)。`phase-doc-sync` skill → `doc-sync` (`.claude/` + `.github/` 両 mirror、正本順位 ladder 化)。`port-adapter-scaffolder` / `port-adapter-boundary-reviewer` の lexical adapter 命名を Elasticsearch prefix に統一、撤去済 backend selector token (`SEMANTIC_BACKEND` 系 = `test_codebase_invariants` の禁止 token) を除去。`.cursor/rules/*.mdc` の multi-phase model 撤去 → flat repo root 前提に rewrite、`.cursor/commands/check-parity.md` の旧 phase path 除去。`.github/workflows/*` の `# Phase 10:` コメント + README タイトル scrub。CLAUDE.md の `.claude/` 一式表を更新 |
| PMLE 学習 doc | 試験対策 doc 化 (2026-05-10) | `docs/pmle-learning-notes.md` 新設、8 章 (ECK 不整合 / KServe cold start / VVS persistent / Composer / Feature Store / SLO / WIF / 運用)。本リポの 2026-05-09/10 incident 群を Vertex AI managed vs self-hosted の比較材料として転用、暗記学習を体験記憶に変換 |

### 7.4 頻発 incident memo

#### 2026-05-09/10 sprint で固定化された 4 incident (まとめ)

| incident | 詳細 | 永続化先 |
|---|---|---|
| WIF pool soft-delete 30 日残置 → tf-apply 409 | `--from-step tf-apply` 単独実行で step 3 (`recover-wif`) を skip すると WIF 409 で 30+ 分の出戻り | `scripts/setup/tf_apply.py` 冒頭で `recover_wif_main()` idempotent hook + contract test `test_tf_apply_invokes_recover_wif_as_pre_step` |
| destroy-all step 分離なし (deploy-all との非対称) | step 単位リカバリーが destroy 側で効かず、PDCA loop 再現性が片肺 | `scripts/setup/destroy_all.py` を 8 step + `DestroyStep` dataclass + `--from-step/--to-step` slicing で対称化。contract test `test_destroy_all_provides_step_slicing_symmetric_with_deploy_all` |
| terraform_lock parser が ANSI escape で fail | bg kill 後の lock 残置で自動 unlock が機能せず手動 force-unlock が必要に | `scripts/domain/terraform/lock.py` を ANSI escape strip + 緩い行頭 anchor (`^[^A-Za-z0-9]*ID:`) に修正。test に実 ANSI escape 入りケース追加 |
| sync-elasticsearch が ECK ApplyingChanges race | step 10 が ES 起動完了より早く走ると `Server disconnected` で fail | `scripts/domain/k8s/elasticsearch_wait.py` 新設、`_run_sync_elasticsearch` 冒頭で `wait_until_es_healthy()` (green/yellow まで poll、5 min timeout)。Makefile target も `--project-id` / `--es-url` を forward。両方 contract test pin。framework 化: `DeployStep.precondition` field + main loop が wait を呼出 (deploy_all + destroy_all 対称、unit test 3 件) |
| **ECK reconcile stall (真の root cause、2026-05-10 確定)** | 2 回連続再発 → log 調査で 2 つの構造的 bug を特定: (1) NetworkPolicy 漏れ (elastic-system ns ingress 不在 → ECK Operator が ES `:9200` に到達不可)、(2) canonical URL `http://...` と ECK 8.x default `xpack.security.http.ssl.enabled=true` の不整合 → `Server disconnected` | (1) `infra/manifests/elasticsearch/networkpolicy.yaml` に `elastic-system` ns ingress 追加。(2) `infra/manifests/elasticsearch/elasticsearch.yaml` に `spec.http.tls.selfSignedCertificate.disabled: true` + `xpack.security.authc.anonymous.{username,roles,authz_exception}` 追加 (HTTP + anonymous superuser、学習プロジェクト前提)。production 化 (HTTPS + password auth) は別 sprint backlog |

troubleshooting docs:
- [`docs/troubleshooting/eck-license-reconcile-stall.md`](../troubleshooting/eck-license-reconcile-stall.md) — ECK reconcile 14h ストール時の destroy-all 判断指針 (sunk cost cut)
- [`docs/troubleshooting/terraform-lock-stale-after-bg-kill.md`](../troubleshooting/terraform-lock-stale-after-bg-kill.md) — lock 残置からの自動回復 / 手動 force-unlock
- [`docs/troubleshooting/bg-pipe-fake-exit-zero.md`](../troubleshooting/bg-pipe-fake-exit-zero.md) — `make ... 2>&1 | tail -N` の偽 exit 0 罠と `pipefail` / `tee` パターン

#### 2026-05-11 M-Wave9 Step 6 deploy で発覚した deploy-path regression 5 件 + run-all-core 順バグ

M-Wave8.6 (scripts/domain 再配置 + adapters 構造化 + capture 統一) の reorg が **deploy 専用パス**にだけ残した欠陥。`make check` (`mypy` は `app ml pipeline` のみ、`pytest` も deploy 専用 callsite を踏まない) がすり抜けていた。`make deploy-all` を 3 回叩いて 1 件ずつ表面化 → 全修正 + AST-scan regression test 2 種で「クラス」を網羅。

| incident | 詳細 | 永続化先 |
|---|---|---|
| `tf_plan.py` が canonical `-var` set を渡さない | M-Wave9 で `terraform_var_args()` を `CANONICAL_TF_VAR_NAMES` 既定にした際 caller 移行が `tf_apply`/`destroy_all`/`recover_wif`/`state_recovery` のみで `tf_plan` 漏れ → `terraform plan` が `No value for required variable public_domain`/`dns_zone_name` で fail (step 5) | `scripts/setup/tf_plan.py` を `*terraform_var_args()` に統一。`test_public_domain_consistency.py::test_tf_plan_feeds_terraform_the_canonical_var_set` |
| stdlib `subprocess.run(..., capture=True)` 3 箇所 | M-Wave8.6 後段の `capture_output=True → capture=True` 一括置換が `_common.run` wrapper だけでなく **stdlib `subprocess.run` 直呼び**にも適用され、`state_recovery._recover_dataform`/`_aiplatform_get` + `vertex_feature_store_wait._rest_get` が `TypeError: Popen ... unexpected kwarg 'capture'` で crash (step 6 `recover_orphan_gcp_resources`) | 3 箇所 `capture_output=True, text=True`。新 file `tests/unit/scripts/test_subprocess_run_kwargs_guard.py` (AST scan: `subprocess.run(..., capture=...)` を `scripts/app/ml/pipeline` 全域で禁止) |
| `parents[N]` が reorg で 1 階層ずれ | `feature_view_sync.py` / `state_recovery.py::main` が `Path(__file__).resolve().parents[2] / "infra"` のまま `scripts/domain/gcp/` へ移動 → `INFRA = scripts/infra/terraform/environments/dev` (不在) → `terraform -chdir=<不在>` が非 0 → `terraform output -json failed while resolving Feature View IDs` (step 12 `trigger-fv-sync`) | `parents[3]` に修正。新 file `tests/unit/scripts/test_repo_relative_paths.py` (AST scan: `Path(__file__).resolve().parents[N] / "<top-level dir>"` が実在 dir を指すか) |
| `run-all-core` の step 順バグ (M-Wave5 以前から潜在) | `ops-train-now` を `ops-label-seed`/`label-build` より**前**に走らせていた → 初回 deploy 直後は `mlops.ranking_labels` が空 → `pipeline/training_job/components/load_features.py` が `rows=0` の training frame を返す → `train-reranker` が `RuntimeError: Empty training frame from load_features` で crash → Vertex pipeline `state=FAILED` → `ops-train-wait` fail。M-Wave5「PASS」は別経路 (`scripts/verify/live_acceptance.py`) だったため `make run-all-core` では露見していなかった | `run-all-core` を Python orchestrator `scripts/ops/run_all.py` 化し `STEPS` を「`...ops-label-seed → label-build → ops-train-now → ops-train-wait...`」順に修正 (encoder warm のため `ops-livez`/`ops-search` も前倒し)。`test_run_all_orchestrator.py` / `test_deploy_all_contract.py::test_run_all_core_pins_canonical_validation_path` で `ops-label-seed < ops-train-now` と `label-build < ops-train-now` を pin |

教訓: **deploy/destroy 専用の callsite は `make check` で踏まれない**ため、(a) AST-scan で「危険な書き方そのもの」を全域禁止する guard test を置く、(b) reorg 後は実機 `make deploy-all`/`destroy-all`/`run-all` を必ず一巡させる。

#### search-api ダミーイメージ混入
- 症状: `search-api` が `gcr.io/cloudrun/hello` で起動し、`ops-search` / `ops-search-components` が誤検知または失敗
- 直接原因: `infra/manifests/search-api/deployment.yaml` のダミー `image` が想定外の apply 手順で再適用される
- 恒久回避:
  - `infra/manifests/search-api/deployment.yaml` の `image: gcr.io/cloudrun/hello` に「placeholder / 本番適用禁止」コメントを維持
  - `scripts/deploy/api_gke.py` / `make deploy-api` を canonical 手順として固定
  - 適用後 `kubectl -n search get deploy/search-api -o jsonpath='{.spec.template.spec.containers[0].image}'` を必ず確認
  - スモーク前チェックに image 検証を追加し、`gcr.io/cloudrun/hello` の場合は即 fail-fast

---

## 8. 残タスクの見方

- 短期実行: [`../tasks/TASKS.md`](../tasks/TASKS.md)
- 長期Wave管理: [`../tasks/TASKS_ROADMAP.md`](../tasks/TASKS_ROADMAP.md)

## 9. 検証インベントリ（workflow contract）

- `tests/integration/workflow/`
- `tests/e2e/`
- `setup/deploy_all.py`
- `ops/vertex/{models_list,pipeline_status,vector_search,feature_group,monitoring,explain}.py`

本書は「完了済みの実装所在」を示すカタログとして維持する。

