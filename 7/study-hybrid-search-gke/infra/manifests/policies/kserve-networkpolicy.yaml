apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: kserve-allow-search
  namespace: kserve-inference
spec:
  podSelector: {}
  policyTypes:
    - Ingress
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: search
