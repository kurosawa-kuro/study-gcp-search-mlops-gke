# 図解（Phase 7）

Phase 7 の教育資料で使う図解原稿。  
Serving 層だけを GKE + KServe に差し替えることが主題。

---

## 図 1: Phase 6 → Phase 7 の差し替え

```mermaid
flowchart LR
    P6[Phase 6]
    P6 --> API6[search-api on Cloud Run]
    P6 --> EP[Vertex Endpoint]
    P7[Phase 7]
    P7 --> API7[search-api on GKE]
    P7 --> KS[KServe InferenceService]
```

---

## 図 2: Phase 7 全体像

```mermaid
flowchart TB
    User --> GW[Gateway API]
    GW --> API[search-api Deployment]
    API --> KS1[encoder on KServe]
    API --> KS2[reranker on KServe]
    API --> BQ[BigQuery]
    API --> Meili[Meilisearch]
    Pipe[Vertex Pipelines] --> MR[Model Registry]
```

---

## 図 3: 変わらないもの / 変わるもの

```mermaid
flowchart LR
    Keep[継承]
    Keep --> K1[Meilisearch]
    Keep --> K2[BigQuery]
    Keep --> K3[Vertex Pipelines]
    Keep --> K4[Model Registry]
    Change[差し替え]
    Change --> C1[search-api 配置先]
    Change --> C2[推論呼び出し先]
    Change --> C3[Gateway / HPA / KServe]
```

---

## 図 4: serving 差分に集中する理由

```mermaid
flowchart TB
    Goal[Phase 7 の学習目標]
    Goal --> S1[serving を Kubernetes で理解する]
    Goal --> S2[検索中核は変えない]
    Goal --> S3[学習基盤は再利用する]
```
