# 図解（Phase 4）

Phase 4 の教育資料で使う図解原稿。  
ローカル検索を GCP 実行基盤へ移す構図を説明する。

---

## 図 1: GCP 全体像

```mermaid
flowchart TB
    Raw[raw data] --> DF[Dataform]
    DF --> BQ[BigQuery feature_mart]
    BQ --> Embed[embedding-job]
    BQ --> Train[training-job]
    Train --> GCS[GCS artifacts]
    API[search-api on Cloud Run] --> Meili[Meilisearch on Cloud Run]
    API --> BQ
    API --> GCS
```

---

## 図 2: GCP 技術と工程

```mermaid
flowchart LR
    CR[Cloud Run] --> API[API / Job]
    BQ[BigQuery] --> FE[feature / logs / semantic]
    GCS[GCS] --> Art[model artifacts]
    PS[Pub/Sub] --> FB[feedback]
    TF[Terraform] --> Infra[resource 管理]
```

---

## 図 3: Secret Manager の流れ

```mermaid
flowchart TB
    SM[Secret Manager]
    SM --> CR[Cloud Run --set-secrets]
    CR --> App[settings 読み込み]
    App --> Meili[Meilisearch master key 利用]
```

---

## 図 4: 再学習ループ

```mermaid
flowchart LR
    Search[/search] --> Log[ranking_log]
    User[feedback] --> FB[feedback_events]
    Log --> Retrain[training-job]
    FB --> Retrain
    Retrain --> Model[GCS / training_runs]
```
