# 図解（Phase 5）

Phase 5 の教育資料で使う図解原稿。  
Phase 4 から Vertex 標準 MLOps へ責務を移す図だけを残す。

---

## 図 1: Phase 4 → Phase 5 の責務移動

```mermaid
flowchart LR
    CRJ[Cloud Run Jobs] --> VAP[Vertex AI Pipelines]
    LocalLoad[API 内部 load] --> VEP[Vertex Endpoint]
    GCSOnly[GCS 中心管理] --> MR[Model Registry]
    Monitor[Cloud Monitoring 中心] --> MM[Model Monitoring]
```

---

## 図 2: Phase 5 全体像

```mermaid
flowchart TB
    DF[Dataform] --> FG[Feature Group]
    DF --> BQ[BigQuery]
    Pipe[Vertex AI Pipelines] --> MR[Model Registry]
    API[search-api] --> EP1[encoder Endpoint]
    API --> EP2[reranker Endpoint]
    API --> BQ
    API --> Meili[Meilisearch]
```

---

## 図 3: search-api の役割変化

```mermaid
flowchart LR
    User --> API[search-api]
    API --> EP[Vertex Endpoint]
    API --> BQ[BigQuery VECTOR_SEARCH]
    API --> Meili[Meilisearch]
    EP --> Result[rerank result]
```

---

## 図 4: Vertex 標準機能の整理

```mermaid
flowchart TB
    V[Vertex AI]
    V --> P[Pipelines]
    V --> E[Endpoint]
    V --> R[Registry]
    V --> F[Feature Group]
    V --> M[Monitoring]
```
